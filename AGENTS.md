# AGENTS.md —《大時代的故事》remake 作業規範

本檔是 Codex、Claude 與其他自動化代理在本儲存庫工作的共同入口。面向使用者與撰寫文件，
預設一律使用繁體中文；程式識別字、命令、API、工具、產品與檔名保留原文，並以完整中文句型包覆。

## 1. 每輪開工順序

1. 先讀 `CONTEXT.md` 的現況、已被推翻的斷言與 worklist。
2. 再讀 `CLAUDE.md` 的硬規則，以及與任務直接相關的 `docs/spec/`、`docs/re/`、
   `docs/mechanics/` 或 `docs/playtest/`；交接時另讀 `HANDOFF.md`。
3. 需求涉及老遊戲、中文化、remake、逆向、開發環境、打包、配樂、字型或實機試玩時，
   先讀 `~/.codex/knowledge-base/knowledge-router.md` 的「復古遊戲路由」，再只載入一份
   任務入口與必要 reference。找不到時要明說，不能假裝已讀。
4. 執行 `git status --short`。工作樹中的既有變更屬於使用者或前一輪工作，不得 reset、覆蓋或丟棄。
5. 先查既有結論與實作，再開始新的逆向或編碼；搜尋落空不等於不存在。

狀態來源有衝突時，以可重現的原版實測、IDA 證據、目前程式與實際測試結果為準，並訂正文件。
`CONTEXT.md` 是狀態入口，但其中部分早期狀態列會被同檔後文推翻，不能只讀表格單列。

## 2. 專案目的與邊界

目標是完整逆向漢堂國際資訊 1992 年 DOS 遊戲《大時代的故事》，以 Go／Ebiten 乾淨重寫
跨平台引擎，還原繁體中文母本，並提供英文與日文語系。

- 核心規則對齊原版；現代化外殼必須明列為 remake 差異。
- 不自行發明、平衡或「順手修正」玩法。疑似原版 bug 先照錄、標證據與推論等級，是否修改由使用者決定。
- 不散布原版執行檔、資料、美術、音樂、其旋律衍生物或倚天字型。
- 唯一圖像白名單是 `docs/images/*.png` 的成果展示截圖；其他位置的 PNG 也會被拒絕清單擋下。
- 原始素材在 `workplace/orig/`，一律唯讀。存檔實驗使用副本或明確輸出目錄。

兩條開發原則：完整性優先於投報；規格驅動開發（SDD），只有標記 `READY` 的規格才能實作。

## 3. Docker-only 硬規則

分析、批次搜尋、轉檔、建置、測試、抓圖、執行程式、IDA、Wine、Xvfb、DOSBox、SDL、
音訊及 GUI 自動化全部只能在 Docker 容器內執行。主機只用於必要的 `docker`、`git`、
工作樹狀態檢查與儲存庫檔案編輯；不得在主機直接執行 Python、專案程式或測試。

- 一次性工作使用 `docker run --rm`，設定合理的 `--memory`、`--cpus`、`--pids-limit`
  與外層逾時；預設 `--network none`。
- 原始資料與研究輸入唯讀掛載；只有工作樹或明確輸出目錄可寫。
- 可寫容器必須使用 `-u "$(id -u):$(id -g)"`，寫前檢查目標擁有權，寫後抽查。
- 不得把主機 Python、虛擬環境或未鎖版 library 掛入工具映像掩蓋版本問題。
- 優先沿用既有映像：Go 使用 `dsds-go:1.25`；IDA 使用 `ida-pro-9.4-ver2`。
- 禁止全域 `docker image/volume/system/container prune`、`docker builder prune` 與 `docker rmi`。
- 每批工作後檢查專案相關容器；只清理由本輪建立且已無用途的容器。不得碰其他專案容器。

既有包裝器：`tools/go.sh`、`tools/py.sh`、`tools/ida.sh`、`tools/dosbox.sh`。
若包裝器缺少必要的資源限制或隔離設定，應修正可重現工具鏈，不可退回主機執行。

## 4. 逆向證據契約

Oracle 優先序：固定狀態的 DOSBox 原版實測 > IDA Pro 9.4 反組譯 > 官方說明書／當年資料 >
社群資料。社群資料與修改版只能當搜尋線索。

- 反組譯先用 IDA Pro 9.4 與 `.i64` 的交叉參照、函式邊界、資料流；Ghidra 只作交叉驗證。
- 不以攤平 `.asm` 取代 `.i64` 關係圖。讀函式用 `tools/dump_func.py`，查位址用
  `tools/addr.py`，查直接交叉參照用 IDC 工具。
- IDA 線性位址與遊戲 `ds:` 偏移是兩套名稱；使用 `tools/addr.py` 的
  `DSEG_BASE = 0x641B0` 換算並清楚標示位址空間。
- `add di, 常數` 等算術不會建立 xref；直接 xref 也抓不到取址後的間接讀寫。
  「讀多寫少」或「零命中」時，先驗查詢工具的正對照，再追取址端與間接存取。
- 保留原函式名、全域名、位址、結構偏移與原始運算元。語意只能用附加註記或受版控索引，
  不以推測性改名覆蓋唯一定位資訊。
- 每筆語意標 `confirmed`、強證據、假說或未知，附輸入檔名、SHA-256、工具版本、位址空間與出處。
  未達 `confirmed` 的項目必須有醒目警示；名稱本身不是證據。
- 推翻舊結論時保留原證據索引，寫明新證據為何足以否定舊解釋，並追加到
  `CONTEXT.md` 的「已被推翻的斷言」，不可抹去歷史。
- 比對結構化存檔時至少使用兩份樣本；一致資料與未初始化殘留必須分開判定。

## 5. 程式與文件紀律

- 分層維持單向：`internal/ui` → `internal/game` → `internal/assets`；排版邏輯不得依賴 Ebiten。
- 一條規則只保留一份實作；新增規則函式前先搜尋現有程式與文件。
- 原版使用 `0xFF` 等哨兵時，不得讓 Go 零值悄悄取代；在唯一入口正規化並測試。
- 存檔寫回採「從原始 bytes 改寫已解欄位」，未解 bytes 不動；要求 byte-for-byte round-trip。
- 所有玩家可見文字放語系資料，不寫死在 Go；繁中是還原母本，英文與日文是後續語系。
- 每解出一條機制，當場更新 `docs/mechanics/`；AI 的優先序、目標、派將、撤退、亂數與難度
  一律同步到 `docs/mechanics/70-ai.md`。
- 任何 `confirmed` 結論要有位址、byte range、資料 diff、原版截圖或可重現實驗。
- 完成宣告至少包含：相關測試、原版／reference 對照、資產拒絕掃描、dirty-tree 檢查與 Docker 清理狀態。
- 若有代理並行寫檔，主迴圈不得使用 `git add -A` 或 `git add .`；提交時列出明確路徑。
- 除非使用者明確要求，不自行 commit 或 push。

## 6. 目前進度快照（2026-08-02，經本工作樹校準）

| 里程碑 | 狀態 | 已達成／主要缺口 |
|---|---|---|
| M0 資產解密 | 完成 | `.TPC`、`.15`、`.RGB`、`.GLB/.GTB`、`.MUS/.TIM`、`.DAT`、`NWMAP`、`RAIL.TPC`、`PLACD.SAV`、`NEWSDATA.DAT` 已解並有 round-trip 證據 |
| M1 文本還原鏈 | 完成 | 51 個字模檔、6,174 字模全量反查；詞表與圖像式新聞文本已定位 |
| M2 執行檔反組譯 | 進行中 | 5 支中 4 支已進 IDA；`SDFA.EXE` 尚未解包；模組間檔案交接仍未全解 |
| M3 規則規格 | 進行中 | 4 份規格 `READY`；15/15 政略指令已定位，戰鬥公式與 AI 主鏈大幅解出；部分政略數值、存檔欄位及戰鬥 AI 分支仍缺 |
| M4 Go 規則層 | 進行中 | 資產、地圖、將領、省份、存檔、政略 AI 與端到端戰鬥已有實作；尚未宣告原版全流程等價 |
| M5 呈現層 | 進行中 | 已有政略畫面、15 項指令選單、六角戰場與部隊圖示；完整互動、畫面對齊與音訊播放仍缺 |
| M6 多語系 | 未開始 | 繁中母本已有；英文、日文與版面適配尚未進行 |
| M7 打包發行 | 未開始 | 跨平台包裝與正常玩家路徑驗收尚未進行 |

本次校準結果：`go test -count=1 -json ./...` 有 **474 個測試案例通過**；
`tools/deny_scan.sh --all` 掃描 **380 個檔案**，原版資產零命中，展示截圖 2 張位置合規。
工作階段紀錄另確認：人物自傳名冊 **417/417 槽位骨架完成**，389 篇成文、30 筆 `unknown`
不立傳；但人物接合、排版、自傳頁與入口尚未實作，不能宣稱玩家功能完成。
`docs/design/20-biography-system.md` 與 `docs/design/10-visual-modernization.md` 均仍為 DRAFT；
modern UI 的 `internal/ui/theme`、`textlayout` 與 `-theme` 旗標目前也都不存在。
此支線目前暫停，且 `#16`／`#21` 的跨批次重複仍須人工合併。

## 7. 目前優先順序

1. 先以現況重驗 DOSBox oracle 的按鍵／焦點路徑；文件同時存在「曾回歸」與「已恢復」紀錄，
   必須用當前容器與固定操作序列裁決，不可沿用舊結論。
2. 補 `.DT1` 未解欄位與區塊 8／9／10、檔尾 7 B，以及 `.DT2` 剩餘部隊欄位。
3. 補完政略指令的數值規則、戰鬥 AI 未解行動、執行期單位欄位與 196×196 成本矩陣用途。
4. 擴充呈現層與 OPL2 音訊，建立固定狀態的原版／remake 截圖對照。
5. 待繁中正常玩家路徑可驗收後，再開始英文／日文版面與跨平台發行。

`SDFA.EXE` 優先度較低，但不得以「投報低」永久跳過；若要處理，優先在既有 IDA 工具鏈中
動態 dump 或建立可重現解包流程。

## 8. 常用驗證

下列命令代表專案慣例，但實際執行仍須遵守本檔的 Docker 資源限制與網路隔離：

```sh
tools/go.sh test ./...
tools/py.sh tools/dump_func.py sub_XXXXX
tools/py.sh tools/addr.py -6221h
tools/ida.sh raw idat -A "-S/work/tools/ida_xref.idc <symbol>" WAR.EXE.i64
tools/deny_scan.sh --all
```

宣稱某畫面、規則或玩家路徑完成前，測試綠只是必要條件；還要有固定狀態的原版 oracle
或等價的獨立證據。不得用 debug 傳送、強制勝利、傳送座標或直接賦予資源代替正常玩家路徑。
