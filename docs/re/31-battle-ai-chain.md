# 31 — 戰鬥中的 AI：決策鏈 `sub_3AB99` 與 13 種行動

> 輸入檔：`WAR.EXE`
> SHA-256：`11dbfcf24686ab7765f788b38514cefd2039d0f60b6bd517d89fb5a84c068015`
> 日期：2026-08-01
> 位址：IDA linear address
> 工具：`tools/battle_ai_chain.py`、`tools/tp_real.py`

`docs/mechanics/70-ai.md` §7 把「戰鬥中的 AI」列為未解，並寫「`sub_55CEC`
的 8 個呼叫端是入口」。順著那條線走進去，找到的是**一整套與政略決策鏈
同構的戰鬥決策鏈**。

---

## 1. 三層結構

```
sub_39B6E          戰鬥主函式
  └ sub_3F698      ← **戰鬥 AI 的主入口**（189 行）
      ├ sub_3AB99  ← **決策鏈**：決定要做什麼，寫進 byte_6AA85 / byte_6AA84
      └ switch     ← **分派器**：依那個值呼叫對應的行動函式
```

`sub_39B6E` 在 `+299` 與 `+4C7` 兩處呼叫 `sub_3F698`，各傳一個 `arg_0`。

✅ **`arg_0` 決定走哪一條決策鏈，而兩條鏈各管一方**——原本標「假說」，
§32 已確認：分支 A（`byte_6AA85`）操作 `764h`（第二方），
分支 B（`byte_6AA84`）操作 `750h`（第一方）。
⚠️ 與 `byte_64901`（攻方 1／守方 2，`30-combat.md` §3i）的對應仍**未驗**——
「第一方／第二方」不等於「攻方／守方」。

## 2. `sub_3AB99`：決策鏈 — **confirmed（結構）**

與政略決策鏈**完全同構**：優先序 + 一個「已決定」旗標（`var_1`），
第一個做出決定的就是答案。

```
sub_55FBE(&格清單, &數量)                    ; 先掃全場（見 §5）

if arg_0 == 0:                               ; 分支 A，五步
    byte_6AA85 = 0
    sub_3A817                                ; 11 / 12 / 16 / 17
    if !決定: sub_3A885                      ; 12
    if !決定: sub_3A8C8                      ; 19
    if !決定 且 byte_6FFCA & 4: sub_3A8F7    ; 14 / 15 / 18
    if !決定 且 byte_6FFCA & 4: sub_3A94E    ; 16 / 17
    if !決定: byte_6AA85 = 13                ; ← **預設行動**
    sub_3A988                                ; 不設值，收尾
    sub_3A9C9                                ; 不設值，收尾
else:                                        ; 分支 B，三步
    byte_6AA84 &= 0C0h                       ; 只留 bit 6/7
    sub_3A9F4                                ; 2
    if !決定: sub_3AA51                      ; 1 / 3
    if !決定 且 byte_6FFCA & 4: sub_3AABA    ; 4
    if !決定: byte_6AA84 |= 3                ; ← **預設行動**
    sub_3AAF4                                ; 不設值，收尾
```

**`byte_6FFCA & 4` 又出現了**——與政略決策鏈的「啟用最後三步」
（`70-ai.md` §6d）是同一個位元、同一個手法。這是 bit 2 的**第四個用途**。

⚠️ `byte_6AA84` 開頭那三段位元操作（`and 80h / or 80h`、`and 40h / or 40h`）
是**恆等變換**——`x & 80h` 非零時再 `or 80h` 不改變任何東西。
淨效果就是最後那一行 `and 0C0h`。與 `sub_306CF` 的死碼（`70-ai.md` §3u）
同型：寫了但不生效。

## 3. 分派表：13 種行動 — **confirmed**

`sub_3F698` 依決策值分派。兩條鏈都取 `& 1Fh`，所以 **bit 5–7 另有用途**
（未解）。

### 分支 A（`byte_6AA85`，九種）

| 值 | 行動函式 | 由哪一步決定 |
|---:|---|---|
| 11 | `sub_3B19C(bp, 0)` | `sub_3A817`／`sub_3A94E` |
| 12 | `sub_3CBD9` | `sub_3A817`／`sub_3A885`／`sub_3A8F7` |
| 13 | `sub_3D57B` | **預設**（五步都沒決定時）|
| 14 | `sub_3D823(bp, 14)` | `sub_3A8F7` |
| 15 | `sub_3D823(bp, 15)` | `sub_3A8F7` |
| 16 | `sub_3DD05` | `sub_3A817`／`sub_3A94E` |
| 17 | `sub_3DED9` | `sub_3A817`／`sub_3A94E` |
| 18 | `sub_3E24F` | `sub_3A8F7` |
| 19 | `sub_3E691` | `sub_3A8C8` |

⚠️ 14 與 15 走**同一支函式**，只差參數——與 `sub_150FC`／`sub_15925`
「同一支函式用不同參數」的手法相同（`70-ai.md` §6d）。

### 分支 B（`byte_6AA84`，四種）

| 值 | 行動函式 | 由哪一步決定 |
|---:|---|---|
| 1 | `sub_3B19C(bp, 1)` | `sub_3AA51` |
| 2 | `sub_3B31D` | `sub_3A9F4` |
| 3 | `sub_3C89A` | `sub_3AA51`／**預設** |
| 4 | `sub_3CA09` | `sub_3AABA` |

**`sub_3B19C` 兩條鏈共用**（參數 0／1），與 `sub_14F9A`（調動）被六步共用
是同一種結構。

### 分派之後還有三支收尾

```
if byte_6A40A == 0:
    if byte_6FFCA & 4:        sub_3EF98
    if byte_6AA84 & 1Fh != 2: sub_3F0EF
if byte_6AA84 & 1Fh != 2:     sub_3F623
```

**決策值 2 會跳過兩支收尾**——那個行動與其他三種性質不同（未解是哪裡不同）。

## 4. `sub_3A320`：戰力比門檻 — **confirmed**

決策鏈用它做判斷，與政略的 `sub_16F89`（優勢比，`70-ai.md` §3w）同型：

```
if mode != 0:  mine = Σ sub_5A0B9(word[0x750 + i×2])   i = 1..10
               other = Σ sub_5A0B9(word[0x764 + i×2])
else:          mine 與 other 對調
回 (mine × Real 常數) ≤ other
```

`0x750`／`0x764` 是兩方各 10 個單位的清單（`docs/re/05` §已記），
`sub_5A0B9` 是單一將領的攻擊力。**AI 的評估函式又是攻擊力公式**
——這是第六個獨立的例子。

### 常數 0.67

`sub_3A8F7` 傳的是 `AX=5280h BX=1EB8h DX=2B85h`，
解碼（`tools/tp_real.py`）得 **0.67**。

```
80 52 b8 1e 85 2b  →  0.67
```

讀作：**「我方戰力 × 0.67 ≤ 對方戰力」**，也就是**對方至少有我方的 2/3**
——勢均力敵或更差時才走那一步。

⚠️ `tools/tp_real.py` 內建 `--self-test`，用五個在別處驗過的常數
（10／0.8／1.5／2.5／3）當正對照，全過才輸出。

## 5. ⭐ 整條鏈圍繞的是**城市** — **confirmed**

決策鏈開頭先跑 `sub_55FBE`：

```
n = 0
for 格 = 0 .. 0C3h (195):
    if sub_50151(格) == 5:
        清單[++n] = 格
for i = n+1 .. 10:
    清單[i] = 0FFh              ; 補哨兵
```

**`sub_50151(格) == 5` 就是「這格是城市」**（`docs/spec/04` §2 的地物編號表，
5 = 城市，全圖 91 格、39/39 省驗證過）。

所以這一支是**掃全場找出所有城市格**，最多 10 個，不足補 `0FFh`。

> **戰鬥 AI 的每一個決策都圍繞城市。**

這解釋了整條鏈的形狀：`sub_3A766`（§6）檢查那些城市格有沒有被攻方站住
或被逼近，六支行動函式也各自拿這份城市清單去做事。
與省份面板顯示「城市數」、`WARPOS.DAT` 每個鄰省分區 10 格對上 10 個部隊槽
（`docs/re/07`）都對得上——**戰場的目標是城市，不是殲滅**。

### `sub_50151`：地形解碼器 — **confirmed**

它同時證實了 `CLAUDE.md` §3 記的 `30 + 25×圖塊 + 地形` 疊加編碼：

```
if 格 == 0FFh: 回 var_1              ; ⚠️ **未初始化**（見下）
v = word[796h + 格×2] & 0FFFh        ; 戰場地形陣列，取低 12 位
if v ≥ 25:
    v -= 30                          ; ← 先減掉 30 那一層
    while v > 25: v -= 25            ; ← 再一路減 25
回 v                                  ; 1..25 的底層地形編號
```

`& 0FFFh` 取低 12 位——高 4 位就是 `NWMAP.DAT` 那批未解的旗標
（`CONTEXT.md` worklist 1a）。**戰鬥用不到高 4 位。**

⚠️ `arg_0 == 0FFh` 時直接跳到出口，回傳**從未賦值的 `var_1`**——
又一個讀未初始化堆疊值的地方（與 `Strength` 的 `BranchWeight == 0`
同型，`strength.go`）。哨兵值 `0FFh` 正是 `sub_55FBE` 自己補的那個，
所以**呼叫端必須先擋掉哨兵**，不能把整份 10 格清單直接餵進來。

## 6. `sub_3A766`：有沒有攻方單位逼近 — **confirmed**

`sub_3A8F7`（決策步驟 4）用它：

```
sub_55FBE(&清單, &n)
for i = 1..n:
    unit = 格佔用表[清單[i]]           ; word[62A8h + 格×2]
    if unit == 0: continue
    if 將領[unit].+8 != 0:  回 true     ; **那格站的是攻方**（+8 = 攻方 1／守方 0）
    if sub_55CEC(2, 格, unit) > 0: 回 true   ; 或 距離 2 內有敵人
回 false
```

`+8` 與格佔用表都是已解的欄位（`30-combat.md` §3d、`docs/re/07` §8）。

## 7. 未解

- **12 種行動的內容**（`sub_3B19C` 已解，見 §9）：`sub_3B31D`／`sub_3C89A`／
  `sub_3CA09`／`sub_3CBD9`／`sub_3D57B`／`sub_3D823`／`sub_3DD05`／`sub_3DED9`／
  `sub_3E24F`／`sub_3E691` 未讀。**這是下一步最有價值的目標。**
- `[di+91Eh]` 與 `[di+65BAh]` 兩張表（`sub_567B9` 會寫，§10）。
- 執行期單位記錄的 `+9`／`+10`／`+11`／`+12`——戰鬥 AI 的行動函式在寫它們（§10）。
- `sub_50151` 回傳的地形值除了 5（城市）之外，哪些行動看哪些地形。
- 決策步驟本身只掃到「設哪個值」，**每一步的判斷條件未讀**
  （`sub_3A817` 63 行、`sub_3AA51` 64 行…）。
- `arg_0` 與 `byte_64901`（攻方／守方）的對應。
- `byte_6AA84`／`byte_6AA85` 的 **bit 5–7**（兩處分派都取 `& 1Fh`）。
- 三支收尾函式 `sub_3EF98`／`sub_3F0EF`／`sub_3F623`，
  以及為什麼決策值 2 會跳過其中兩支。
- `byte_6A40A`（`sub_54391` 每場戰鬥設成 1，`30-combat.md` §3i）在這裡當
  gate 用，語意未解。

## 8. 這一輪的方法

沒有逐支讀 100 多行組語，而是**先掃形狀**：11 支決策步驟都是同一個模式
（呼叫 `sub_3A320` 比戰力 → 設一個值 → 立旗標），
所以 `tools/battle_ai_chain.py` 一次抽出全部的「常數 + 決策值 + 呼叫」。

> ⚠️ 第一版把 `12h` 讀成十進位 12（正確是 18）——IDA 的 `h` 後綴是可選的，
> 正則寫成 `([0-9A-F]+)h?` 之後沒有記下有沒有真的匹配到 `h`。
> 症狀是「值域看起來很合理」（11–15），**不會報錯**。
> 修法是把後綴抓進 group 再決定基數。這與 `tools/field_writes.py`
> 那次「過濾器有洞造成假零命中」同型：**掃描腳本本身要有正對照**。


## 9. ⭐ 行動 A11 / B1 = **結束戰鬥** — **confirmed**

分派表裡最短的一支：`sub_3B19C` 只有 15 行，轉手呼叫 `sub_3AC7B`：

```
if byte_6A8F2 != 0: 什麼也不做
if arg_0 != 0: sub_54E3B(word_64942, word_64944)
else:          sub_54E3B(word_64944, word_64942)
```

`sub_54E3B` 就是**九步結算**（`30-combat.md` §3）。第二個參數是**勝方**
——`sub_54DAC` 拿它與 `word_64942`（攻方勢力）比，相等就把省份易主。

所以：

| 分支 | 決策值 | 參數 | 意思 |
|---|---:|---:|---|
| A | 11 | 0 | 結算，**判給 `word_64942`**（攻方）|
| B | 1 | 1 | 結算，**判給 `word_64944`**（守方）|

`sub_3B19C` 被兩條鏈共用，參數決定判給誰——**這是 AI 的「認輸／收兵」行動**。
決策鏈算出自己贏不了（`sub_3A320` 的戰力比）就走這一步，直接進結算。

⚠️ `byte_6A8F2` 當 gate：`sub_54391`（九步的第 2 步）每場戰鬥把它設成 1，
所以**結算過一次之後就不會再結算**。這是防重入。

## 10. `sub_567B9` = **移動單位** — **confirmed（欄位）**

七支行動共用的那一支（331 行）。用 `tools/field_writes.py`（過濾器經正對照）
掃它的寫入：

```
mov [di+62A8h], ax      3 處   ← **格佔用表**
mov [di+91Eh], dl       3 處
mov byte ptr [di+65BAh], 0   2 處
```

`62A8h` 是格佔用表（`docs/re/07` §8），而移動的寫法正是
「舊格清 0、新格填單位」——`docs/re/07` 記過的那兩行。
它吃兩個格編號（`arg_2`／`arg_4`，任一是 `0FFh` 就直接返回），
形狀完全吻合。

> **七支行動共用一支移動函式**，與政略六步共用 `sub_14F9A`（調動）
> 是同一種結構。

⚠️ `[di+91Eh]` 與 `[di+65BAh]` 是另外兩張以格或單位為索引的表，**未解**。
`sub_3D0AC` 把 `65BAh` 那格設成 1、`sub_567B9` 設成 0，形狀像個佔用／
已行動旗標。

### 三支輔助函式寫的欄位

| 函式 | 寫入（將領記錄，基址 `7A7Dh`）|
|---|---|
| `sub_3D261`（191 行）| `+10`(word)、`+13`(byte) |
| `sub_3D0AC`（201 行）| `+9`(word)、`+11`(word)、`+12`(byte) |
| `sub_3DAEF`（267 行）| 只寫 `es:[di+1]`／`es:[di+3]`（區域結構）|

`+13` 是「每回合歸零」那一格（`30-combat.md` §3d）。
**`+9`／`+10`／`+11`／`+12` 目前都沒有定名**——這四格是執行期單位記錄
剩下的主要缺口，而它們正好在戰鬥 AI 的行動函式裡被寫。

⚠️ `CONTEXT.md` 把 `+9` 記成「所屬省」。這裡 `sub_3D0AC` 在**戰鬥中**寫它，
與「所屬省」的語意對不太上。**兩者至少有一個要修**，但現在證據不足以
判定是哪一個——先記成衝突，不改任何一邊。


## 11. 12 支行動函式的**共同形狀** — **confirmed（欄位）**

沒有逐支讀 200–500 行，而是用 `tools/field_writes.py`（過濾器經正對照）
一次掃全部的寫入。結果顯示它們**填同一個結構**。

### ⛔ 訂正：那個「命令結構」**就是單位記錄的 `+9` 起**

`sub_3D0AC` 有一行揭穿了它：

```asm
mov     di, ax
add     di, 7A86h                    ; ← 7A86h = 將領記錄的 +9
mov     word ptr [bp+var_8], di
mov     word ptr [bp+var_8+2], ds    ; var_8 = &將領[u].+9
...
les     di, [bp+var_8]
mov     byte ptr es:[di], 6          ; ← 就是 將領[u].+9 = 6
mov     es:[di+3], al                ; ← 就是 將領[u].+12
```

所以下面那張表的 `es:[di+N]` **要整個平移**：

```
es:[di+0] = +9      es:[di+1] = +10     es:[di+3] = +12
es:[di+9] = +18     es:[di+10] = +19    es:[di+13] = +22
```

**`+9` 就是那個「命令類型」欄位**，值域至少 `{1, 2, 3, 6}`。
`+10`（目標單位）與 `+12`（下一格）在 §12 已經定名——三格是同一組
「單位當前的命令」。

⚠️ `sub_3E691` 用 `es:[di+9]`／`+10`／`+12`／`+13` 那一組，
平移之後是 `+18`／`+19`／`+21`／`+22`——**那是另一組欄位**，不是同一個命令。

### 原始掃描結果（偏移未平移）

| 偏移 | 寬 | 寫入者 | 值 |
|---:|---|---|---|
| +0 | byte | `sub_3CBD9`／`sub_3D57B`／`sub_3DD05`／`sub_3DED9` | **2** |
| +0 | byte | `sub_3D823`／`sub_3E24F` | **3** |
| +1 | word | 六支 | 變數 |
| +3 | byte | 七支 | 變數 |
| +9 | byte | `sub_3E691` | **3** |
| +10 | word | `sub_3DD05`／`sub_3DED9`／`sub_3E691` | 變數 |
| +12 | byte | `sub_3E691` | 變數 |
| +13 | byte | `sub_3DD05`／`sub_3DED9`／`sub_3E691` | 變數 |

**`es:[di+0]` 就是單位記錄的 `+9`＝命令類型**（見上），目前看到 2 與 3，
加上 `sub_3D0AC` 寫的 6 與它檢查的 1，值域至少 `{1, 2, 3, 6}`。

### 順帶動到的執行期單位欄位

| 欄位 | 寫入者 |
|---|---|
| `+9` | `sub_3DED9`（設 **2**）、`sub_3D0AC` |
| `+10` | `sub_3CA09`、`sub_3D261` |
| `+12` | `sub_3B31D`、`sub_3CA09`、`sub_3CBD9`、`sub_3D0AC` |
| `+13` | `sub_3CBD9`／`sub_3D57B`／`sub_3DD05`／`sub_3DED9`／`sub_3D261` |

`+13` 是已知的「每回合歸零」那格（`30-combat.md` §3d）。
**`+9`／`+10`／`+12` 仍未定名**（`CONTEXT.md` worklist 1g），
但現在知道**是哪些行動在寫它們**——那是下一步的入口。

### 這一輪學到的

掃形狀比逐支讀快得多，而且**看得到跨函式的模式**（12 支填同一個結構、
`+0` 是類型）。逐支讀 500 行只會得到 12 份互不相關的筆記。

⚠️ 但它**只回答「動了什麼」不回答「為什麼」**。要定名 `+9`／`+10`／`+12`
還是得挑一兩支讀完整。


## 12. ⭐⭐ 行動 B2 = **佈防**，而且解開了 `+12` = 目標格

`sub_3B31D`（107 行，分支 B 的決策值 2）讀完了：

```
sub_3B1B1(bp)                              ; 產生一份排序過的格清單
將領[word_64902].+12 = 清單[1]             ; ← **主將先拿第一名**

for i = 2..10:                             ; 第一方的單位清單 word[0x750+i×2]
    格 = 將領[u].+5
    if sub_50151(格) == 5:  continue       ; **已經站在城市上的跳過**
    sub_3B079(u)

for i = 2..10:
    if 將領[u].+12 != 0FFh:  continue      ; 已經有目標的跳過
    將領[u].+12 = 清單[++n]                ; ← **依序分派**
```

### `sub_3B1B1`：把周圍的格依**防禦力**排序

```
主將格 = 將領[word_64902].+5
sub_55632(&清單, 1, 主將格)                ; 收集 **37 格**
; 依 sub_503BB(格, 兵種) **降冪**選擇排序
```

**37 正好是六角格距離 ≤3 的格數**：`1 + 6 + 12 + 18 = 37`
（`docs/re/07` 的六角鄰接）。所以 `sub_55632` 是「收集周圍三圈」。

而 `sub_503BB` **早就解過了**——它是**地形 × 兵種的防禦表**
（`docs/re/08` §6a）。所以排序鍵是**該兵種站在該格的防禦力**，降冪。

> **行動 B2 = 佈防：找主將周圍最好守的格，主將先挑，其餘依序分配。**

### ⭐ `+12` = 目標格 — **confirmed**

四條證據指向同一件事：

1. `+12` 的值**來自格清單**（`sub_55632` 產生的 37 個格編號）。
2. **`0FFh` 是哨兵**，與 `+5`（戰場格編號）用同一套「0FFh = 沒有」。
3. **已經站在城市上的單位跳過**——目標已達成就不用再指派。
4. **已經有 `+12` 的單位跳過**——一個單位只有一個目標。

加上 `sub_567B9`（移動，§10）吃兩個格編號，`+12` 正好可以是其中的終點。

`CONTEXT.md` worklist 1g 把 `+9`／`+10`／`+12` 列為未定名。**`+12` 解了。**

### ✅ 升到 confirmed：`sub_567B9` 的回傳值就寫進 `+12`

追 `sub_3B079`（100 行，`sub_3B31D` 對每個單位呼叫它）找到那一行：

```
var_3 = 將領[u].+12                          ; 先備份
if 將領[u].+10 == 0:
    var_4 = sub_567B9(mode, 主將格, u 的格)   ; ← **回傳一個格編號**

if sub_510E0(u 的格, 主將格):                 ; 相鄰／邊界判斷
    sub_3ACDF(0, u, u)
    if 將領[u].+10 變了:
        將領[u].+10 = var_2;  將領[u].+12 = var_3    ; 還原
    else:
        將領[u].+12 = var_4                   ; ← **寫入 sub_567B9 的回傳值**
```

`sub_567B9` 吃「起點格、終點格」並回傳一個格編號，而那個值直接寫進 `+12`。
加上 §12 的四條，**`+12` 存的就是格編號，而且與移動直接相關**。

⚠️ 順帶修正 §10 對 `sub_567B9` 的描述：它不只是「移動」——
它**執行移動（寫格佔用表）並回傳一個格編號**。回傳的是「最後停在哪一格」
還是「往終點的下一跳」**還沒定**，兩者都與 `+12` 的語意相容。

### ⭐⭐ `+10` = **目標單位的 ID** — **confirmed**

`sub_3ACDF`（460 行）讀完了。它是**六方向鄰接掃描**，尾段這一段解開 `+10`：

```
for i = 1..候選數:
    候選 = var_12[i]                        ; 六方向掃到的單位
    將領[u].+12 = sub_567B9(0, 候選的格, u 的格)
    if 將領[u].+12 != 0FFh:  break          ; 走得到就停
if 將領[u].+12 != 0FFh:
    將領[u].+10 = 候選                      ; ← **存單位 ID**
```

`var_12[i]` 被 `mul 21h` 拿去索引將領記錄（33 B/筆），所以它是**單位 ID**
不是格編號。存進 `+10` 的就是那個值。

所以這一對欄位是：

| 欄位 | 語意 |
|---|---|
| `+10` | **目標單位**（要接近／攻擊誰）|
| `+12` | **往目標走的下一格**（`sub_567B9` 回傳）|

`sub_3B079` 的 `if 將領[u].+10 == 0` 讀作「**這個單位還沒有目標**」，
而不是先前猜的「路徑成本為零」。

`CONTEXT.md` worklist 1g 的三個未定名欄位，**`+10` 與 `+12` 都解了**，
只剩 `+9`。

### 六方向掃描用的位移表在 `ds:625Ah`

```
鄰 = 格 + word[625Ah + (格 mod 2)×12 + 方向×2]
```

`(格 mod 2) × 12` 是「依奇偶欄選一組 6 個 word」——
與 `docs/re/07` 解的六角位移表（偶數欄 −1/+14/+1/−15/−14/−13、
奇數欄 +13/+14/+15/−1/−14/+1）**結構完全吻合**。
那份筆記解的是 `sub_4E65C` 裡的**建表**程式，這裡是**執行期的表位址**。

### 這也解釋了為什麼決策值 2 會跳過兩支收尾

§3 記過「`byte_6AA84 & 1Fh != 2` 才呼叫 `sub_3F0EF` 與 `sub_3F623`」。
佈防**只指派目標不執行移動**，性質與其他三種行動不同——
那兩支收尾大概就是執行移動的地方。


## 13. ⭐⭐ `+9` = **命令類型**，worklist 1g 三個欄位全解

`sub_3D0AC`（201 行）同時解掉兩件事：`+9` 的語意，以及 §11 那個
「來源未追的命令結構」到底是什麼。

```
for u in 第二方的單位清單 word[0x764 + i×2]:
    if 將領[u].+9 != 1:            continue    ; ← **命令類型 == 1 才處理**
    if sub_50151(將領[u].+5) != 5: continue    ; ← **站在城市上的**

    格旗標[將領[u].+5] = 1                      ; byte[65BAh + 格] = 1

    ; 把 u 的命令整組複製給另一個單位
    將領[v].+9:+10  = 將領[u].+9:+10
    將領[v].+11:+12 = 將領[u].+11:+12
    將領[v].+12     = 將領[u].+5                ; 目標格 = u 現在的位置

    將領[u].+9  = 6                             ; ← **自己的命令改成 6**
    將領[u].+12 = var_2
```

讀作：**站在城市上的單位把守城任務交給另一個單位，自己改成命令 6**
——換防／接替。

### 執行期單位記錄的命令三格

| 欄位 | 語意 | 證據 |
|---|---|---|
| `+9` | **命令類型**（至少 1／2／3／6）| 本節 + §11 的分佈 |
| `+10` | **目標單位** | §12（`sub_3ACDF` 尾段）|
| `+12` | **往目標走的下一格** | §12（`sub_567B9` 的回傳值）|

`CONTEXT.md` worklist 1g 列的三個未定名欄位**全部解完**。

### 命令類型的值域：**1 – 6** — **confirmed（值域）／未定（語意）**

掃全檔對 `[di+7A86h]` 的寫入與比較（`+9` 的絕對定址形式）：

| 值 | 誰**寫**它 | 誰**比較**它 |
|---:|---|---|
| 1 | `sub_4798B`、`sub_47EAA` | 8 支（含 `sub_3D0AC`、`sub_3D823`、`sub_3DED9`）|
| 2 | `sub_3DED9`、`sub_3E93A`、`sub_48A95` | 11 支 |
| 3 | `sub_3C26A`、`sub_3F623`、`sub_47EAA` | 11 支 |
| 4 | `sub_3C26A` | 10 支 |
| 5 | — | 6 支 |
| 6 | `sub_3D0AC` | — |
| 變數 | `sub_3C777`(cl)、`sub_3D0AC`(cx)、`sub_3ED4F`(cx) | — |

**沒有任何函式寫立即數 5**——那個值只能從 `cl`／`cx` 那三條路徑進來，
或是初始化時設的。同理**沒有函式比較 6**，所以 6 大概是「已完成／閒置」
之類的終態。

⚠️ **每個值的語意未定。** 已知的兩條線索：`sub_3D0AC` 檢查 1（守城中）
並寫 6；`sub_3DED9` 同時寫 2 又比較 1/2/3/4/5，形狀像命令的**狀態機核心**
——那是下一步最該讀的一支。

⚠️ 幾支函式**寫不只一個值**（`sub_47EAA` 寫 1 與 3、`sub_3C26A` 寫 3 與 4），
所以「一支函式 = 一種命令」的對應**不成立**，不能靠寫入者反推語意。


## 14. `sub_3DED9`（行動 A17）= **重算全軍的行動** — **confirmed（結構）**

390 行，三個掃描迴圈，每個都掃第二方的 10 個單位（`word[0x764 + i×2]`）：

```
; 迴圈 1：命令 3 的清理
for u in 單位清單:
    if 將領[u].+9 == 3 且 將領[u].+12 == 0FFh:
        將領[u].+9 = 2                    ; ← **沒有目標格就降級**

; 迴圈 2：命令 2／4／5 一批
for u in 單位清單:
    if 將領[u].+9 ∈ {2, 4, 5} 且 sub_510E0(相鄰判斷):
        將領[u].+12 = sub_567B9(...)      ; 重算下一格
        if 將領[u].+12 == 0FFh:  sub_3DAEF(...)
        將領[u].+13 = ...

; 迴圈 3：命令 1 單獨一批
for u in 單位清單:
    if 將領[u].+9 == 1:
        將領[u].+12 = sub_567B9(...)
        if 將領[u].+12 == 0FFh:  sub_3DAEF(...)
        將領[u].+13 = ...

sub_3D0AC(...)                            ; 最後跑換防（§13）
```

**這是「重新規劃全軍的行動」**——把每個單位的下一格（`+12`）重算一遍，
算不出來的交給 `sub_3DAEF` 處理。

### 命令類型的語意線索

三個迴圈的分組本身就是證據：

| 觀察 | 推得 |
|---|---|
| **命令 3 沒有目標格就降成 2** | 3 需要目標格、2 不需要 → 3 像「前往某處」、2 像「待命」|
| **1 單獨一批**，而 `sub_3D0AC` 檢查 1 且只處理站在城市上的（§13）| 1 像「守城」|
| **2／4／5 同一批**處理 | 三者的移動需求相同 |
| 6 沒人比較（§13）| 終態 |

⚠️ 這些仍是**推論**，不是定名。要 confirm 得找到「設定命令 1／3 的地方
在做什麼」——`sub_4798B`／`sub_47EAA`（寫 1）與 `sub_3C26A`／`sub_3F623`
（寫 3）是入口。

### `+13` 也在這裡被寫

三個迴圈都寫 `+13`（`docs/re/30-combat.md` §3d 記的「每回合歸零」那格）。
形狀像「這一輪已經處理過」的標記，但**沒有直接證據**。


## 15. ⭐⭐ 命令類型的語意大致成形

讀了兩支「設定命令」的函式，語意收斂了。

### `sub_47EAA`（118 行）：命令 1 是**主將前往城市駐守**

```
u = 狀態[+0Ah]                                    ; 當前單位

; 分支 A：決策值 2（佈防，§12）且 u 是主將
if (byte_6AA84 & 1Fh) == 2 且 u == word_64902:
    將領[u].+9 = 1;  return

; 分支 B：u 是主將，`byte_6FFCA & 4` 啟用
if u == word_64902:
    if sub_50151(將領[u].+5) == 5:  return        ; ← **已經站在城市上就不做**
    if (byte_6AA84 & 0C0h) != 0C0h  或  sub_58D4A() == 0
       或 將領[u].+29 < 45  或  將領[u].+30 ≥ 35:
        將領[u].+12 = sub_47B6D(狀態)             ; 目標格
        將領[u].+9  = 1                           ; ← **命令 1**
        將領[u].+10 = 0                           ; ← **沒有目標單位**
    return

; 分支 C：u 不是主將
if 將領[u].+12 == 0FFh:                           ; 沒有目標格
    將領[u].+9 = 3                                ; ← **命令 3**
```

三條線索指向同一個語意：

1. **主將專屬**（兩個分支都檢查 `u == word_64902`）
2. **已經站在城市上就不設**（`sub_50151 == 5`）
3. **`+10 = 0`**——目標是「格」不是「單位」

再加上 §13 的 `sub_3D0AC`：「命令 1 **且站在城市上**」→ 交接並改成 6。

> **命令 1 = 前往城市駐守。命令 6 = 駐守完成（交接後的終態）。**

⚠️ 那道複合條件用到 `+29`（**體力**）與 `+30`：`+29 < 45` 或 `+30 ≥ 35`。
`+29` 已定名為體力（`docs/spec/02`），`+30` 仍未解。
`byte_6AA84 & 0C0h`（bit 6／7）也在裡面——那兩位在 §2 記過是
`sub_3AB99` 開頭唯一真正保留的。

### `sub_3F623`（58 行）：只要有人不在待命，就派 `word_64904` 去

```
for u in 第一方的單位清單:
    if 將領[u].+9 != 2:  有人不在待命 = true
if 有人不在待命 且 word_64904 != 0:
    將領[word_64904].+9 = 3
    sub_3C777(狀態)
```

**這強化了「2 = 待命」**——其他單位都在待命時就不做這件事。

### 目前的語意表

| 值 | 語意 | 證據 |
|---:|---|---|
| 1 | **前往城市駐守**（主將）| `sub_47EAA` 分支 A／B + `sub_3D0AC`（§13）|
| 2 | **待命**（不需要目標格）| `sub_3DED9` 的降級、`sub_3F623` 的「都在待命就不動」|
| 3 | **需要（重新）找目標／路徑** | `sub_47EAA` 分支 C（沒目標格就設 3）＋ `sub_3DED9`（命令 3 沒目標格就降成 2）|
| 4 | 未定 | 與 2／5 同批處理（§14）|
| 5 | 未定；**沒有函式寫立即數 5** | 同上 |
| 6 | **駐守完成**（終態，沒人比較它）| `sub_3D0AC`（§13）|

3 與 2 的關係值得記：**3 是「該找目標了」，找不到就退回 2**——
兩者形成一個小迴圈。

⚠️ 4 與 5 仍未定。`sub_3C26A`（210 行，寫 3 與 4）是下一個入口。


## 16. ⭐⭐ `sub_3A320` 六個倍率全解 + `sub_58D4A` 主將優勢等級

`sub_3A320` 的公式與 0.67 那個常數 **§4 已經記過**（追命令 4 時忘了先查，
又犯一次 `CLAUDE.md` §7.1）。這一節補的是新東西：**其餘五個呼叫端的倍率**、
`sub_58D4A`，以及把倍率掛回 §3 分派表之後解出來的語意。

`ratio` 是 Turbo Pascal 的 48-bit Real，由呼叫端用三個 push 傳進來
（`tools/tp_real.py` 的解碼規則）：

| 呼叫端 | side | 倍率 | 成立的意思 | 呼叫端要哪一邊 |
|---|---:|---:|---|---|
| `sub_3A817` | 0 | **5.0** | 第二方被壓到第一方的 1/5 以下 | 成立才繼續 |
| `sub_3AA51` | 1 | **5.0** | 第一方被壓到第二方的 1/5 以下 | 成立才繼續 |
| `sub_3A8F7` | 0 | **0.67** | 第一方 ≥ 第二方的 2/3 | **不成立**才繼續 |
| `sub_3C26A` | 1 | **0.5** | 第二方 ≥ 第一方的一半 | **不成立**才繼續 |
| `sub_3C89A` | 1 | **1.5** | 第二方 ≥ 第一方的 1.5 倍 | 成立才繼續 |
| `sub_3CA09` | 1 | **1.5** | 同上 | 成立才繼續 |

⚠️ **倍率越大，越是在問「我方是不是被輾壓」**——別把 5.0 讀成「五倍優勢」。

### ⭐⭐ 5.0 = 必勝門檻，直接跳過過程結算

那兩支寫的不是「事件碼」，是 **§3 分派表的決策值**
（`byte_6AA85` 是分支 A、`byte_6AA84` 是分支 B）。把倍率掛回分派表就閉合了：

| 判斷 | side | 成立的意思 | 決策值 | 行動 |
|---|---:|---|---:|---|
| `sub_3A817` | 0 | 第二方被壓到 1/5 以下 | A = 11 | `sub_3B19C(0)` |
| `sub_3AA51` | 1 | 第一方被壓到 1/5 以下 | B = 1 | `sub_3B19C(1)` |

而 `sub_3B19C` 這條路 **§10 已經追到底**：

```
sub_3B19C(arg) → sub_3AC7B(arg) → sub_54E3B(...)   ; 九步結算（30-combat §3）
    arg != 0: sub_54E3B(word_64942, word_64944)    ; 第二個參數是勝方
    arg == 0: sub_54E3B(word_64944, word_64942)
```

對起來完全自洽：

- 第二方被輾壓 → 分支 A → `arg = 0` → 勝方是 `word_64942`（第一方）
- 第一方被輾壓 → 分支 B → `arg = 1` → 勝方是 `word_64944`（第二方）

> **一方戰力不到對方的五分之一，電腦不打了——直接叫九步結算判勝負。**

✅ **`word_64942`／`word_64944` 已解**（§37）：兩者都是**勢力領袖 ID**，
不是「第一方／第二方的代表單位」。原本的推論方向就錯了，§35 的「反證」
也隨之消失——它不是反證，是我把變數讀錯造成的假矛盾。

⛔ 這一節第一版把 5.0 寫成「像撤退／求和的判定」，方向是反的：
那是**發動結算**不是脫離戰鬥。`sub_53619` 只是成立後的另一道確認。

Real 常數的解碼在 `internal/game/battleforce.go` 改用整數分數
（5/1、67/100、1/2、3/2）。0.67 的 Real 實際值是 **0.670000000000073**
（Real 存不下 0.67），與 `67/100` 在剛好相等的邊界會分歧——已知偏差。

### `sub_58D4A()`：主將優勢等級（0／1／2）

```
主將戰力 = sub_5A0B9(word_64902)
敵方總和 = Σ sub_5A0B9(第二方 index 1..10)

if Round(敵方總和 × 2) ≤ 主將戰力:  return 2   ; 壓倒
if 敵方總和          ≤ 主將戰力:  return 1   ; 優勢
                                  return 0   ; 劣勢
```

⚠️ **一個人對全軍**。看起來不對等，但攻擊力公式吃能力值與兵力，
主將通常是全軍最高的那個。原版就這樣寫，照抄。

## 17. `sub_3C26A`（210 行）：預備隊的投入與主將親征

上一節那兩支的第一個用途。

```
優勢 = sub_58D4A()
命令3數 = count(i in 2..10, 將領[i].+9 == 3)
待命數  = count(i in 2..10, 將領[i].+9 == 2)

; 迴圈 3：i 從 10 倒著走到 2
for i = 10 downto 2:
    if 將領[i].+9 != 2: continue
    if 待命數 > 2
       或 (待命數 == 2 且 優勢 ≥ 1)
       或 (待命數 == 1 且 優勢 == 2):
        將領[i].+9 = 4                     ; ← **投入**
        待命數--
```

三道 `cmp` 的**穩定點**是一張很乾淨的表：

| 優勢等級 | 留幾個待命 |
|---:|---:|
| 0 劣勢 | **2** |
| 1 優勢 | **1** |
| 2 壓倒 | **0** |

> **越占上風，預備隊留得越少；壓倒性優勢時全部投入。**

⚠️ 迴圈**倒著掃**，所以先被投入的是 index 大的單位；留下的是 index 小的。
兩個計數迴圈的下界都是 **2**，index 1 完全不碰。

### 後半段：主將要不要親自上

```
單位數 = count(i in 1..10, 單位存在)
戰力和 = Σ sub_5A0B9(i in 2..10)          ; ← **算了卻沒用，死碼**

if byte_64900 ≤ 4: return                  ; 語意未解的閘門

if (sub_3A320(1, 0.5) == 0 且 單位數 ≤ 5) 或 命令3數 ≤ 1:
    if 優勢 == 1: 將領[word_64902].+9 = 4
    if 優勢 == 2: 將領[word_64902].+9 = 3
    ; 優勢 == 0 → 什麼都不做
```

讀作：**兵少但打得順，或幾乎沒人在找目標了，主將才親自上；劣勢時主將不動。**

⭐ `戰力和` 算完沒有任何地方讀它——原版的死碼，記一筆免得下次又去追它。

⭐⭐ 這一段給了命令 4 最硬的線索：**壓倒性優勢時主將設 3，只是優勢時設 4**
→ **4 比 3 保守**。配上「待命(2) 被啟用會變成 4」，4 像是
「投入戰鬥但不主動找目標」。⚠️ 仍是推論，`CLAUDE.md` §9。

`sub_3C89A`／`sub_3CA09` 是 `sub_3C26A` 的兩個呼叫端，兩者都先問
「第二方 ≥ 第一方 1.5 倍嗎」（劣勢）才往下走——這條線下一輪再讀。


## 18. `byte_6AA85` 的 bit 6／7 — 解了一半

§3 記過兩處分派都取 `& 1Fh`，所以 **bit 5–7 另有用途（未解）**。
其中兩位找到來源了：

| 位元 | 設定者 | 條件 |
|---:|---|---|
| **7**（80h）| `sub_3A988` | `word[796h + i×2] & 2000h` |
| **6**（40h）| `sub_3A9C9` | `將領[word_64944].+4 == byte_6FFC4` |

`+4` 是**省份**（`docs/spec/02`），所以 bit 6 是
「第二方的代表單位在 `byte_6FFC4` 這個省」。⚠️ `byte_6FFC4` 的語意未解。

讀取端只有一處，在 `sub_3D57B`（決策值 13 的行動）：

```
if (byte_6AA85 & 80h) != 0 且 byte_64900 ≥ 5:  → loc_3D80E
```

⚠️ **順序上有陷阱**：那一串 `mov byte_6AA85, imm`（0Bh..13h）是**整個覆蓋**，
會把 bit 6／7 清掉；`or al, 80h` 是疊加。誰先誰後決定 bit 7 讀不讀得到，
**未驗**——`sub_3A988`／`sub_3A9C9` 在鏈中的位置要再確認。

bit 5 仍然沒有任何寫入者。


## 19. `sub_3C89A`（198 行）：決策值 B=3，也就是**預設行動**

§3 的表記過 `sub_3C89A` 由 `sub_3AA51` 或**預設**觸發。
分支 B 五步都沒決定時就跑它，所以這是電腦最常執行的一支。

```
sub_55FBE(&城市清單, &城市數)                  ; 掃全場城市（§5）
敵方單位數 = sub_58DF9(0)

; 前置：四道全過才「不投入預備隊」
if sub_3A320(1, 1.5)              ; 第二方 ≥ 第一方 1.5 倍（我方劣勢）
   且 敵方單位數 > 3
   且 (byte_6AA84 & 0C0h) != 80h
   且 sub_58D4A() > 0:            ; 且我方不算劣勢
    ; 跳過
else:
    sub_3C26A(狀態)               ; ← 預備隊投入 + 主將親征（§17）

清空 選中[1..10]
sub_3B492(狀態, 0, &評分表, &城市清單, 城市數)   ; ← **367 行，每個城市算一個 32-bit 分數**

for i = 1..城市數:                             ; 迴圈 1
    if 城市清單[i] == 0FFh: continue
    if 評分表[i] != 0: continue
    sub_3B780(狀態, &var_22, 城市清單[i])      ; ← 分數 0 的城市

首選 = 0
for i = 1..城市數:                             ; 迴圈 2
    if 城市清單[i] == 0FFh 或 首選 != 0: continue
    if 評分表[i] > 0: 首選 = 城市清單[i]       ; ← **第一個分數 > 0 的**

sub_3C40C(狀態, &var_22)
sub_3C213(狀態, 首選, &var_22)
sub_3C633(狀態)
sub_3C777(狀態)
```

⭐ **評分表是 32-bit 陣列**（`shl di,1` 兩次 = ×4，低位 `var_4E`、高位 `var_4C`）。
又是同一個型——這份程式的 32-bit 值一律拆成相鄰兩個 word，
第三次撞到了（`[-236h]:[-234h]`、`sub_58D4A`、這裡）。

> **戰鬥 AI 的核心是「給每個城市打一個分數」**，分數 0 的城市走一條路，
> 第一個正分的城市當目標。§5 說「每個決策都圍繞城市」，這裡是最直接的證據。

`sub_3B492`（367 行）是**評分函式**，也是這條線接下來最值得讀的一支。

## 20. `sub_58DF9(side)`：數某一方還有幾個單位

```
if side != 0: 複製 word_64902 起 20 bytes 到區域變數
else:         複製 word_64916 起 20 bytes
數其中非 0 的 word 個數（index 1..10）並回傳
```

`0x64916 = 0x64902 + 20`，兩個 10 格 word 陣列**首尾相接**。

⚠️ 這與 `sub_3A320` 掃的 `0x750`／`0x764`（§4）是**不同的兩組陣列**，
兩組都是「兩方各 10 格」。哪一組是什麼**未解**，別把兩者混為一談。

### 連帶訂正：`word_64902` 不是「主將」

§17 與 `battleforce.go` 把 `word_64902` 稱作「主將」。
從這裡看清楚了：它是**第一方單位陣列的第 1 格**（`word_64916` 是第二方的第 1 格）。

「第 1 格 = 主將」是推論，來自 `sub_3C26A` 的兩個計數迴圈**都從 index 2 起**、
第 1 格由 `word_64902` 單獨處理——形狀對得上，但沒有直接證據。
文件與程式碼一律改稱**「首位單位」**，語意標推論。


## 21. ⭐⭐ `sub_3B492` + `sub_3B400`：**挑最近的敵方城市**

§19 的評分函式解開了。367 行拆成兩段。

### 第一段：給每個城市打分

```
for i = 1..城市數:
    格 = 城市清單[i]
    u  = word[62A8h + 格×2]                ; 格佔用表（docs/re/07 §已記）
    if u == 0:                    評分[i] = 0
    elif 將領[u].+8 != 0:                   ; ← 攻方
        if (將領[u].+12 == 格 且 將領[u].+9 == 5) 或 將領[u].+12 == 0FFh:
            城市清單[i] = 0FFh              ; ← **整格剔除**
        評分[i] = 0
    else:                                   ; ← 守方
        評分[i] = sub_3B400(狀態, 格)       ; ← 分數
```

⭐⭐ `cmp byte ptr [di+7A86h], 5` 比較的是命令 5。
配上「`+12`（下一跳）正好是這個城市」，語意很清楚：

> **命令 5 = 已經朝這個城市去了。** 有人去了就把城市從候選剔除，別再派人。

⛔ 這裡原本寫「**整份程式唯一比較命令 5 的地方**」。**錯了，而且錯很多**——
全檔掃描顯示有 **6 支**函式比較命令 5（見 §36 的表）。
先是寫「唯一」，發現 §35 之後改成「兩處」，還是錯。
**「唯一」「只有一處」這種話沒有全檔掃描佐證就不該寫。**

§15 記過「沒有任何函式寫立即數 5 進 `+9`」——那一條仍然成立，
**值從哪裡來仍未解**（存檔？某個計算結果？）。

### 第二段：`sub_3B400` 就是**曼哈頓距離**

```
myCell = 將領[word_64902].+5      ; 首位單位所在格
回 |myCell div 14 − 格 div 14| + |myCell mod 14 − 格 mod 14|
```

`cwd / xor ax,dx / sub ax,dx` 是標準的絕對值慣用法，兩次。
14 就是戰場欄數（`docs/re/07` §3，196 = 14×14）。

⚠️ **戰場是六角格，這個距離卻是矩形的曼哈頓距離。** 從頭到尾沒有六角修正。
這是原版的估算方式，照抄——`internal/game/battlecity.go` 標了這一條。

### 第三段：升序排序

雙層迴圈（i = 1..n−1，j = i+1..n），比較 32-bit 評分：

```
if 評分[j] < 評分[i] 且 城市清單[j] != 0FFh: 交換
```

**升序**，小的在前；被剔除的 `0FFh` 不參與交換。

配上 §19 的「取第一個分數 > 0 的城市」，整條鏈合起來就是：

> ⭐⭐ **挑距離最近、由敵方佔據、而且還沒有人去的城市。**
> 距離 0（自己就站在城市上）被 `> 0` 濾掉。

Go 實作 `internal/game/battlecity.go`（`CellManhattan`／`RankCityTargets`／
`FirstPositiveCity`），升序那條做過正對照。
`CombatUnit` 同時補上 `Command`（`+9`）、`NextCell`（`+12`）、
`TargetUnit`（`+10`）三個欄位。


## 22. 目標選定之後：`sub_3C213` 的三向分流

```
sub_3C213(狀態, 選中城市, 旗標陣列):
    if 選中城市 == 0:                       sub_3B9D8(狀態, 1, 旗標陣列)   ; 371 行，未讀
    else:
        u = word[62A8h + 城市×2]
        if u != 0 且 將領[u].+8 == 0:       sub_3C0D5(bp)   ; ← **城市有守軍**
        else:                               sub_3BF6A(bp)   ; ← 城市空著，175 行，未讀
```

### ⭐ 讀這份程式的一個關鍵：Pascal 巢狀程序

`push bp / call sub_3C0D5` 傳的是**呼叫者的整個 stack frame**，
而 `sub_3C0D5` 用 `ss:[di+4]`／`ss:[di+6]`／`ss:[di+8]` 取回外層的三個參數。
這是 Turbo Pascal 巢狀程序（nested procedure）的標準做法。

> **看到只有一個 `arg_0` 卻在裡面 `ss:[di+n]` 亂取的函式，那是巢狀程序，
> `arg_0` 是外層的 bp。** 不是「參數很少」，是參數在外層。

## 23. ⭐⭐ `sub_3C0D5`（149 行）：把單位派去打選中的城市

```
sub_3BCED(狀態, 0, 城市格, &數量, &清單)        ; 產生某個清單

for i = 1..10:
    u = word[750h + i×2]
    if u == 0: continue
    if 旗標陣列[i-1] != 0: continue
    if 將領[u].+9 != 3 且 將領[u].+9 != 4: continue    ; ← **只派命令 3 與 4**

    將領[u].+10 = word[62A8h + 城市格×2]              ; ← 目標單位 = 城市上的守軍
    將領[u].+12 = sub_567B9(0, 城市格, 將領[u].+5)    ; ← 下一跳（尋路）

    if 將領[u].+12 == 0FFh 且 數量 > 0:               ; 走不到
        sub_58209(1, 旗標陣列, 數量, &清單, u)
```

三個收穫：

1. ⭐⭐ **命令 3 與 4 在這裡一視同仁**——兩者都是「可以派去攻擊」的狀態。
   §15 分別記成「需要找目標」與「預備隊被投入」，現在合流為
   **「等待指派目標的單位」**。兩者的差別仍未解。
2. **`+10` 的用法確定了**：目標單位 = **城市上的守軍**。
   §15 記過 `sub_47EAA` 設命令 1 時把 `+10` 寫 0（目標是格不是單位），
   兩者對照起來很一致。
3. **`sub_567B9(0, 目標格, 起點格)` 是尋路函式**，回下一跳，
   走不到回 `0FFh`。這與 `sub_47B6D`（`sub_47EAA` 用來算 `+12` 的那支）
   可能是同一件事的兩個入口，**未驗**。

剩下三支未讀：`sub_3BF6A`（175，城市空著）、`sub_3B9D8`（371，沒選到城市）、
`sub_3BCED`（清單產生）。


## 24. `sub_3BF6A`（175 行）：城市空著時改追人

`sub_3C213` 二分的另一半。與 §23 的 `sub_3C0D5` **結構完全平行**，
只有「目標是誰」不同。

```
sub_3BCED(狀態, 0, 城市格, &數量, &清單)
if 數量 == 0: return

for i = 1..10:
    u = word[750h + i×2]
    if u == 0 或 旗標陣列[i-1] != 0: continue
    if 將領[u].+9 != 3 且 != 4: continue        ; ← 同樣只派命令 3 與 4

    j = 0
    repeat                                       ; ← 逐一試清單裡的單位
        j++
        將領[u].+12 = sub_567B9(0, 將領[清單[j]].+5, 將領[u].+5)
    until 將領[u].+12 != 0FFh 或 j == 數量

    if 將領[u].+12 != 0FFh:
        將領[u].+10 = 清單[j]                    ; ← 目標單位
        旗標陣列[i-1] = 1                        ; ← 標記已指派
    else:
        sub_3B8B0(狀態, 1, u)                    ; 一個都走不到
```

兩支合起來：

| 選中的城市 | 目標單位（`+10`）| 下一跳（`+12`）|
|---|---|---|
| **有守軍** | 那個守軍 | 往城市走 |
| **空著** | 清單裡第一個**走得通**的單位 | 往那個單位走 |

⭐ 城市空著時電腦不是「去佔領」，而是**改去追人**。
「走得通」的判斷是實際跑一次尋路（`sub_567B9`），走不到就換下一個目標，
全部走不到才呼叫 `sub_3B8B0`。

`旗標陣列` 這時才看懂：它是**「這個單位已經有任務了」的標記**，
所以 §23 那支不寫它（那邊全員都指向同一個城市），這邊寫（每人目標不同）。

## 25. `sub_3BCED`：候選清單怎麼來的 — **部分**（§57 補完）

```
清空 清單[1..10]；數量 = 0
sub_55632(&格清單, 0, 城市格)              ; ← 產生一批格
for 格 in 格清單（0FFh 結束）:
    if !sub_55BCC(城市格, 格): skip
    u = word[62A8h + 格×2]
    if u == 0: skip
    ...（未讀完）
```

⛔ 上一版寫「`sub_55632` 未讀，所以清單裡是哪些單位不知道」。**錯了——
`sub_55632` §12 就解過**：`sub_55632(&清單, mode, 格)` 是**收集該格周圍三圈、
共 37 格**。所以清單的來源是清楚的：

> **清單 = 城市周圍三圈之內、格子上站著的單位。**

`sub_55BCC(a, b)` 是兩個判斷的 or：

```
sub_55BCC(a, b) = sub_510E0(a, b) 或 sub_55AB7(a, b)
```

`sub_510E0` 是**邊界／相鄰規則**（`docs/re/07` §6，`30-combat.md` 已記）。

**`sub_55AB7(a, b)` 是兩層六方向掃描**：

```
for i = 1..6:
    n1 = b + 位移[b mod 2][i]          ; ds:625Ah 的六角位移表（docs/re/07 §6）
    if !sub_510E0(b, n1): continue
    for j = 1..6:
        n2 = n1 + 位移[n1 mod 2][j]
        if !sub_510E0(n1, n2): continue
        if a == n2: return 1
return 0
```

> **`sub_55AB7(a, b)` = 「a 在 b 的兩步之內」。**

所以 `sub_55BCC(a, b)` = **a 在 b 的一步或兩步之內**，而候選清單的定義是：

> ⭐⭐ **清單 = 距離城市兩格之內、格子上站著單位的那些格。**

⚠️ `sub_55632` 先收一批格再用 `sub_55BCC` 篩到兩圈，看起來重複。
§12 那次 `sub_55632` 傳 mode **1** 收 37 格（三圈：1+6+12+18），
這裡傳 mode **0**，收的範圍可能不同——**mode 的差別未驗**，
所以不要直接說「先收三圈再篩兩圈」。

⚠️ `sub_55632` 的 mode 參數：§12 用的是 **1**（得 37 格），這裡傳 **0**，
兩者的差別未驗。

### 這是這個 session 第五次沒先查手上已有的

`sub_54DAC`／`sub_3A320`／`sub_55632`／`sub_567B9`／`sub_510E0` 全都是
文件裡已有結論、卻又整支重讀了一遍。治本的作法已經做了：
**`docs/re/00-function-index.md`**（`./tools/py.sh tools/gen_func_index.py`
自動產生，目前 391 個函式）。**動手讀任何 `sub_` 之前先 grep 那份索引。**


## 26. `sub_3C777`（143 行）：收尾補救，而且**保存-呼叫-還原**

`sub_3C89A` 的最後一支，`sub_3F623`（§15）也呼叫它。

```
n = 0
for i = 1..10:                        ; 第二方（764h）
    u = word[764h + i×2]
    if u == 0 或 將領[u].+5 == 0FFh: continue   ; 不在場上的跳過
    n++;  清單[n] = u;  旗標[i] = 0

for i = 2..10:                        ; 第一方（750h），**從 2 起**
    u = word[750h + i×2]
    if u == 0: continue
    if 將領[u].+9 == 2: continue      ; 待命的不管
    if 將領[u].+12 != 0FFh: continue  ; **已經有下一跳的不管**

    保存 = 將領[u].+9
    sub_58209(1, &旗標, n, &清單, u)
    將領[u].+9 = 保存                 ; ← **還原**
```

讀作：**掃出「不是待命、卻沒有下一跳」的單位，拿敵方在場單位的清單再救一次。**
這正是 §23／§24 那兩支派完任務之後可能留下的殘局。

⭐ `保存 → 呼叫 → 還原` 這個手法本身是線索：**呼叫鏈裡有東西會改 `+9`，
而這個呼叫端不要那個副作用。**

⛔ 這裡原本寫「`sub_58209` 會改 `+9`」。讀完 `sub_58209`（§27）之後
**證據不支持**：它只寫 `+10` 與 `+12`，完全沒碰 `+9`。
真正改 `+9` 的最可能是它裡面呼叫的 `sub_567B9`（§10 記為「移動單位」），
但**未驗**。也可能純粹是防禦性寫法。

⚠️ 第二方那個迴圈從 **1** 起、第一方從 **2** 起。與 §17 的
「index 1 由 `word_64902` 單獨處理」一致。

`sub_58209` 是這條線的關鍵共用函式（§24 與這裡都用），下一個該讀。


## 27. ⭐⭐ `sub_58209`（289 行）：照距離排序，逐一試路

§24 與 §26 共用的那支。

```
sub_58209(mode, 旗標ptr, n, 清單ptr, u):
    list[1..10] = 複製 清單 的 20 bytes
    myCol = 將領[u].+5 mod 14 ;  myRow = 將領[u].+5 div 14

    ; 選擇排序：照「到 u 的曼哈頓距離」由近而遠
    for a = 1..n-1:
        for b = a+1..n:
            if 將領[list[a]].+5 == 0FFh: continue
            dA = |rowA − myRow| + |colA − myCol|
            dB = |rowB − myRow| + |colB − myCol|
            if dA > dB: swap(list[a], list[b])

    ; 逐一試尋路，找到走得通的就收工
    j = 0
    repeat
        j++
        將領[u].+12 = sub_567B9(0, 將領[list[j]].+5, 將領[u].+5)
    until 將領[u].+12 != 0FFh 或 j == n

    if 將領[u].+12 != 0FFh:
        將領[u].+10 = list[j]                       ; 目標單位
        旗標[sub_579F4(mode, u)] = 1                ; 標記「這個單位有任務了」
```

> **把候選照「離我多近」排好，然後從最近的開始試，第一個走得通的就是目標。**

⭐ 這正好補上 §24 的對照：`sub_3BF6A` 也是逐一試尋路，但**沒有排序**
（照清單原順序）。同一個模式的兩個版本，一個帶排序一個不帶。

⭐ 曼哈頓距離**第二次**出現（§21 的城市評分是第一次）。
這份程式的距離估算一律是矩形曼哈頓，戰場卻是六角格。

### `sub_579F4(side, u)`：查 u 在哪一格

```
複製 word_64902（side != 0）或 word_64916（side == 0）起 20 bytes
for i = 1..10:
    if 陣列[i] == u: return i
return 0
```

純查詢，**不改任何東西**。回傳值當旗標陣列的索引用。

⚠️ 這推翻了 §26 原本的推論——`sub_58209` 這條鏈上唯一可能改 `+9` 的
只剩 `sub_567B9`，未驗。

## 28. 教訓：**不要用 grep 過濾組合語言**

這一輪有兩次差點誤讀，都是同一個原因：為了讓輸出短一點，用
`grep -vE "mov di, ax|add di, ax|shl di, 1"` 之類的過濾器把
「看起來是樣板」的行濾掉——**而那些正是索引計算**。

- `sub_3B492` 的排序：濾掉 `shl ax,1` 之後看不出比較的是陣列第幾格
- `sub_58209` 的尾段：濾掉 `add di, ax` 之後 `旗標[sub_579F4(...)] = 1`
  看起來像 `旗標[0] = 1`

**要短就用 `sed -n` 取行段，不要用 `grep -v` 濾內容。**
Turbo Pascal／Borland C 的 16-bit 位址計算就是靠這些「樣板行」表達的。


## 29. ⭐⭐ `sub_3CA09`（229 行）：決策值 B=4 —— 打敵方主力周邊

分派表的最後一個空格補上了（§3 分支 B）。

先比對呼叫序列，差異一目了然：

```
sub_3C89A（值 3）  55FBE 58DF9 3A320 58D4A 3C26A │ 3B492 3B780 3C40C 3C213 │ 3C633 3C777
sub_3CA09（值 4）  55FBE 58DF9 3A320 58D4A 3C26A │ 3C40C 3BCED 567B9      │ 3C633 3C777
```

**前置五支與收尾兩支完全相同**，中段換掉：值 3 走城市評分（`sub_3B492`）
再分派（`sub_3C213`），值 4 **跳過城市評分**，直接：

```
if 將領[word_64944].+5 == 0FFh: 跳到收尾        ; 第二方代表單位不在場上

sub_3BCED(狀態, 1, 將領[word_64944].+5, &數量, &清單)
                                ↑ **以第二方代表單位所在格為中心**

for i = 1..10:
    u = word[750h + i×2]
    if u == 0 或 將領[u].+9 ∉ {3, 4}: continue

    j = 0
    repeat
        j++
        將領[u].+12 = sub_567B9(1, 將領[清單[j]].+5, 將領[u].+5)
    until 將領[u].+12 != 0FFh 或 j == 數量

    if 將領[u].+12 != 0FFh:
        將領[u].+10 = 清單[j]                   ; ← 沒有「旗標 = 1」的標記
```

> **決策值 3 = 打城市；決策值 4 = 打敵方主力周邊。**

與 §24 的 `sub_3BF6A` **結構完全相同**，三個差別：

| | `sub_3BF6A`（值 3 的分支）| `sub_3CA09`（值 4）|
|---|---|---|
| 中心點 | 選中的城市格 | **第二方代表單位所在格** |
| mode | **0** | **1** |
| 成功後標旗標 | 有 | **沒有** |

### ⭐ mode 參數在同一條鏈上是一致的

§25 記過「`sub_55632` 的 mode 差別未驗」。現在多一條觀察：

```
決策值 3：sub_3BCED(…, 0, …)   sub_567B9(0, …)
決策值 4：sub_3BCED(…, 1, …)   sub_567B9(1, …)
```

**同一個決策值底下，整條鏈傳的 mode 一致。** mode 是什麼仍未解，
但它不是每支函式各自的旗標，而是**從決策層一路傳下去的參數**。
⚠️ 不要猜它是「攻守視角」或「陸路水路」——沒有證據。


## 30. ⭐⭐ `sub_3B31D`（107 行）：決策值 B=2 —— **佈防**

分支 B 的分派表最後一格。

```
sub_3B1B1(bp)                              ; 巢狀程序，填「佈防位置表」
n = 1
將領[word_64902].+12 = 位置表[1]           ; 首位單位分到第一格

for i = 2..10:                             ; 第一段
    u = word[750h + i×2]
    if u == 0: continue
    if sub_50151(將領[u].+5) == 5: continue    ; ← **已經站在城市上就跳過**
    sub_3B079(u)                                ; §12 已部分解讀

for i = 2..10:                             ; 第二段
    u = word[750h + i×2]
    if u == 0 或 將領[u].+12 != 0FFh: continue  ; 已有下一跳的不動
    n++
    if 位置表[n] == 0FFh: continue
    將領[u].+12 = 位置表[n]                     ; ← 依序發格子
```

### 位置表怎麼來的：`sub_3B1B1`（178 行）

```
位置表[1] = 將領[word_64902].+5            ; 首位單位自己的格
兵種      = 將領[word_64902].+21
sub_55632(&暫存, 1, 位置表[1])             ; ← **收集周圍 37 格**
for i = 2..37: 位置表[i] = 暫存[i-1]
```

`0x25 = 37`，與 §12 記的「`sub_55632` mode 1 收三圈共 37 格」**完全對上**
（1 + 6 + 12 + 18 = 37）。

> ⭐⭐ **決策值 2 = 佈防**：以首位單位所在格為中心，把周圍三圈的格
> 依序發給還沒有去處的單位。已經站在城市上的不動。

### 三處獨立對上

1. §15 記過 `sub_47EAA` 分支 A 的條件是 `(byte_6AA84 & 1Fh) == 2`
   → 設命令 1（**前往城市駐守**）。**那個「2」就是這裡的決策值 2。**
2. §3 記過「**決策值 2 會跳過兩支收尾**」（`sub_3F0EF`／`sub_3F623`）。
   佈防不需要「找目標」的收尾，合理。
3. 第一段跳過「已經站在城市上」的單位——與 §15 的
   「`sub_47EAA` 已在城市上就不設命令 1」同一個判斷（`sub_50151 == 5`）。

⚠️ `sub_3B1B1` 讀了 `+21`（兵種）存進區域變數，但這 60 行裡沒用到——
後半 118 行未讀，兵種多半在那裡影響分配。

### 分支 B 的分派表補完

| 值 | 行動 | 語意 |
|---:|---|---|
| 1 | `sub_3B19C(1)` | **必勝結算**（§16）|
| 2 | `sub_3B31D` | **佈防**（本節）|
| 3 | `sub_3C89A` | **打城市**（預設，§19）|
| 4 | `sub_3CA09` | **打敵方主力周邊**（§29）|

**四種行動全部有語意了。** 分支 A 的九種還有五種未讀。


## 31. ⭐⭐ `+13` 是位元欄位，bit 7 = **本回合已指派** — **confirmed**

§20 那批表只記了「哪些函式寫 `+13`」，沒看位元。全檔掃過 50 處存取之後
（20 處寫、28 處讀），它是一個**位元欄位**，用到五個位元：

| 位元 | 設起來 | 清掉 | 讀取 |
|---:|---|---|---|
| 2（04h）| `sub_457F9` | `sub_45C79` | — |
| 3（08h）| `sub_45A92` | `sub_44B10`／`sub_475EB` | `sub_44B10`／`sub_46331`／`sub_4798B` |
| 5（20h）| **`sub_3E691`**（指標寫法，§40）| — | `sub_42C8F` |
| 6（40h）| — | `sub_39B6E` | `sub_42C8F` |
| **7（80h）**| **六支 AI 行動** ＋ `sub_46A33`／`sub_46E19`／`sub_4732C` | `sub_3E81F` | `sub_4732C` |

⛔ 這裡原本寫「**bit 5 沒有任何寫入者**」。**錯了**——`sub_3E691`（值 19，§40）
會設它，寫法是 `es:[di+0Dh] |= 20h`，**透過指標**。

我的掃描只找 `mov [di+7A8Ah], cl` 這種直接形式，所以漏掉。
⚠️ **這正好印證本節自己寫的那條警告**：「xref 只涵蓋直接參考，
`ptr = &x` → `es:[di] = v` 這種間接寫入抓不到」——寫了警告，
下一段還是照著踩進去。

bit 0／1／4 仍然完全沒人碰（同樣只保證直接形式）。
bit 2／3／6 由 `0x4xxxx` 段的函式操作（玩家互動層），語意未解。

### bit 7 的三條獨立證據

**1. 設它的是分支 A 的行動函式**——`sub_3CBD9`(值 12)、`sub_3D261`、
`sub_3D57B`(值 13)、`sub_3DD05`(值 16)、`sub_3DED9`(值 17)。
每一支都在「決定好目標」之後設。

**2. `sub_3E81F` 把它和 `+10`／`+12` 一起清**：

```
for i = 1..10:
    ptr = &將領[word[750h + i×2]].+9
    ptr[3] = 0FFh          ; +12 = 沒有下一跳
    ptr[1] = 0             ; +10 = 沒有目標單位
    將領[u].+13 &= 7Fh     ; ← bit 7
```

**那三個正是「指派」寫進去的三個欄位**（§23：`+10` 目標單位、`+12` 下一跳）。
一起設、一起清 —— 它們是同一組狀態。

**3. `sub_4732C` 讀它並「已設就跳過」**：

```
if 目標格上的是攻方 且 將領[u].+13 & 80h != 0:  跳過
else:                                            sub_47075(...)
```

> **`+13` bit 7 = 這個單位這一輪已經被指派了目標。**

⭐ 與 `docs/re/14` 的 `+32` bit 2（「本回合已處理過」，立起來就不進玩家選單）
**完全同型**。這份程式用同一個手法管兩層的「做過了」：省份層用 `+32` bit 2，
單位層用 `+13` bit 7。

⚠️ `30-combat.md` §3d 記過「`+13` 每回合歸零」（`sub_5446D`）——
那是回合初始化，與這裡的 `sub_3E81F`（戰鬥中重置指派）是兩件事，不衝突。


## 32. ⭐⭐ `sub_3D57B`（333 行）：分支 A 的**預設行動**（決策值 13）

分支 A 五步都沒決定時跑它，所以這是第二方最常執行的一支。

### 結構性發現：分支 A 操作**第二方**

```
for i = 1..10:
    u = word[764h + i×2]        ; ← **764h，不是 750h**
```

§2 當初只敢說「兩條鏈各用一個相鄰的變數，形狀像兩方各一套」。現在確認：

> ⭐⭐ **分支 A（`byte_6AA85`）管第二方（`764h`）；
> 分支 B（`byte_6AA84`）管第一方（`750h`）。**

這也對上 §16 的倍率表——分支 A 的判定 `sub_3A817` 傳 `side = 0`
（我方 = 第二方），分支 B 的 `sub_3AA51` 傳 `side = 1`。
而 `sub_3B19C` 兩條鏈共用卻傳不同參數，那個參數決定的正是誰是勝方。

### 依單位的命令分流

```
sub_55FBE(&城市清單, &城市數)

for i = 1..10:
    u = word[764h + i×2];  if u == 0: 跳過

    ── 命令 1（前往城市駐守）──────────────────────
    if sub_50151(將領[u].+5) == 5:        ; 已經站在城市上
        +12 = 將領[u].+5                   ; 下一跳 = 原地
    else:
        +12 = 0FFh
        for j = 1..城市數:
            if word[62A8h + 城市[j]×2] == 0:   ; ← **空的城市**
                +12 = 城市[j]                   ; 後面的會蓋掉前面的
        if +12 == 0FFh: +12 = 城市[1]           ; 一個空城都沒有 → 取第一個

    ── 命令 2 或 3 ────────────────────────────────
    +9  = 2                                 ; ← 統一壓成 2
    +12 = 0FFh
    格 = sub_560D7(2, 將領[u].+5)
    if 格 == 0FFh 或 該格沒人 或 該格的單位不是守方: 跳過
    if sub_510E0(格, 將領[u].+5): 跳過      ; ← **已經相鄰就不用移動**
    +12 = sub_567B9(0, 格, 將領[u].+5)
    if +12 != 0FFh:
        +10 = word[62A8h + 格×2]            ; 目標單位
        +9  = 2
        +13 |= 80h                          ; ← 已指派（§31）
    else:
        sub_3D261(狀態, &城市清單, 城市數, u)

    ── 命令 4 ─────────────────────────────────────
    +12 = 將領[u].+5                        ; 下一跳 = 原地
```

收尾三支：

```
if byte_6FFCA & 4:  sub_3D0AC(狀態)          ; §13：命令 1 且站在城市上 → 交接改成 6
if (byte_6AA85 & 80h 且 byte_64900 ≥ 5) 或 byte_6B89E != 0:
    sub_3D411(狀態, &城市清單, 城市數)
```

⚠️ 命令 1 那段的「找空城」用的是**最後一個**空城（迴圈不 break，後面覆蓋前面），
不是最近的。與分支 B 值 3 的「照距離排序取最近」（§21）**完全不同的挑法**。
這是原版行為，照抄。

⚠️ `sub_560D7`（第二方找目標格用的）與 `sub_3D261`（走不到時的後備）
都未讀。`sub_3D411` 也未讀。


## 33. `sub_3CBD9`（327 行）：決策值 A=12 —— **重置後重新找目標**

與值 13（§32）前四支呼叫完全相同，全程操作 `764h`（第二方），
但有一個關鍵差別：

> **值 12 完全不看 `+9`。** 整支沒有任何對命令的比較，對全體一視同仁。

```
sub_55FBE(&城市清單, &城市數)
for i = 1..10:
    u = word[764h + i×2];  if u == 0: 跳過
    if sub_50151(將領[u].+5) == 5: …          ; 站在城市上
    格 = sub_560D7(2, 將領[u].+5)
    if 格 == 0FFh: …
    sub_3B15E(u)                               ; ← **先重置**

    找到了 = 0
    for j = 1..:                               ; 掃一份格清單
        g = 清單[j]
        v = word[62A8h + g×2]
        if v == 0: 跳過                        ; 空格
        if 將領[v].+8 != 0: 跳過               ; ← **不是守方就跳過**
        將領[u].+12 = sub_567B9(0, g, 將領[u].+5)
        if 將領[u].+12 != 0FFh: 找到了 = 1; break
    …
    將領[u].+13 |= 80h                         ; 已指派（§31）
```

### `sub_3B15E(u)`（27 行）：把單位重置回待命

```
將領[u].+9  = 2        ; 待命
將領[u].+12 = 0FFh     ; 沒有下一跳
將領[u].+10 = 0        ; 沒有目標單位
```

⭐ 與 `sub_3E81F`（§31）是一對：兩者都清 `+10`／`+12`，
但 `sub_3E81F` 清 `+13` bit 7、`sub_3B15E` 改設 `+9 = 2`。

> **值 12 = 把單位打回待命，然後重新挑一個「站著守方、而且走得通」的格。**

值 13 是**依現有命令分流**（保留狀態），值 12 是**推倒重來**。
兩者的觸發條件在 §16 的倍率表裡：值 12 由 `sub_3A817`／`sub_3A885`／
`sub_3A8F7` 決定，其中 `sub_3A8F7` 用的是 0.67（勢均力敵）。

⚠️ 那份格清單（`var_1A`）的來源沒追到——`sub_560D7` 只回一個格，
清單是別處填的。**未讀**。


## 34. ⭐⭐ `sub_3D823`（270 行）：決策值 A=14 與 15 —— **全軍撲向敵方主帥**

§3 記過「14 與 15 走同一支函式，只差參數」。那個參數的作用解出來了，
而且**兩者的效果正好相反**。

```
sub_3D823(狀態, 值):                      ; 值 = 14 或 15
    已留守 = 0
    n = 0
    for i = 1..10:                         ; 收集第一方的單位
        if word[750h + i×2] != 0:
            n++;  第一方清單[n] = word[750h + i×2]

    for i = 1..10:                         ; 處理第二方（分支 A 管第二方，§32）
        u = word[764h + i×2]

        if 已留守 == 0 且 將領[u].+9 == 1:      ; ← 命令 1 = 前往城市駐守
            if 值 == 14: 已留守 = 1              ; ← **只有 14 會設這個旗標**
            跳過這個單位                          ; 兩個值都跳過

        將領[u].+12 = sub_567B9(1, 將領[word_64902].+5, 將領[u].+5)
                                    ↑ **目標是第一方的首位單位**
        if 走不到: 掃第一方清單找別的目標
        …
```

### 那個旗標讓兩個值的效果相反

| 值 | 第一個命令 1 的單位 | 第二個以後的命令 1 | 效果 |
|---:|---|---|---|
| **14** | 跳過，`已留守 = 1` | 條件不成立 → **被派出去** | **只留一個駐守** |
| **15** | 跳過，旗標不設 | 旗標恆 0 → **繼續跳過** | **全部駐守的都留著** |

> **值 14 = 只留一個守城，其餘全部撲向敵方主帥。**
> **值 15 = 所有駐守中的都留下，只派沒在駐守的。**

⭐ **15 比 14 保守**，而不是「14 與 15 只是換個參數」。
一個旗標設不設，決定了「留一個」還是「全都留」——這種寫法很省程式碼，
但也很容易讀錯（第一眼會以為 14 才是保守的那個）。

⭐ 兩者的目標都是 **`word_64902`，也就是第一方的首位單位**（§20）。
配上 §17「首位單位常是全軍最強」的觀察，這兩個決策值是
**斬首**：不打城市、不打最近的，直接撲敵方主帥。

⚠️ `sub_567B9` 這裡傳 **mode 1**（值 12／13 傳 0），與 §29 記的
「mode 在同一條鏈上一致」相符。

§16 的倍率表記過值 14／15 由 `sub_3A8F7` 決定，用的是 **0.67**
（勢均力敵時才走）。**勢均力敵就斬首**——與「打不贏就別打城市」是一致的取捨。


## 35. `sub_3DD05`（210 行）：決策值 A=16 —— 只處理待命與已出發的單位

不掃城市（沒有 `sub_55FBE`），是分支 A 裡最短的攻擊型行動。

```
for i = 1..10:                              ; 第二方
    u = word[764h + i×2];  if u == 0: 跳過

    ; ── 篩選：哪些單位要動 ──
    if 將領[u].+9 == 2:            處理           ; 待命一律處理
    elif byte_64900 >= 5:
        if 將領[u].+9 == 4:        處理
        elif 將領[u].+9 == 5:      處理
        else:                      跳過
    else:                          跳過

    if sub_510E0(將領[word_64944].+5, 將領[u].+5): 跳過   ; ← 已相鄰就不動
    將領[u].+12 = sub_567B9(…)
    if 將領[u].+12 == 0FFh: sub_3DAEF(u)                   ; 267 行，未讀
    將領[u].+13 |= 80h                                     ; 已指派（§31）

sub_3D0AC(狀態)                                            ; §13：駐守完成的交接
```

⭐ **命令 4 與 5 要 `byte_64900 ≥ 5` 才處理，命令 2 無條件處理。**
`byte_64900` 在 `sub_3C26A`（§17）也出現過（門檻 `> 4`），
兩處門檻一致（≥ 5 等價於 > 4）。語意仍未解，但它顯然是個**階段或難度**的閘門。

⚠️ 這一節推翻了 §21 的「命令 5 唯一比較點」。

⛔ 它原本還被當成「`word_64944` 語意」的反證。**那個矛盾是假的**——
§37 用 IDA 的 xref 查出 `word_64944` 是**該省司令**，值 16 拿它當目標
完全合理（分支 A 打的就是守方領袖）。

⚠️ `sub_3DAEF`（267 行，走不到時的後備）未讀。


## 36. `+9`（命令）的全檔統計 —— 別再憑印象說「唯一」

這一節被寫錯兩次（§21 先寫「唯一比較命令 5 的地方」，訂正成「兩處」還是錯），
所以直接把全檔掃描的結果釘在這裡當基準。

### 直接比較 `cmp byte ptr [di+7A86h], N`

| 值 | 支數 | 函式 |
|---:|---:|---|
| 1 | **8** | `sub_3D0AC` `sub_3D823` `sub_3DED9` `sub_42C8F` `sub_4798B` `sub_52B50` `sub_52D8F` `sub_53B1E` |
| 2 | **11** | `sub_3B780` `sub_3B8B0` `sub_3C26A` `sub_3C633` `sub_3C777` `sub_3CF9F` `sub_3DD05` `sub_3DED9` `sub_3F623` `sub_42C8F` `sub_48F3D` |
| 3 | **11** | `sub_3B9D8` `sub_3BF6A` `sub_3C0D5` `sub_3C26A` `sub_3C4E5` `sub_3CA09` `sub_3DED9` `sub_3E93A` `sub_3E9CA` `sub_3ED4F` `sub_49494` |
| 4 | **10** | `sub_3B9D8` `sub_3BF6A` `sub_3C0D5` `sub_3C4E5` `sub_3CA09` `sub_3D411` `sub_3DD05` `sub_3DED9` `sub_3E93A` `sub_49494` |
| 5 | **6** | `sub_3B492` `sub_3C40C` `sub_3C633` `sub_3D411` `sub_3DD05` `sub_3DED9` |
| 6 | **0** | — |

### 直接寫入立即數 `mov byte ptr [di+7A86h], N`

| 值 | 支數 | 函式 |
|---:|---:|---|
| 1 | 2 | `sub_4798B` `sub_47EAA` |
| 2 | 3 | `sub_3DED9` `sub_3E93A` `sub_48A95` |
| 3 | 3 | `sub_3C26A` `sub_3F623` `sub_47EAA` |
| 4 | 1 | `sub_3C26A` |
| 5 | **0** | — |
| 6 | **0** | — |

### 讀得出來的三件事

1. ⭐ **值 5 有 6 支讀、0 支直接寫**。§15 記的「沒有任何函式寫立即數 5」
   得到全檔佐證。值一定是從別處來的——存檔、或某個計算結果。
2. ⭐ **值 6 既沒人讀也沒人直接寫**。§15 說它是「終態」，這裡是佐證：
   寫進去就再也沒有人看它。
3. `sub_3DED9`（值 17，重算全軍行動）**比較了 1 到 5 全部五個值**——
   它是唯一一支這樣做的，符合 §14「重算全軍」的定位。

⚠️ **這張表只統計 `[di+7A86h]` 的直接形式**。透過指標寫的
（`ptr = &+9` 之後 `es:[di] = N`，§23／§32 都是這種）**沒有統計進來**——
`sub_3D0AC` 把命令改成 6 就是那種形式。所以「值 6 有 0 支寫入」只代表
沒有直接形式，不代表沒人寫。要完整統計得先辨識所有 `ptr = &+9` 的位置。


## 37. ⭐⭐⭐ `word_64942`／`word_64944` = **兩個勢力領袖** — **confirmed**

這一組被推錯過兩次（§16 推成「第一方／第二方的代表單位」，§35 以為找到反證）。
用 `tools/ida_xref.idc` 查 IDA 的交叉參考圖，**寫入點只有兩處**，一讀就結案。

### `sub_4E627`（22 行）

```
word_64942 = 將領[word_64902].+14              ; 7A8Bh = 0x7A7D + 14
word_64944 = word[byte_6FFC4 × 25h − 6221h]
```

兩個都對得上既有的 confirmed 結論：

| | 出處 | 語意 |
|---|---|---|
| `+14` | `combat.go` / `sub_534FF` | **這個單位效忠的勢力領袖**（將領 ID）|
| `25h = 37` | `docs/spec/03` §1 | 省份記錄大小 |
| `−6221h` | `docs/re/10` §已記 | 省份記錄基址 `−6235h` **+ 20** = **司令**欄位 |

> ⭐⭐⭐ **`word_64942` = 首位單位效忠的勢力領袖。**
> **`word_64944` = `byte_6FFC4` 那個省的司令**（也是勢力領袖）。

`byte_6FFC4` 因此也解了：**當前交戰的省份編號**。

### 三個舊觀察一次全部對上

1. **`mul word_64944` 出現 14 次**，乘的是 `21h`（將領記錄大小）——
   它當然是將領 ID。這一條 xref 統計直接給出，不必推。
2. §16 的 `sub_54E3B(word_64944, word_64942)`「第二個參數是勝方」——
   兩個參數都是**勢力領袖**，九步結算判的是哪一方贏，語意通順。
3. §18 的 bit 6 條件「`將領[word_64944].+4 == byte_6FFC4`」——
   「該省司令本人是否就在該省」，是一個很自然的判斷。

### §35 的「反證」是假的

§35 說「值 16 是分支 A（管第二方）的行動，目標卻是 `word_64944`，
若那是第二方就成了自己打自己」。**前提就錯了**——`word_64944` 不是第二方，
是**守方領袖**。分支 A 打它完全合理。

> 讀錯一個變數，會製造出一個看起來很像真的矛盾，
> 然後那個假矛盾又會把另一個正確的結論拖下水。

### 這一節是工具換來的

§16 的錯誤推論走的是「從呼叫端的參數順序反推」——`.asm` 是攤平的文字，
沒有交叉參考圖，只能這樣推。`tools/ida_xref.idc` 直接問 IDA
「誰寫過這個位址」，答案是兩處，讀 22 行就結案。

**這就是 `CLAUDE.md` §4.1 新增「不要 grep `.asm`，要查 `.i64`」那一節的由來。**


## 38. `sub_3E24F`（541 行）：決策值 A=18 —— 分支 A 唯一比戰力的行動 — **部分**

分支 A 九種行動的最後一個。541 行，是這條鏈裡最長的一支。

### 骨架

```
sub_55FBE(&城市清單, &城市數)

; 第一段：對每個城市，統計周圍的單位
for c = 1..10:
    格 = 城市[c]
    u = word[62A8h + 格×2];  if u == 0 或 將領[u].+8 != 0: 跳過
    sub_55632(&周圍格, ?, 格)                    ; 收集周圍 37 格（§12）
    for j in 周圍格（0FFh 結束）:
        v = word[62A8h + 周圍格[j]×2];  if v == 0: 跳過
        idx = sub_579F4(將領[v].+8, v)           ; 查 v 在陣列的第幾格（§27）
        …統計…
    ⚠️ 這裡有個 cmp var_2D, 24h（36）——37 格的上界

; 第二段：同型的第二輪統計

; 第三段：⭐ 照攻擊力排序
for i:
    for j = i+1..n:
        if sub_5A0B9(清單[i]) > sub_5A0B9(清單[j]): swap(i, j)

; 第四段：對第二方單位派工
for i = 1..10:
    u = word[764h + i×2]
    if sub_50151(將領[u].+5) == 5: …             ; 站在城市上
    將領[u].+12 = sub_567B9(…)
    if 走不到: sub_3D261(…)
```

### 兩件已確認的事

1. ⭐ **這是分支 A 九種行動裡唯一呼叫 `sub_5A0B9`（攻擊力公式）的一支。**
   其餘八種都只看距離、命令、或位置關係。
2. ⭐ **排序是升序**（交換條件 `戰力[i] > 戰力[j]`，把小的往前移）。
   與 §21 的城市距離、§27 的候選距離**方向一致**——
   這份程式的排序一律升序，沒有例外。

升序 + 攻擊力 = **弱的排在前面**。配上第四段的派工，形狀是
「**挑最弱的目標打**」。

⚠️ **標為部分**：`var_2C` 那個被排序的清單，內容是第一段與第二段統計出來的，
兩段各 100 行以上**還沒讀完**。所以「挑最弱的打」是**依排序方向與派工結構推出來的形狀**，
不是直接讀到的結論。要 confirm 得把兩段統計讀完。

⚠️ `sub_3D261`（191 行，走不到時的後備）在值 13／14／15／18 都出現，仍未讀。

## 39. 分支 A 的分派表補完

| 值 | 行動 | 語意 | 節 |
|---:|---|---|---|
| 11 | `sub_3B19C(0)` | **必勝結算**（戰力差五倍直接判勝負）| §16 |
| 12 | `sub_3CBD9` | **推倒重來**（重置回待命再找目標）| §33 |
| 13 | `sub_3D57B` | **預設**（依現有命令分流）| §32 |
| 14 | `sub_3D823(14)` | **斬首**，只留一個守城 | §34 |
| 15 | `sub_3D823(15)` | **斬首**，所有駐守的都留 | §34 |
| 16 | `sub_3DD05` | 只處理待命與已出發的單位 | §35 |
| 17 | `sub_3DED9` | **重算全軍的行動** | §14 |
| 18 | `sub_3E24F` | 唯一比戰力的一支，形狀像「挑最弱的打」| §38（部分）|
| 19 | `sub_3E691` | 未讀 | — |

**分支 A 九種解 8 種，分支 B 四種全解。** 剩 19（`sub_3E691`，200 行）。


## 40. ⭐⭐ `sub_3E691`（200 行）：決策值 A=19 —— **分支 A 補完**

```
第二方清單 = 複製 word_64916 起 20 bytes
第一方清單 = 複製 word_64902 起 20 bytes
城市未滿 = sub_56D13()                       ; 「某個計數 < 城市數」
if 城市未滿 且 sub_56D49(0) 且 byte_64900 ≤ 4: 城市未滿 = 0

; ── 第一段：把第一方清單**壓實**（去掉不在場上的）──
n = 0
for i = 1..10:
    if 將領[第一方[i]].+5 == 0FFh: 跳過       ; 不在場上
    n++;  第一方[n] = 第一方[i]

; ── 第二段：對第二方每個單位派工 ──
for i = 1..10:
    u = 第二方[i];  if u == 0: 收尾
    if sub_50151(將領[u].+5) == 5 且 !城市未滿: 跳過   ; 站在城市上且城市已滿

    sub_58209(0, &旗標, n, &第一方清單, u)     ; §27：照距離排序，逐一試路
    將領[u].+9  = 3                            ; ← 命令 3（找目標）
    將領[u].+13 |= 20h                         ; ← **bit 5**
    if 將領[u].+12 == 0FFh: …word_64902…       ; 走不到時的後備
```

> **值 19 = 把第二方全體設成「找目標」，目標池是第一方還在場上的單位。**

與 §34 的斬首（值 14／15，只打 `word_64902` 一個）不同：
這裡的目標池是**第一方所有在場單位**，而且用 `sub_58209` 照距離排序——
形狀是**全面接戰**而不是斬首。

⭐ 「壓實清單」那一步值得記：原版不是把不在場的標成無效，
而是**把陣列往前擠**，讓 `sub_58209` 的 `n` 直接是有效個數。

### ⭐⭐ 順帶找到 `+13` bit 5 的寫入者

`es:[di+0Dh] |= 20h`（ptr 基址是 `7A7Dh`，即 `+0`，所以 `+0Dh` = `+13`）。

§31 說「bit 5 沒有任何寫入者」——**那是掃描方法的漏洞**，
只找了 `mov [di+7A8Ah], cl` 的直接形式。§31 自己寫了「間接寫入抓不到」的警告，
下一段還是照著踩進去。已訂正。

⚠️ bit 5 的**語意仍未解**。已知：值 19 設它、`sub_42C8F` 讀它。
形狀像「這一輪被值 19 處理過」的標記，但**沒有證據**，不要當結論。

## 41. 兩條決策鏈**全部解完**

| | 值 | 行動 | 語意 |
|---|---:|---|---|
| **A**（第二方）| 11 | `sub_3B19C(0)` | 必勝結算 |
| | 12 | `sub_3CBD9` | 推倒重來 |
| | 13 | `sub_3D57B` | **預設**：依現有命令分流 |
| | 14 | `sub_3D823(14)` | 斬首，只留一個守城 |
| | 15 | `sub_3D823(15)` | 斬首，所有駐守的都留 |
| | 16 | `sub_3DD05` | 只處理待命與已出發的單位 |
| | 17 | `sub_3DED9` | 重算全軍的行動 |
| | 18 | `sub_3E24F` | 唯一比戰力的一支（形狀像挑最弱的打）|
| | 19 | `sub_3E691` | **全面接戰**（本節）|
| **B**（第一方）| 1 | `sub_3B19C(1)` | 必勝結算 |
| | 2 | `sub_3B31D` | 佈防 |
| | 3 | `sub_3C89A` | **預設**：打城市 |
| | 4 | `sub_3CA09` | 打敵方主力周邊 |

**13 種行動全部有語意。** 其中值 18 標「部分」（被排序的清單來自兩段未讀完的統計）。

剩下的支線未讀：`sub_3B9D8`(371)、`sub_3D261`(191)、`sub_3DAEF`(267)、
`sub_3D411`、`sub_560D7`、`sub_56D49`(81)、`sub_3E24F` 的前兩段統計。


## 42. 決策鏈的三個步驟解出來了（值 12／19／4）

§41 把 13 種**行動**解完了，但「哪一步會選中哪個值」是另一回事。
這一節補三步。

### `sub_3A885`（41 行）→ 值 12，與 `sub_3A8C8`（28 行）→ 值 19 **共用同一組判斷**

```
sub_3A885:   if !A(1) 且 B(1) 且 !A(0) 且 B(0):  決策值 = 12
sub_3A8C8:   if !A(0) 且 B(0):                   決策值 = 19

其中   A = sub_3A63C     B = sub_3A730
```

> **兩方都成立 → 12（推倒重來）；只有第二方成立 → 19（全面接戰）。**

⭐ 這是很乾淨的結構：同一組判斷，範圍不同就給不同的行動。
而且 `sub_3A885` 排在 `sub_3A8C8` 前面，所以「兩方都成立」會先被攔下來。

### `sub_3A63C` 與 `sub_3A730`：同型的比率門檻

兩支**結構完全相同**，只差內層呼叫：

```
sub_3A63C(side) = ( sub_3A57E(side) + byte_64900 ) < 15
sub_3A730(side) = ( sub_3A672(side) + byte_64900 ) < 15
```

而 `sub_3A57E` 與 `sub_3A672` 又是同型（各 97 行）：

```
分子 = side ? word_64936 : word_64938      ; sub_3A57E
分子 = side ? word_64932 : word_64934      ; sub_3A672
分母 = sub_3A4CE(side)

if 分子 == 0: 回 0
if 分母 == 0: 回 15
否則         回 分子/分母 的某種換算（Real 除法 + Round，上限 15）
```

> **四個判斷都是「某個比率 + 階段值 < 15」。比率越小越容易成立。**

`byte_64900` 第三次出現（§17 的 `> 4`、§35 的 `≥ 5`、這裡的加項），
它顯然是**階段或難度**，語意仍未解。

### `sub_3AABA`（34 行）→ 值 4

```
if sub_56D49(0):
    if byte_6AA84 & 0C0h != 0: byte_6AA84 |= 4     ; 保留 bit 6/7
    else:                      byte_6AA84 = 4
    決定 = 1
```

⭐ 這裡看到 `byte_6AA84` bit 6/7 的**保留邏輯**——§2 記過決策鏈開頭
`byte_6AA84 &= 0C0h`（只留 bit 6/7），這一步寫值時也小心不碰那兩位。
兩處一致，佐證 bit 6/7 是跨決策保留的狀態。

`sub_56D49`（81 行）是關鍵共用判斷——§40 的值 19 前置也用它。仍**未讀完**。

### [訂正] 本節原本寫「來源未解」——**答案早就在 `docs/re/05`**

當時順著呼叫鏈挖到第五層（`sub_4136B`）停手，記成
「`word_64932/34/36/38` 形狀像按比例衰減的累積量，但沒有證據」。

那四個變數是**雙方帶進戰場的黃金與糧食**，`docs/re/05` §2 早就解過並標 confirmed。
分母 `sub_3A4CE` 是該方的兵力總和，比率是「每 150 兵分到多少資源」。
**完整推導與證據見 §48**，這裡不重複。

`sub_4136B` 的前半仍未讀，但它已經不擋路——它是這四個變數的另一個寫入者
（戰鬥中的消耗？），不是它們的定義。


## 43. ⭐⭐⭐ 兩條鏈在**同一個局勢**下各自做重整（值 2 與值 12）

### `sub_3A9F4`（58 行）→ 分支 B 值 2（佈防）

```
if !A(0) 且 B(0) 且 !A(1) 且 B(1) 且 word_6493A == 0:
    byte_6AA84 = 2         ; 保留 bit 6/7（與 §42 的 sub_3AABA 同一個手法）
    決定 = 1
```

把它跟 §42 的 `sub_3A885` 擺在一起看：

| 函式 | 鏈 | 條件 | 值 | 行動 |
|---|---|---|---:|---|
| `sub_3A885` | A | `!A(1) 且 B(1) 且 !A(0) 且 B(0)` | 12 | 推倒重來 |
| `sub_3A9F4` | B | `!A(0) 且 B(0) 且 !A(1) 且 B(1)` **且 `word_6493A == 0`** | 2 | 佈防 |

**四個判斷完全相同**（順序不同，邏輯一樣），分支 B 只多一個 `word_6493A == 0`。

> ⭐⭐⭐ **同一個局勢下，分支 A 選「推倒重來」、分支 B 選「佈防」。**
> 兩者都是**重整**型的行動——一個把單位打回待命重新找目標，
> 一個把陣形攤開。兩條鏈對同一個局勢的反應是同構的。

這也回頭補強 §42：那組比率門檻不是某一步的私有判斷，
而是**兩條鏈共用的局勢判斷**。

⭐ `word_6493A` 是分支 B 獨有的額外閘門 = **第一方（攻方）的彈藥**（§48）。
**沒彈藥就不進攻，改佈防。**

### `sub_3A94E`（33 行）→ 分支 A 值 16／17

```
if sub_56D49(0):
    if sub_53619(0): 決策值 = 17      ; 重算全軍
    else:            決策值 = 16      ; 只處理待命與已出發
    決定 = 1
```

⭐ `sub_56D49(0)` 是前置，`sub_53619(0)` 決定 16 還是 17。

**`sub_56D49` 現在是三處共用**——值 4（`sub_3AABA`，§42）、
值 16／17（本節）、值 19 的前置（§40）。81 行，**未讀**，
是這條線目前投報最高的一支。

`sub_53619` 在 §16 也出現過（5.0 必勝門檻成立後接它），同樣未讀。

### 未讀清單更新

補完這兩支之後剩五筆：

```
sub_3A817 在 11／12／16／17 之間怎麼選
sub_3A8F7 在 14／15／18 之間怎麼選
sub_3AA51 在 1／3 之間怎麼選
比率門檻的來源（word_64932/34/36/38 與 sub_3A4CE）
sub_56D49（三處共用）＋ sub_53619 ＋ word_6493A
```


## 44. ⭐⭐⭐ `sub_56D49(side)` = **該方的勢力領袖本人在不在場上**

四處共用的那支（值 4、值 16／17、值 19 的前置），81 行，解開了。

```
if side != 0:
    清單 = 複製 word_64902 起 20 bytes     ; 第一方的 10 個單位
    領袖 = word_64942                       ; 首位單位效忠的勢力領袖（§37）
else:
    清單 = 複製 word_64916 起 20 bytes     ; 第二方的 10 個單位
    領袖 = word_64944                       ; 當前交戰省的司令（§37）

for i = 1..10:
    if 清單[i] == 0: continue
    if 清單[i] == 領袖: return 1            ; ← **領袖本人就在隊伍裡**
return 0
```

> ⭐⭐⭐ **`sub_56D49(side)` 回答一件事：那一方的勢力領袖有沒有親自上戰場。**

而全部四個呼叫端傳的都是 **0**，也就是問
**「當前交戰省的司令本人在不在守方的隊伍裡」**。

### 四個用途一次全部合理化

| 決策值 | 行動 | 為什麼要問這個 |
|---:|---|---|
| 4 | 打敵方主力周邊（`sub_3AABA`，§42）| 敵方領袖在場 → 主力就在那裡 |
| 16 | 只處理待命與已出發（`sub_3A94E`，§43）| 同一個前置，`sub_53619` 再分流 |
| 17 | 重算全軍 | 同上 |
| 19 | 全面接戰（`sub_3E691`，§40）| 領袖在場 → 值得全面接戰 |

**守方主帥親自坐鎮，是電腦改變打法的觸發點。** 這條規則本身就有史實味道
——`docs/reference/history/40-warlord-politics.md` 提到的「打不打取決於對方
是誰在指揮」是同一個直覺。

⚠️ §37 解出 `word_64942`／`word_64944` 是**勢力領袖**之後，這一支才讀得懂。
當初若還停在「第一方／第二方的代表單位」那個錯誤推論上，
這裡會讀成「隊伍裡有沒有某個特定單位」，完全看不出語意。
**一個變數讀錯，會讓依賴它的四個決策點全部讀不懂。**

### 未讀清單降到 4 筆

```
sub_3A817 在 11／12／16／17 之間怎麼選
sub_3A8F7 在 14／15／18 之間怎麼選
sub_3AA51 在 1／3 之間怎麼選
比率門檻的來源（word_64932/34/36/38 與 sub_3A4CE）＋ sub_53619 ＋ word_6493A
```


## 45. ⭐⭐ `sub_3A817` 與 `sub_3AA51`：兩條鏈的第一步，以及一段**死碼**

### `sub_3AA51`（64 行）→ 分支 B 值 1／3

```
if sub_3A320(1, 5.0):                    ; 第一方被壓到第二方 1/5 以下
    if !sub_53619(1): 決策值 = 1         ; 必勝結算
    else:             決策值 = 3         ; 打城市
    決定 = 1
```

乾淨的二分。**兩個分支都保留 `byte_6AA84` 的 bit 6／7**（與 §42／§43 一致）。

### `sub_3A817`（63 行）→ 分支 A 值 11／12／16／17

```
if sub_3A320(0, 5.0):                    ; 第二方被壓到第一方 1/5 以下
    if !sub_53619(0):
        決策值 = 11                       ; 必勝結算
        byte_6B968 = 1                    ; ← 額外設一個旗標（語意未解）
    else:
        if !sub_56D49(0): 決策值 = 12    ; 推倒重來（守方領袖不在場）
        else:
            if sub_53619(0): 決策值 = 17  ; 重算全軍
            else:            決策值 = 16  ; ← **走不到**
    決定 = 1
```

### ⛔ 值 16 在這一支是死碼

外層已經確定 `sub_53619(0) != 0`（否則走 11），內層又問一次同一個
無副作用的函式，**結果必然相同**——所以那條路恆走 17，**16 永遠走不到**。

`sub_56D49` 是純查詢（掃陣列比對，§44），不會改變 `sub_53619` 的結果。

⭐ 值 16 本身**不是**死碼——`sub_3A94E`（§43）走得到它。
死的只有 `sub_3A817` 裡的這一條路徑。與 §2 記的
「`byte_6AA84` 開頭那三段位元操作是恆等變換」同型：**原版有寫但不生效的段落**。

照抄（`CLAUDE.md` §9：原版行為就是規格），但在 Go 裡標明。

### `sub_53619(side)`（97 行）：三處分流的那支

```
清單 = side ? word_64902 : word_64916 起 20 bytes
u = 清單裡最後一個非 0 的單位
if u == 0: 回 1
sub_534FF(&out1, &out2, u)
回 out2 > 0 ? 0 : 1
```

`sub_534FF` 是**比對 `+14`（效忠的勢力領袖）與省份記錄 `+20`（司令）**
的那一支（`combat.go` 已記）。所以 `out2` 像是某種歸屬計數。

在決策鏈裡它的作用很一致：

| 呼叫端 | `sub_53619 == 0` | `sub_53619 != 0` |
|---|---|---|
| `sub_3A817` | **11 必勝結算** | 12／17 |
| `sub_3AA51` | **1 必勝結算** | 3 打城市 |
| `sub_3A94E` | 16 | 17 |

> **兩處「必勝結算」都要 `sub_53619 == 0`。**
> 也就是說：戰力差五倍**還不夠**，還要這個條件成立才准直接判勝負。

⚠️ `sub_534FF` 的第二個輸出是什麼**未讀**，所以 `sub_53619` 的語意
只到「與勢力歸屬有關的一個判斷」。形狀像「這一方是不是單一勢力」
（混編就不能簡單判勝負），**但沒有證據，不當結論**。

### 未讀清單降到 2 組

```
比率門檻的來源（word_64932/34/36/38 與 sub_3A4CE，§42 挖到第五層）
sub_534FF 的第二個輸出（決定 sub_53619）＋ word_6493A ＋ byte_6B968
```

**決策鏈本身的結構已經全部解完**——五步／三步每一步的條件與分流都清楚了，
剩下的是三個底層判斷的**語意**（不影響結構，只影響什麼時候會成立）。


## 46. ⭐⭐ 值 18 的候選來源：**守方城市周圍聚集的攻方單位**

§38 把 `sub_3E24F` 標「部分」，因為被排序的清單來自兩段未讀完的統計。
第一段讀完了。

```
for c in 城市清單（sub_55FBE 掃出來的）:
    格 = 城市[c]
    u = word[62A8h + 格×2]
    if u == 0: 跳過
    if 將領[u].+8 != 0: 跳過                ; ← **只看守方佔據的城市**

    sub_55632(&周圍格, 0, 格)               ; 收集城市周圍（mode 0）
    for j in 周圍格（0FFh 結束）:
        v = word[62A8h + 周圍格[j]×2]
        if v == 0: 跳過
        if 將領[v].+8 == 0: 跳過            ; ← **只收攻方單位**
        idx = sub_579F4(將領[v].+8, v)      ; 查它在陣列的第幾格（§27）
        …收進清單…
```

兩道篩選是對稱的：**城市要守方佔的、周圍的單位要攻方的**。

配上 §38 已解的第三段（照 `sub_5A0B9` 攻擊力**升序**排）與第四段（派工）：

> ⭐⭐ **值 18 = 在「圍著我方城市」的敵軍裡，挑戰力最弱的打。**

分支 A 管第二方（守方），所以這是**守軍反擊圍城部隊、挑最弱的下手**。
與值 3（挑最近的城市）、值 14／15（斬首主帥）並列，是第三種目標選法：
**不是最近、不是最強，是圍住我的人裡最弱的那個**。

⭐ 這也解釋了為什麼值 18 是分支 A 九種行動裡**唯一呼叫攻擊力公式**的一支
（§38）——只有「挑最弱」需要真的比戰力，其他行動比的是距離或位置關係。

⚠️ 第二段統計（同型的第二輪）仍未讀，它可能是對稱地統計另一組。
但候選來源與排序方向都清楚了，**值 18 的語意從「部分」升為 confirmed**。

⚠️ `sub_55632` 這裡傳 **mode 0**（§30 的佈防傳 mode 1 收 37 格），
兩者差別仍未驗（§29 記過同一條鏈上 mode 一致）。


## 47. ⭐⭐⭐ `sub_534FF` = **找可用的我方鄰省**，於是 `sub_53619` 也解了

§45 把 `sub_53619` 標成「與勢力歸屬有關的一個判斷」，因為 `sub_534FF`
的第二個輸出沒讀。讀完了。

```
sub_534FF(out_清單, out_數量, unit):
    領袖 = 將領[unit].+14                       ; 效忠的勢力領袖（§37）
    *out_數量 = 0;  清空 out_清單[0..6]

    for i = 1..8:                                ; 掃**當前交戰省**的鄰省（+22..+29）
        鄰 = byte[省份[byte_6FFC4] + 21 + i]
        if 鄰 == 0 或 鄰 == 0FFh:      跳過
        if 省份[鄰].+20 == 0:          跳過      ; 無主省
        if 省份[鄰].+20 != 領袖:       跳過      ; ← **不是我方的省**
        if 省份[鄰].+32 & 40h:         跳過      ; ← bit 6（新發現）
        if sub_5A881(鄰) >= 100:       跳過      ; ← 某個值的上限

        (*out_數量)++
        out_清單[*out_數量 - 1] = 鄰
```

> ⭐⭐⭐ **`sub_534FF` 找的是「當前交戰省的鄰省裡，屬於我方而且可用的省」。**

而 `sub_53619(side)` 回 `out_數量 > 0 ? 0 : 1`（§45），所以：

| `sub_53619` | 意思 |
|---:|---|
| **0** | **有可用的我方鄰省**（有後援）|
| 1 | 沒有 |

### 這讓兩處「必勝結算」的條件變得很合理

§45 記過**兩處必勝結算都要 `sub_53619 == 0`**。現在讀得懂了：

> **戰力差五倍，而且有我方鄰省可以支援——才敢直接判勝負。**
> 沒有後援就不冒險速戰，改走值 3（打城市）或值 12（推倒重來）。

同一個 `sub_53619` 在 `sub_3A94E` 決定 16 還是 17，也是同一個直覺：
有後援就重算全軍（17，積極），沒有就只處理待命與已出發的（16，保守）。

### 三個順帶的發現

1. ⛔ 本節初稿寫「省份 `+32` bit 6 語意未解」、「`sub_5A881` 那支未讀」。
   **兩條都錯**——`sub_534FF` 在 `docs/re/07` §9 已經整支解完，
   `+32` bit 6 是「**那個省正在打仗**」（Go 側常數 `ProvinceFlagInBattle`），
   `sub_5A881` 是「該省可用將領數」（`docs/re/07` §9 也寫了它的
   `>= 100` 門檻**實質恆真**——第一期全遊戲才 274 位將領）。
   詳見 §51。
2. 政略的增援與戰鬥的後援判斷**共用同一支** `sub_534FF`。
3. 迴圈掃的是**省份 `+22`..`+29`**（`i = 1..8`，基址 −6235h + 21 + i），
   **與 `docs/spec/03` §2 的「鄰省列表 +22，8×u8」完全對上**。

   ⛔ 第一版寫成「掃 +23..+29，跳過第一格，疑似與 spec 對不上」——**讀錯了**。
   迴圈是 `mov var_3, 1` 之後 `jmp` 到**迴圈體**（不經過 `inc`），
   所以第一次是 1；`inc` 在迴圈尾的回跳目標上。
   **Turbo Pascal 的 for 迴圈就是這個形狀**，把入口跳當成經過 `inc`
   會讓起始值整個差 1，而這種錯不會有任何症狀——只是安靜地少算一格。

### 未讀清單降到 2 筆

```
比率門檻的來源（word_64932/34/36/38 與 sub_3A4CE，§42 挖到第五層）
word_6493A（分支 B 值 2 的閘門）＋ byte_6B968 ＋ sub_5A881
```


## 48. ⭐⭐⭐ 比率門檻 = **補給撐得住幾回合** —— 而且答案早就寫在 `docs/re/05`

§42 追那條線挖到第五層停手，結論寫「`word_64932/34/36/38` 的語意未解」。
**那是錯的。這四個變數在 2026-08-01 的 `docs/re/05` 就已經解過，標 confirmed。**

`tools/ida_xref.idc` 一跑就看到寫入者是 `sub_4F468`（讀 `MEM_WAR.DAT`）
與 `sub_3964E`（寫回），而 `docs/re/00-function-index.md` 對這兩支的說明
直接指向 `docs/re/05`：

```
+0 | 4 × u16 | word_64932/64936/6493A/6493E | 攻守雙方帶的黃金／糧食／彈藥／燃料
```

### 四個變數的歸屬 — **confirmed**

`docs/re/05` 只寫了「每個欄位是一對」，沒說哪一份屬於哪一方。
`sub_3A4CE` 把這件事釘死了：它用同一個 `side` 參數同時決定

```
side != 0 → 掃 0x750（第一方）    sub_3A57E 取 word_64936
side == 0 → 掃 0x764（第二方）    sub_3A57E 取 word_64938
```

| 資源 | 第一方 | 第二方 | 結算寫回省份 |
|---|---|---|---|
| 黃金 | `word_64932` | `word_64934` | `+0` |
| 糧食 | `word_64936` | `word_64938` | `+2` |
| 彈藥 | `word_6493A` | `word_6493C` | `+4` |
| 燃料 | `word_6493E` | `word_64940` | `+8` |

寫回的偏移是從 `sub_546D1` 直接讀出來的（基址 `9DCBh` + 省編號 × 37），
對照 `docs/spec/03` §2 的省份欄位表逐項吻合。煤礦（`+6`）與鐵礦（`+10`）
不在其中——礦藏不隨部隊移動，`sub_546D1` 連清零都沒碰它們。

### `sub_3A4CE(side)`（94 行）= **該方的兵力總和**

```
sum = 0                       ; 32-bit
for i = 1 to 10:
    id = 單位陣列[i]           ; side ? 0x750 : 0x764
    if id != 0:
        sum += word[0x7A7D + 33*id + 17]     ; 將領記錄 +17 = 兵力
return sum
```

`+17 = 兵力` 是 `docs/spec/02` §3 已經解出來的欄位。
迴圈**不檢查死活**，只看槽位有沒有單位——陣亡單位的兵力是 0，加進去無害。

### `sub_3A57E`（97 行）與 `sub_3A672`：**每 150 兵分到多少資源**

兩支同型，只差分子（前者糧食、後者黃金）：

```
資源 = side ? word_64936 : word_64938        ; sub_3A57E
兵力 = sub_3A4CE(side)

if 資源 == 0: return 0
if 兵力 == 0: return 15
d = Round(兵力 / 150);  if d == 0: d = 1
return Round(資源 / d)
```

那個 `150` 是暫存器裡的 Turbo Pascal Real 常數
`CX:SI:DI = 0088h / 0000h / 1600h`，用 `tools/tp_real.py` 解出來
（該工具的自我測試有五個已驗過的正對照）。

> ⭐⭐⭐ **這是補給指標**：資源 ÷ (兵力/150)，也就是「每 150 個兵分到多少」。
> 門檻 `值 + byte_64900 < 15` 等價於 **`資源 < 兵力 / 10`**（`byte_64900` 為 0 時）。

兩個特例都合理：**沒資源 → 0 → 一定吃緊**；**沒兵 → 15 → 不吃緊**（沒兵要吃什麼）。

### 數量級：那三個行動是**主線**，不是例外

拿實機湖北的存量對三個滿員步兵師（60,000 兵，分母 400）：

| 資源 | 存量 | 每 150 兵 | `< 15`？ |
|---|---:|---:|---|
| 糧食 | 18,050 | 45 | 否（充足）|
| 黃金 | 4,200 | 10 | **是（吃緊）**|

複合條件 `!糧食吃緊 && 黃金吃緊` 因此**成立**。

> 這翻轉了 §42 留下的印象。那時只知道「四個同型的比率門檻」，
> 看起來像少見的特殊局勢；實際上**黃金一向比糧食少一個數量級**，
> 所以值 12（推倒重來）／值 19（全面接戰）／值 2（佈防）是電腦的常態選擇。
> 語意也順：**有糧能打、沒錢耗不起 → 速戰速決。**

### `word_6493A` = **第一方的彈藥**（§43 那個閘門）

§43 記「`word_6493A` 是分支 B 獨有的額外閘門，語意未解」。現在解了：

> **分支 B 值 2（佈防）要「攻方的彈藥為 0」。沒彈藥就不進攻，改把陣形攤開。**

而且它只查第一方的彈藥、不帶 `side` 參數——這與 §32 的
「分支 B 管第一方」互相佐證，是一條獨立的旁證。

### ⚠️ 這一節是「已經有答案卻又推錯」的第二次

第一次是 `word_64944`（§37）。兩次的失敗模式一樣：**手上有答案，但沒想到要查。**

差別在這次的偵錯成本：`tools/ida_xref.idc` 跑一次就看到寫入者，
函式索引直接把 `sub_4F468` 指向 `docs/re/05`，全程不到十分鐘。
§42 當初是順著呼叫鏈一層層往下挖到第五層才停手——
**方向是對的，但那條路上根本不會經過 `docs/re/05`**。
交叉參考是橫向的，呼叫鏈是縱向的，兩者不能互相取代。

`tools/dump_func.py` 現在會把已知語意直接印在組語旁邊，
`docs/re/05` 的欄位表也已經進了 `tools/addr.py` 的 `GLOBALS`——
下次讀到 `mov ax, word_64936` 會當場看到「第一方帶的糧食」。

### 未讀清單更新

```
sub_3A817 在 11／12／16／17 之間怎麼選
sub_3A8F7 在 14／15／18 之間怎麼選
sub_3AA51 在 1／3 之間怎麼選
byte_64900（三處出現：§17 的 > 4、§35 的 ≥ 5、§48 的加項）
byte_6B968 ＋ sub_5A881（sub_534FF 的第五道篩選）
MEM_WAR.DAT 的 +0..+7 是誰在出兵時決定的
```


## 49. ⭐⭐⭐ `byte_64900` = **回合數**；戰鬥有 16 回合上限，而且**補給會把戰鬥打完**

`byte_64900` 在決策鏈裡出現三次（§17 的 `> 4`、§35 的 `≥ 5`、§48 的加項），
一直標「疑似階段或難度」。xref 一跑就結束了：**只有兩個寫入者**。

```
寫  sub_4F0E2   4f255   mov  byte_64900, 1      ; 開戰時
寫  sub_41D20   41d63   inc  byte_64900         ; 每回合末
```

> **`byte_64900` 是戰鬥回合數，1-based。** 十二個讀取點比較的是
> 4／5／7／12／15／16——全部是回合門檻。

### `sub_41D20`（64 行）= 回合結束

由戰鬥主函式 `sub_39B6E` 呼叫，做完這些事：

```
sub_5A4BC(...)              ; 畫面
if byte_6B969 & 2: sub_41B71
sub_410B0
sub_4136B                   ; ← 補給消耗（見下）
sub_4123D
if byte_6A40A == 0: sub_4180D
inc byte_64900              ; ← 回合 +1
byte_6AA84 &= 0C0h          ; ← 分支 B 的決策值重置（§2 記過這個手法）
sub_41209                   ; ← 回合上限檢查
...依 byte_64901 呼叫 sub_54E3B / …
```

⭐ 決策值的重置就在回合遞增的隔壁——這是「決策鏈每回合重跑一次」的直接證據。

### `sub_41209`（28 行）= **回合上限 16**

```
if byte_6FE7E != 2 且 byte_64900 == 16:  byte_6A40A = 1
if byte_6FE7E == 2 且 byte_64900 == 15:  byte_6A40A = 1
if byte_64901 != 0:                      byte_6A40A = 1
```

`byte_6A40A` 是**戰鬥結束旗標**（`sub_41D20` 用 `cmp byte_6A40A, 0` 決定跳過收尾）。
`byte_64901` 是**勝方**：1 = 第一方、2 = 第二方、0 = 未分。

> 回合從 1 開始、回合末才 +1，所以**實際打得到 15 個完整回合**。

⭐ `byte_6FE7E` 是**月份**（`docs/re/16` §3b、`docs/re/19` 都寫過），所以那條分支是：

> **二月打的仗只有 15 回合，其他月份 16 回合。** 二月比較短。

⚠️ 本節初稿把 `byte_6FE7E` 寫成「語意未解（疑似戰鬥種類）」。
**那是同一類錯誤的第三次**（前兩次：`word_64944`、比率門檻的來源）——
答案在 `docs/` 裡放著，但沒想到要查。修法見 §50 末段。

### `sub_4136B`（196 行）= **每回合的補給消耗**，而且會直接判勝負

§42 當初把這支記成「四個變數的寫入者，形狀像按比例衰減的累積量」。
它其實是回合結算裡最重要的一步。兩半完全對稱，第一方那半是：

```
黃金' = word_64932 - Round(第一方兵力 / 1500)      ; 32-bit 有號
糧食' = word_64936 - Round(第一方兵力 / 150)

if 黃金' > 0 且 糧食' > 0 且 word_6493E != 0 且 word_6493A != 0:
    寫回黃金'、糧食'
else:
    if 黃金' < 0: word_64932 = 0
    if 糧食' < 0: word_64936 = 0
    byte_6A40A = 1
    byte_64901 = 2            ; 第一方見底 → 第二方勝
    sub_4DD04()
```

第二方那半一模一樣，只是 `byte_64901 = 1`。

`第一方兵力`（`word_6492A:word_6492C`，32-bit）與第二方的
（`word_6492E:word_64930`）由 `sub_4DD04` 算好放著——那支用的迴圈與
`sub_3A4CE` 完全相同（掃 10 個槽位加總 `+17`），只是把結果快取成全域。

> ⭐⭐⭐ **補給是獨立的勝負條件**：黃金或糧食扣到 0 以下、
> 或燃料／彈藥為 0，戰鬥當場結束，對方獲勝。
> 燃料與彈藥在這裡**只被檢查、不被扣**，消耗發生在別處（移動與射擊，未追）。

#### 一個怪癖，照著做

見底的分支裡**只有變成負數的那一項會被寫成 0**，沒變負的那一項
連扣除後的值都不寫回，保持原值。所以敗方最後一回合的消耗會少扣一部分。
`loc_4145F` 那一段只有兩個條件式歸零，沒有無條件寫回——不是我讀漏。

### ⭐⭐ 回頭補強 §48：那個比率就是**補給天數**

`sub_3A57E` 的分母是 `兵力 / 150`。**每回合的糧食消耗正好也是 `兵力 / 150`。**

> 所以 `sub_3A57E` 回的不是抽象比率，是字面意義的「**糧食還能撐幾回合**」。
> 門檻 `< 15` 對上 16 回合的上限，意思就是「**糧食撐不完整場**」。

黃金那條（`sub_3A672`）用了**同一個分母**，但黃金的實際消耗是 `兵力/1500`
——**慢十倍**。所以它算出來的是「黃金能撐的回合數 ÷ 10」，`< 15` 等於
「黃金撐不到 150 回合」。以 16 回合的上限來看，這條在多數局面成立。

是刻意設計成寬鬆條件，還是原版寫錯了分母，**沒有證據可以判斷**。
可以確定的是後果：複合條件 `!糧食吃緊 && 黃金吃緊` 在實務上
**幾乎等於「糧食撐得完整場」**。

### 未讀清單更新

```
sub_3A817 在 11／12／16／17 之間怎麼選
sub_3A8F7 在 14／15／18 之間怎麼選
sub_3AA51 在 1／3 之間怎麼選
彈藥與燃料在哪裡被扣（sub_4136B 只檢查不扣）
byte_6B968 ＋ sub_5A881（sub_534FF 的第五道篩選）
MEM_WAR.DAT 的 +0..+7 是誰在出兵時決定的
```


## 50. ⭐⭐⭐ `sub_38C01` = 選歷史背景；`byte_6FE88` = **時期編號**

追 `byte_6FE7E`（月份）的寫入者時掉出來的。`sub_38C01` 讀選單按下的字元
（`'1'`／`'2'`／`'3'`），一次設四個全域——**這就是三個劇本的起始參數表**。

| 選項 | 時期 | 年 `byte_6FE7D` | 月 `byte_6FE7E` | 西元 | 省份數 `word_7001E` | `byte_6FE88` |
|---:|---|---:|---:|---|---:|---:|
| 1 | 北伐時期 | 民國 15 | 7 | **1926-07** | **36** | 1 |
| 2 | 抗戰前期 | 民國 26 | 7 | **1937-07** | 39 | 2 |
| 3 | 抗日後期 | 民國 30 | 12 | **1941-12** | 39 | 3 |

三個日期全部對得上史實：1926 年 7 月國民革命軍誓師北伐、
1937 年 7 月七七事變、1941 年 12 月太平洋戰爭爆發。

### `byte_6FE88` = 時期 1／2／3 — **confirmed**

`docs/re/11` §7 把它列在「還沒解」。現在解了，而且答案很簡單：
**它就是資料檔後綴的那個 N**。

```
MAN(1).DAT  TOWN(1).DAT  FAN(1).15   ←  byte_6FE88 == 1
MAN(2).DAT  TOWN(2).DAT  FAN(2).15   ←  == 2
MAN(3).DAT  TOWN(3).DAT  FAN(3).15   ←  == 3
```

這回頭解釋了 `docs/re/11` §5 那段「只在 `byte_6FE88 == 1` 時才跑」的程式碼
——那是**北伐時期專屬**的規則，不是難度分支。

### `word_7001E` 是**可用省份數，而且隨劇本改變** — **confirmed**

`docs/re/11` §54 已經把它讀成「省份總數」，但沒記到它**不是常數**：
北伐時期只有 **36** 省，抗戰的兩期是 39 省。

用法之一在 `sub_19D6A`（亂數事件）：`Random(word_7001E) + 1` → 1..36 或 1..39，
**隨機挑一個省**。所以北伐時期的隨機事件永遠不會落在最後三個省。
那三個是哪三個要對 `TOWN(1).DAT` 才知道，未查。

### ⚠️ 這一節的起因是本 session 第三次「已經有答案卻推錯」

§49 初稿把 `byte_6FE7E` 寫成「語意未解（疑似戰鬥種類）」。
`docs/re/16` §3b 與 `docs/re/19` 早就寫明它是**月份**。

三次的共同點都不是「查不到」，是「**沒想到要查**」：

| 次 | 變數 | 答案原本在哪 |
|---:|---|---|
| 1 | `word_64944` | 從呼叫端參數順序反推，推錯（§37 修正）|
| 2 | `word_64932/34/36/38` | `docs/re/05` §2，標 confirmed（§48 修正）|
| 3 | `byte_6FE7E` | `docs/re/16` §3b、`docs/re/19`（本節修正）|

第二次之後加的 `tools/addr.py` 沒擋住第三次，原因很具體：
**那三張表是手維護的，`byte_6FE7E` 不在裡面**，所以 dump 出來一片空白，
我就照那片空白寫下「未解」。工具的警語（「零命中不代表沒碰已解欄位」）
印在 stderr，但警語不是機制。

**修法**：`tools/dump_func.py` 現在會把 dump 範圍裡出現的每一個符號
（`word_*`／`byte_*`／`sub_*`）拿去 grep `docs/**/*.md`，
**已經被寫過的一律列出來**。不依賴我記得去查，也不依賴 `addr.py` 的表夠全。

```
[dump_func] ⚠️ 下列符號 **docs/ 裡已經寫過**，下結論前先去讀，不要重推：
  byte_6FE7E     mechanics/30-combat.md  re/13-turn-structure-and-roster.md  re/16-game-over.md（另 4 份）
```


## 51. ⛔ 第四次「已經有答案卻推錯」——這次連程式碼都重抄了一份

§47 解 `sub_53619` 時，順手把 `sub_534FF`（找可用鄰省）也「解」了一遍，
記下兩條未解：「`+32` bit 6 語意未解」「`sub_5A881` 那支未讀」，
然後在 `internal/game/battlesupport.go` 依那個理解**重寫了一份實作**。

**`sub_534FF` 在 `docs/re/07` §9 已經整支解完，而且 `province.go` 的
`ReinforcementSources` 早就完整實作了它**，包含我以為未解的兩道：

| 我記的 | 實際（`docs/re/07` §9）|
|---|---|
| `+32` bit 6 語意未解 | **那個省正在打仗**（`ProvinceFlagInBattle`）|
| `sub_5A881` 未讀 | **該省可用將領數**；`>= 100` 的門檻**實質恆真** |

而且重抄的那份**行為就是不一樣**：原版遇到無主省是**停止掃描**（`break`），
我寫成跳過（`continue`）；原版只掃鄰省表的**前 7 格**，我掃全部。

> `CLAUDE.md` §7.4：**一條規則只留一份實作。重抄的那份一定會漂，
> 症狀是「看起來完全合理的錯」。** 這次的漂移在 24 小時內就發生了。

### 這次是工具先抓到的

`tools/dump_func.py` 的 docs 掃描（§50 加的）在我讀 `sub_5A881` 的第一秒就印出：

```
[dump_func] ⚠️ 下列符號 **docs/ 裡已經寫過**，下結論前先去讀，不要重推：
  sub_5A881      mechanics/20-military.md  mechanics/30-combat.md  mechanics/70-ai.md（另 6 份）
```

九份筆記。前三次（`word_64944`、比率門檻、`byte_6FE7E`）都是寫完錯的結論
之後才發現，**這次是在寫下去之前**。

### 修法

`battlesupport.go` 不再自己實作，改成轉給 `ReinforcementSources`，
只留「戰鬥決策鏈怎麼問它」那一層。`ProvinceFlagNoSupport` 這個
我自己取的常數名刪掉——`ProvinceFlagInBattle` 才是正確的名字。

⚠️ **加一條檢查**：往後在 `internal/game/` 新增規則函式之前，
先 grep 既有實作。反組譯筆記重複只是浪費，**程式碼重複會產生行為差異**。


## 52. ⭐⭐⭐ `sub_3B9D8`（371 行）：沒有城市可打時 —— **追人，而且不准一窩蜂**

§22 記它是「選中城市 == 0」那條路，371 行未讀。它是這條線上最大的一支，
解出來有兩個新規則。

### 骨架

```
sub_3B9D8(狀態, side, 旗標陣列):
    敵方 = side ? word_64916 : word_64902        ; 兩份 10 word 的拷貝
    我方 = side ? word_64902 : word_64916

    清單[1..n] = 敵方裡非零的槽位              ; 壓縮成連續清單
    火力[1..10] = 0                            ; 32-bit
    強度[1..10] = 0                            ; 32-bit

    for i = 1..10:                             ; 我方
        u = 我方[i]
        if u == 0:                  continue
        if 旗標陣列[i-1] != 0:       continue
        if 將領[u].+9 != 3 且 != 4:  continue   ; ← 同 §23／§24，只派等待指派的

        for j = 1..n:                          ; 逐一試敵方單位
            強度[j] = sub_5A0B9(清單[j])        ; 該敵方單位的攻擊力
            sub_56A57(&強度[i], 將領[清單[j]].+5)
            將領[u].+12 = sub_567B9(0, 將領[清單[j]].+5, 將領[u].+5)

            if 將領[u].+12 != 0FFh:
                if sub_3B961() != 0:            ; ← **火力已經夠了**
                    格佔用[將領[u].+12] = 0
                    將領[u].+12 = 0FFh          ; 換下一個敵人
                else:
                    火力[j] += sub_5A0B9(u)     ; 記帳
            until 將領[u].+12 != 0FFh 或 j == n

        if 將領[u].+12 != 0FFh:
            將領[u].+10 = 清單[j]               ; 目標單位
            旗標陣列[i-1] = 1
        else if 最後一個算得出來的下一跳 != 0 且 格佔用[它] == 0:
            將領[u].+12 = 它                    ; ← 退而求其次
            將領[u].+10 = 最後試的那個敵人
            旗標陣列[i-1] = 1
            格佔用[它] = 1
        else:
            sub_3B8B0(狀態, side, u)
```

結構與 §24 的 `sub_3BF6A` 幾乎一樣（逐一試到走得通為止），
差別是候選清單**不經過 `sub_3BCED`，直接就是敵方全部單位**，
而且多了下面兩層。

### ⭐⭐⭐ `sub_3B961`（57 行）= **火力分配上限：3 倍**

```
sub_3B961(外層bp):
    j = 外層.var_3
    if j == 0: return 0
    return 火力[j] >= 強度[j] × 3
```

> **一個敵方單位最多吸引三倍於它的我方戰力。** 額度用完，
> 後面的單位就算走得到也不派過去，改試下一個敵人。

這是整條戰鬥 AI 裡**唯一一處主動分散兵力**的規則。其他地方
（值 3 打城市、值 14／15 斬首）都是全軍撲同一個目標；
只有這裡會說「那邊人夠了，你去別的地方」。

對 remake 的意義很直接：**電腦不會一窩蜂**，這個行為要照做，
不然重製版的 AI 會比原版更笨（或更兇，看局面）。

### ⭐⭐ `byte[65BAh + 格]` = **下一跳的佔用表** — 解了一半

`docs/re/31` §16 把 `[di+65BAh]` 記成「形狀像個佔用／已處理標記，未解」。
這一節看到它的完整用法：

| 位置 | 動作 |
|---|---|
| 火力額度用完 | `格佔用[下一跳] = 0`（讓出來）|
| 退而求其次那條路 | 先檢查 `== 0`，用了就設 `= 1` |

所以它是**「這一格這回合已經被別人當成下一跳」**。
⚠️ 但主要成功路徑（走得通、火力也夠額度）**沒有設它**——
只有 fallback 那條會設。這個不對稱是原版就有的，不是讀漏。

### `sub_56A57(累加器, 格)` = 這一格周圍聚集的**守方**戰力

新解出來的一支。兩段結構：

```
for dir = 1..6:                              ; 相鄰六格
    鄰 = 格 + 六角位移表[625Ah + (格 mod 2)×12 + dir×2]
    if !sub_510E0(鄰):        continue        ; 格無效
    if 格佔用[鄰] == 0:        continue
    if 將領[格佔用[鄰]].+8 != 0: continue      ; ← **只算守方**（+8 == 0）
    *累加器 += sub_5A0B9(該單位)

for dir = 1..12:                             ; 距離 2 的那一圈，另一張表 6272h
    同樣的事
```

⚠️ **一個讀不通的地方，據實記著**：呼叫端傳的是 `&強度[i]`——
**我方的 index**，而 `sub_3B961` 讀的是 `強度[j]`（敵方 index）。
兩者不一致，而 `強度[j]` 在每次內層迴圈開頭都會被直接覆寫成
「敵方 j 的攻擊力」，所以 `sub_56A57` 加進去的量多半會被蓋掉。

可能是原版的索引錯誤，也可能是我讀漏了什麼。**沒有證據可以判斷**，
所以 Go 側只實作看得懂的那部分（3 倍上限），
`sub_56A57` 的貢獻標為未採用，不假裝知道它該落在哪裡。

### 未讀清單更新

```
sub_3A817 在 11／12／16／17 之間怎麼選
sub_3A8F7 在 14／15／18 之間怎麼選
sub_3AA51 在 1／3 之間怎麼選
sub_3B8B0（一個目標都走不到時的收尾，兩支共用）
sub_3BCED 的清單怎麼產（§25 部分）
sub_56A57 那個索引不一致
彈藥與燃料在哪裡被扣
```


## 53. ⭐⭐ `sub_3A8F7` 的三向分流解了 —— **決策鏈全部沒有未知步驟**

§16 只知道這一步「給 14／15／18 三個值」，怎麼選未讀，
所以 `battleai.go` 一直取 14 當代表並標為已知偏差。現在補上：

```
sub_3A8F7(bp):
    if sub_3A320(0, 0.67) != 0: return          ; 敵方 >= 我方的 2/3 → 不出手
    if sub_3A766():             決策值A = 18     ; 有攻方單位逼近城市
    else if sub_56D13():        決策值A = 14     ; 斬首，只留一個守城
    else:                       決策值A = 15     ; 斬首，駐守的都留
    決定 = 1
```

那個 `0.67` 是暫存器裡的 Real 常數（`AX:BX:DX = 5280/1EB8/2B85`），
與 `AIBattleRatioEven` 對得上。

### `sub_56D13`（37 行）= **攻方單位數 < 城市格數**

§40 記成「某個計數 < 城市數」，沒說那個計數是什麼。是這樣：

```
sub_56D13():
    sub_55FBE(&城市格清單, &城市數)
    return sub_56CD0() < 城市數

sub_56CD0():  數 word[750h + i×2] 非零的槽位（i = 1..10）→ 第一方的單位數
```

> ⭐ 語意很順：**敵人少就大膽出擊（只留一個守城），敵人多就保留守軍。**
> `sub_3A766`（§6，已解）是更優先的一關——**自家城市被逼近就先解圍**。

三者合起來是一個完整的優先序：**先救城，再看敢不敢傾巢而出。**

### ⭐⭐⭐ 順帶把 `byte_64900` 的加項讀通了

§48 的門檻寫成 `比率 + byte_64900 < 15`，當時 `byte_64900` 標「未解」，
§49 解出它是**回合數**。兩件事湊起來，整條式子有了字面意義：

```
還能撐幾回合  +  已經打了幾回合  <  15
```

> **= 這批補給撐不到第 15 回合。** 而戰鬥上限正好是 16 回合（§49）。

所以「吃緊」不是抽象的比率門檻，是**撐不到終局**。
`cmd/dsds` 原本把那個加項給 0（「最寬鬆的一端」），現在傳當前回合數。

### 決策鏈的未讀清單清空

`UndecidedBattleSteps` 從三筆縮到一筆：

```
byte_6B968（sub_534FF 的另一個輸出，語意未解）
```

`sub_3A817`／`sub_3AA51` 早在 §45 就解過（本節查證時確認，沒有重推）。


## 54. ⭐⭐ `sub_3B8B0`（90 行）：走不到任何目標時的收尾 —— **兩方不對稱**

§52 與 §24 都以「全部走不到才呼叫 `sub_3B8B0`」收尾，那支一直未讀。

```
sub_3B8B0(狀態, side, u):
    if side == 0:                       ; 第二方（守方）
        將領[u].+9  = 2                 ; 打回待命
        將領[u].+10 = 0                 ; 清目標
        return

    ; ── 第一方（攻方）多兩道保護 ──
    if u == word_64902: return          ; ① 首位單位免疫

    待命數 = 0
    for i = 2 to 10:                    ; ← 從 2 起，跳過首位
        v = 第一方[i]
        if v != 0 且 將領[v].+9 == 2: 待命數++
    if 待命數 > 2: return               ; ② 已有 3 個待命就不再多一個

    將領[u].+9  = 2
    將領[u].+10 = 0
```

> ⭐⭐ **攻方保護主帥、限制待命人數；守方兩者都沒有。**
>
> 這是目前看到最清楚的一處「兩條鏈不是同一套規則」的證據。
> §32 已經知道分支 A 管第二方、分支 B 管第一方，但那是**分工**；
> 這裡是**行為差異**——同一件事（派不出去怎麼辦）兩方做法不同。

### 三個細節

1. **待命上限是 3 不是 2。** 判斷是 `> 2` 才擋，所以待命數 0/1/2 時
   還可以再加一個。這種「門檻寫 2、實際上限 3」很容易讀成 2。
2. **迴圈真的從 2 開始。** `mov var_1, 2` 之後 `jmp` 到迴圈體，
   `inc` 在回跳目標上——第一次用的是 2。`CLAUDE.md` §7 第 2.4 條警告的是
   「把入口跳當成經過 `inc`」，那會讓起始值差 1；**這一支剛好相反**，
   它是真的從 2 起，而且跳過首位是有意義的（與 ① 一致）。
3. **只設 `+9` 與 `+10`，不動 `+12`。** 與 `sub_3B15E`（§33 的
   `ResetToStandby`，會把 `+12` 清成 `0FFh`）不同。兩支不能合併——
   走不到的單位保留原來的下一跳，下回合可能還走得到。

Go 側 `internal/game/battlechase.go` 的 `FallbackToStandby`，
常數 `AIStandbyCap = 3`，四條測試把不對稱與上限都釘住。

### 未讀清單更新

```
sub_3BCED 的清單怎麼產（§25 部分）
sub_56A57 那個索引不一致（§52）
彈藥與燃料在哪裡被扣（§49）
byte_6B968（sub_534FF 的另一個輸出）
```


## 55. ⭐⭐⭐ 彈藥與燃料在哪裡被扣 —— **騎兵不吃油，鐵路省三分之二**

§49 解出回合結算（`sub_4136B`）只**檢查**彈藥與燃料是否為 0，不扣它們，
並把「在哪裡扣」列進未讀清單。答案是兩支結構相同的函式：

| 動作 | 函式 | 扣什麼 |
|---|---|---|
| 射擊 | `sub_507C7` | 彈藥 |
| 移動 | `sub_50905` → `sub_5088C` | 燃料 |

兩支都是：**依兵種與兵力算消耗 → 依 `+8`（攻方 1／守方 0）從自己那一方扣 → 不足歸零**。

⭐ 順帶第三次確認 **第一方 = 攻方**：`+8 != 0` 的分支扣 `word_6493A`（第一方彈藥）。

### 消耗表（換成滿員兵力）

| 兵種 | 彈藥／射擊 | 燃料／移動 | 走鐵路 |
|---|---|---:|---:|
| 1 步兵（20,000）| 兵力 / 200 = **100** | 兵力 / 200 = **100** | 33 |
| 4 砲兵（2,000）| 兵力 / 4 = **500** | 兵力 / 10 = **200** | 66 |
| 5 裝甲（200）| 兵力 × 2 = **400** | 兵力 = **200** | 66 |
| 6 騎兵（10,000）| 兵力 / 200 = **50** | **0** | 0 |

三個設計上的重點：

1. ⭐⭐ **騎兵不耗燃料**（`sub_5088C` 對兵種 6 直接寫 0）。**馬不吃油。**
2. ⭐⭐ **砲兵一次齊射的彈藥是步兵的五倍**（500 對 100）。
   拿實機湖北的彈藥存量 8,787 來算，一個滿員砲兵旅只能射 **17 次**。
3. ⭐⭐ **走鐵路的燃料消耗除以 3**。`sub_50905` 呼叫 `sub_4FEF0(格)`
   ——那支 `docs/re/07` §442 早就解出來是「**這一格有沒有鐵路**」
   （`22 < 地圖值 & 0FFFh < 550`）。

> 第三條對這個題材特別貼切：1920–40 年代的中國，鐵路就是戰略骨幹。
> 原版把它做進了燃料帳，不只是移動成本。

### 對 remake 的意義

`docs/mechanics/30-combat.md` 原本只記「補給見底會輸」，沒有消耗端。
補上之後整條補給線是閉合的：

```
帶多少進場（MEM_WAR +0..+7）
  → 每回合固定扣黃金與糧食（sub_4136B，按兵力）
  → 每次射擊扣彈藥、每次移動扣燃料（按兵種）
  → 任一項見底 → 戰鬥當場結束，對方勝
```

**兵種選擇因此有了戰略代價**：砲兵火力強但吃彈藥，裝甲耗油，
騎兵機動又不吃油但攻擊力低。這不是平衡表上的數字，是**資源帳上的差異**。

Go 側 `internal/game/battleconsume.go`，六條測試。

### 未讀清單更新

```
sub_3BCED 的清單怎麼產（§25 部分）
sub_56A57 那個索引不一致（§52）
byte_6B968（sub_534FF 的另一個輸出）
sub_507C7／sub_50905 的呼叫端（誰算「一次射擊」「一次移動」）
```


## 56. ⭐⭐ `byte_6B968` = **「這場仗是被必勝結算判掉的」**，戰報要多印一段

決策鏈的最後一筆未讀。xref 只有三處，一看就完了：

```
寫  sub_39B6E   mov byte_6B968, 0      ; 戰鬥主函式，開場清零
寫  sub_3A817   mov byte_6B968, 1      ; ← 只有分支 A 選中**值 11** 時
讀  sub_540B2   cmp byte_6B968, 1
```

`sub_540B2` 是**戰報畫面**——它載入 `warrep.tpc`
（`docs/formats/06` §319 已記：戰鬥九步結算第 1 步，同一支函式配 `bt02` 音樂）。
`byte_6B968 == 1` 時多畫一段，用的是字模 buffer `word_6B98A`／`word_6B98C`
（`docs/re/24` §163 記過那一族指標）。

### 它標的是哪一種結束

`sub_3A817`（§45）只有一條路設它：

```
if sub_3A320(0, 5.0)          ; 第二方被壓到第一方的 1/5 以下
   且 !sub_53619(0):          ; 而且守方**沒有**鄰省後援
       決策值 = 11             ; 必勝結算
       byte_6B968 = 1          ; ← 這裡
```

> **戰力差五倍、又叫不到援軍 → 直接判勝負，而戰報上會多一段字。**

所以它不是遊戲規則的一部分，是**呈現層的旗標**：告訴戰報畫面
「這場不是打到一方全滅，是被優勢差判掉的」。

⚠️ **多印的那段文字內容未解**——要追 `word_6B98A` 指向哪個 `.15` 檔
以及那幾個座標對應的槽位。`docs/re/24` §163 把這一族 buffer 指標列為未解，
是同一條線。

### 對 remake 的意義

`BattleRunStats` 早就有 `Decisive` 與 `DecisiveNote`（§16 加的），
語意剛好對得上——**但只有分支 A 的值 11 會設原版那個旗標**，
分支 B 的值 1（`sub_3AA51`，攻方被壓到 1/5）**不設**。

這個不對稱與 §54 是同一類：**兩條鏈不是同一套規則**。
Go 側因此把它拆成 `Decisive`（兩條鏈都會設）與 `DecisiveReport`（只有分支 A）。

### 決策鏈的未讀清單**清空**

```
（無）
```

剩下的三筆都不在決策鏈本身：

```
sub_3BCED 的清單怎麼產（§25 部分，行動的內部細節）
sub_56A57 那個索引不一致（§52，讀不通的地方）
戰報多印的那段文字是什麼（本節，呈現層）
```


## 57. ⭐⭐ `sub_3BCED` 補完 —— 清單**只收守方**，而且**省司令永遠是候選**

§25 讀到「`u = 格佔用[格]`；`if u == 0: skip`；…（未讀完）」就停了。補完：

```
sub_3BCED(&清單, mode, 城市格, &數量, …):
    清單[1..10] = 0；數量 = 0
    sub_55632(&格清單, 0, 城市格)              ; 城市周圍三圈（§12）

    for k = 1 to 36:
        格 = 格清單[k]
        if 格 == 0FFh:               跳過
        if !sub_55BCC(城市格, 格):    跳過      ; 兩步之內（§25）
        u = 格佔用[格];  if u == 0:   跳過
        if 將領[u].+8 != 0:           跳過      ; ← **只收守方**（+8 == 0）
        數量++;  清單[數量] = u

    ; ⭐ mode == 1 的額外規則
    if mode == 1 且 將領[word_64944].+5 != 0FFh:
        數量++;  清單[數量] = word_64944        ; ← **當前交戰省的司令**

    if 數量 == 0: return
    …（後段是排序，未讀）
```

### 兩個新規則

1. **清單只收守方**（`+8 == 0`）。§25 沒讀到這一關，所以「清單裡是哪些單位」
   一直只知道位置條件、不知道陣營條件。**它是「城市周圍的守軍」，不是「所有單位」。**

2. ⭐⭐ **`mode == 1` 時，當前交戰省的司令無條件加進候選清單**
   ——只要他還在場上（`+5 != 0FFh`），**不管他離城市多遠**。

> 第 2 條是這一節最有意思的地方：**省司令永遠是可打的目標。**
> 圍城的攻方不必先靠近城市才能盯上守方主帥。
>
> 這與 §34 的值 14／15（斬首）不同——那兩個是**專門**撲主帥的行動；
> 這裡是**打城市**這條路順便把主帥也放進候選池。

`word_64944` 是「當前交戰省的司令」（§37 confirmed），與 §44 的 `sub_56D49(0)`
問的是同一個人。

### 迴圈跑 36 格，不是 37 — **強證據**

`sub_55632` 產生的是「該格周圍三圈」（§12 記 37 格），而這裡的迴圈上限是
`cmp [bp+var_25], 24h` = **36**。

`1 + 6 + 12 + 18 = 37`——**36 正好是不含中心的鄰格數**。
語意也對得上：中心格就是城市本身，**城市上的守軍由 §23 的 `sub_3C0D5` 另外處理**
（那支直接取 `格佔用[城市格]` 當目標）。兩支分工，不重複。

⚠️ 標**強證據**不是 confirmed——我沒有回頭核 `sub_55632` 的輸出到底含不含中心格。
要確認就去讀那支的寫入端。

### 未讀清單更新

```
sub_3BCED 的後段排序
sub_56A57 那個索引不一致（§52）
戰報多印的那段文字是什麼（§56）
```
