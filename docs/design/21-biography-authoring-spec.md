# DESIGN-21 人物小傳自撰規格

> **狀態：READY（撰寫作業）／DRAFT（落地整合）**
>
> §2–§7 的 schema、模板、判準、派工格式已在 10 篇試寫中檢驗過，可據以派工撰寫。
> §9 的落地整合（怎麼寫回 `people.json`、怎麼產生語系檔）仍未定案，**先不要動**。
>
> 上游：`DESIGN-20 人物自傳系統` §3.6a（裁決與自撰流程初稿）、§6.1（編輯方針五原則）、
> §5.1–§5.4（版面與字元可畫性）。
> 產物：`docs/reference/people/facts-sample.json`、`docs/reference/people/bios-sample.md`（10 篇試寫）。

---

## 0. 這份文件在解什麼

使用者 2026-08-01 裁決：417 個槽位的人物小傳**全部自撰**，切斷與中文維基百科（CC BY-SA）
及其他來源的衍生關係。`DESIGN-20` §3.6a 定了方向與一版模板初稿，但明白寫著
「上面是初稿，要先在 10 篇上試寫再定」。

這份文件就是試寫之後的定案：**後續幾十個 agent 照著做的作業規格**。

⚠️ 這份規格管的是「怎麼寫」，不管「寫完怎麼進遊戲」。後者見 `DESIGN-20` §3.5 與 §9。

### 為什麼「改寫」不夠

著作權保護**表達**，不保護**事實**。把別人的句子換個說法仍可能構成衍生作品；
真正切斷關係的唯一作法是**只取事實，從事實重新寫**。

本規格把這件事做成可執行的機制：**事實骨架（`facts.json`）與成文（`bio_zh`）分離**，
成文階段**只能看骨架，不能看原文**。骨架是欄位，欄位沒有表達；從欄位長出來的句子是新表達。

---

## 1. 適用範圍

| 適用 | 不適用 |
|---|---|
| `docs/reference/people/` 的 417 個槽位（扣除 `#274 無省長` 這個佔位值，實得 416） | 遊戲原版文本（那是保存對象，一個字都不改） |
| 繁中母本 | 英譯與日譯（那是 `DESIGN-20` §8 的工作，以本規格的繁中母本為輸入） |

**前置**：動手前必讀 `DESIGN-20` §2.3（identity 陷阱）、§2.4（12 個不可畫字）、
§5.1–§5.3（版面）、§6.1（編輯方針五原則）。

---

## 2. `facts.json` 骨架 schema

### 2.1 設計原則

1. **每一個事實都自帶來源與信心度。** 不存在「全篇一個 confidence」——那會讓
   「生卒年是猜的、任職序列很硬」這種常見情形無法表達。人物層的 `confidence`
   是彙總結果，不是唯一標記。
2. **只放欄位，不放句子。** 骨架裡出現完整敘述句就是流程走歪了。
   例外：`conflicts[].statements[].says` 必須原樣記錄來源的說法，允許是句子。
3. **不確定的東西有位置放。** `conflicts`（來源互相衝突）、`gaps`（查不到什麼）、
   `identity.disambiguation`（同名怎麼排除）三個欄位存在的意義，就是讓
   「不知道」有地方寫，不必為了讓骨架看起來完整而編。

### 2.2 結構

```jsonc
{
  "schema_version": "1",
  "generated": "YYYY-MM-DD",
  "batch": "batch-03",              // 派工批次代號
  "sources": {                       // 本批用到的來源總表，key 是短代號
    "S-GPOST": {
      "tier": "A",                   // A / B / C，判準見 §3.1
      "title": "中華民國政府官職資料庫（國立政治大學圖書館）",
      "url": "https://gpost.lib.nccu.edu.tw/",
      "note": "取自政府公報的任免令，逐筆帶日期",
      "fetched": "2026-08-01"
    }
  },
  "people": [
    {
      "id": 353,                     // 對應 people.json 的 id，唯一鍵
      "name_ingame": "葉肇",          // 遊戲寫法，不得改
      "name_common": "葉肇",          // 通行寫法

      "identity": {
        "courtesy": "伯芹",           // 字；無則 null
        "aliases": ["葉賡泮（原名）"],  // 號、別名、化名、他稱綽號（他稱要標明）
        "disambiguation": null        // 同名排除說明；無風險則 null
      },

      "life": {
        "birth":      { "year": 1892, "month": null, "day": null,
                        "src": ["S-GDLQW"], "conf": "low" },
        "death":      { "year": 1953, "month": null, "day": null,
                        "src": ["S-GDLQW"], "conf": "low" },
        "birthplace": { "value": "廣東省新興縣（今雲浮市新興縣）",
                        "src": ["S-GDLQW"], "conf": "low", "note": "…" }
      },

      "education": [                 // 出身與學歷，時間排序
        { "year": 1919, "end_year": null,
          "institution": "保定陸軍軍官學校", "program": "第六期",
          "src": ["S-WPZH"], "conf": "medium", "note": null }
      ],

      "posts": [                     // 任職序列，時間排序
        { "from": "1936-08-07",      // ISO 前綴，精度可到年 / 年-月 / 年-月-日
          "to": null,                // 卸任；不明則 null
          "title": "陸軍第一百五十二師師長",   // 職務全稱，當時的正式機構名
          "org": null,               // 所屬機構（title 已含則 null）
          "station": null,           // 駐地
          "src": ["S-GPOST"], "conf": "high", "note": null }
      ],

      "events": [                    // 參與的重大事件
        { "when": "1938-07",
          "where": "江西德安萬家嶺",
          "name": "武漢會戰／萬家嶺戰役",
          "role": "第六十六軍軍長",
          "outcome": "完成阻絕防禦任務",
          "src": ["S-WPZH", "S-1937NJ"], "conf": "high" }
      ],

      "ending": { "when": "1953", "where": "臺灣", "how": "病故，終年 61 歲",
                  "src": ["S-GDLQW"], "conf": "low" },

      "verified_by": ["S-GPOST", "S-WPZH", "S-1937NJ", "S-GDLQW"],
      "confidence": "medium",        // 人物層彙總，判準見 §3.2
      "conflicts": [
        { "field": "posts",
          "statements": [
            { "src": "S-GPOST",  "says": "1949-08-03 任廣州綏靖公署副主任兼西江指揮所指揮官" },
            { "src": "S-GDLQW",  "says": "1949 年 3 月調任廣州市警備司令部司令兼廣州綏靖公署副主任" }
          ],
          "resolved": false,          // 一律 false。本規格不裁決
          "note": "處理方式：Tier A 與 Tier C 相衝時採 A，不並列（§4.4）"
        }
      ],
      "gaps": ["生卒年、籍貫、晚年去向只有一個 Tier C 來源"],
      "editorial_notes": [],          // 選填：這一篇套編輯方針時的判斷紀錄
      "supersedes": "原 people.json 此筆來源為…，本輪改以…重建"  // 選填
    }
  ]
}
```

### 2.3 欄位級 `conf`

每一筆 `life` / `education` / `posts` / `events` / `ending` 都要標 `conf`：

| 值 | 判準 |
|---|---|
| `high` | ≥2 個獨立來源，且至少一個是 Tier A/B |
| `medium` | 1 個 Tier A/B 來源；或多個 Tier C 但彼此非同源 |
| `low` | 只有 Tier C；或雖有 Tier A/B 但「這筆講的是不是同一個人」需要推定 |
| `unknown` | 查無。此時 `value`/`year` 為 `null`，`src` 為 `[]` |

**空欄位一律留 `null` + `conf: "unknown"`，不得省略欄位、不得填空字串。**
省略欄位會讓「沒查」和「查了沒有」混在一起。

---

## 3. 來源分級與 `confidence` 判準

### 3.1 來源分級

| Tier | 內容 | 算不算「獨立來源」 |
|---|---|---|
| **A** | 官方檔案與學術工具書：國史館、中研院近史所、**中華民國政府官職資料庫**（`gpost.lib.nccu.edu.tw`）、《民國職官年表》、中華百科全書民國人物庫（`digroc.pccu.edu.tw`）、省市地方志人物志、學術期刊論文 | ✅ |
| **B** | 中文維基百科**有引註的條目**、有署名與出處的專題館藏文章（如南京民間抗日戰爭博物館口述史） | ✅ |
| **C** | 百度百科、求真百科／factpedia、華人百科、百科知識、知乎、搜狐、網易、每日頭條、論壇貼文、自媒體、地方文史整理稿 | ❌ 只當線索 |

三條配套規則：

1. **同源合併。** 兩個網站放的是同一段文字（互相轉載）算**一個**來源。
   中文百科站之間互抄極普遍，預設全部合併成一個 Tier C。
   同理，維基的兩個不同條目算**一個** Tier B 來源。
2. **`gpost.lib.nccu.edu.tw` 是這個專案最被低估的來源。**
   10 篇試寫裡它命中 **8 篇**，其中 `#367 許鴻林`、`#93 王獻臣`
   在維基完全查不到的情況下仍取得帶公報日期的任免令。
   **每一篇都要查它，這是強制步驟**（§5.2 第 2 步）。
   查法：`https://gpost.lib.nccu.edu.tw/view_career.php?name=<URL-encoded 姓名>`。
   ⚠️ 它的年份是**民國紀年**（「022年」= 民國 22 年 = 1933），換算後才寫進骨架。
3. **搜尋引擎摘要不是來源。** 只看到摘要、沒抓到原文的，一律標
   `note: "僅搜尋摘要，未核對全文"` 並降一級。試寫中至少兩次遇到摘要與原文不符
   （`#158 楊森` 被摘要說成「湖南人」、`#368 于鎮河` 的集團軍番號）。

### 3.2 人物層 `confidence`

| 值 | 判準（三條全中才算） |
|---|---|
| **`high`** | ① 生卒年、籍貫、任職序列三者的欄位級 `conf` 都是 `high` 或 `medium`；② 至少一個 Tier A/B 來源；③ 沒有影響主軸的未解 `conflict` |
| **`medium`** | 任職序列站得住（有 Tier A/B 佐證），但生卒年或籍貫只有單一來源／只有 Tier C；或有未解 `conflict` 但不影響主軸 |
| **`low`** | 只有一個可靠來源；或全部只有 Tier C；或查得到的只是零星時間點、串不成序列；或**「這些記載是不是同一個人」需要推定**（§5.3） |
| **`unknown`** | 什麼都查不到。**不寫小傳**，`bio_zh` 留空 |

三個容易搞錯的地方：

- **來源可靠度升級 ≠ 信心度升級。** `#367 許鴻林` 從論壇貼文換成官職資料庫，
  內容具體得多，但**獨立來源數仍是 1**，維持 `low`。這一條寫在這裡就是為了防止
  「我查到好東西了所以升級」的直覺。
- **`low` 不是失敗。** `low` 是「照實說我只有這麼多」。
  硬用模板湊出一篇看起來像樣的傳記，比 `low` 更糟（`CLAUDE.md` §9）。
- **不可外推。** 同一批人裡 A 的來源可靠不代表 B 的可靠，逐人判。

---

## 4. 敘述模板（定案）

### 4.1 句序

```
S1  身分句   <姓名>（<生年>年—<卒年>年），字<字>，<籍貫>人。
S2  出身句   <年份>年<入／畢業於><學校><科別／期別>。
S3  任職句   <年份>年任<職務全稱>，<年份>年改任<職務全稱>，…
S4  事件句   <年份>年<月>，<地點><事件名>，<角色>，<結果>。
S5  結局句   <年份>年<月><日>於<地點><結局事實>。
S5b 待考句   另據<來源>，…；<相合的點>，是否同一人待考。   ← 僅在 §5.3 觸發時
S6  缺漏句   <欄位>不詳。／ 其餘經歷未查得記載。              ← 僅在核心欄位確實查無時
```

**句序固定，不得重排。缺段直接跳過，不留佔位。**
S3、S4 可各占 1–4 句；其餘每種最多 1 句。

`S6` 的位置有例外：若缺的是生卒與籍貫（S1 的內容），`S6` 上提到 S1 的位置，
寫成「<姓名>，生卒年與籍貫不詳。」——這是 `#93 王獻臣`、`#367 許鴻林` 的形狀。

### 4.2 逐句規則

| 項 | 規則 |
|---|---|
| 生卒 | `（1892年—1953年）`。缺一半寫 `（1892年—卒年不詳）`；兩者全缺則整個括號省略，改用 S6 |
| 主語 | 第一次用全名；其後用姓（葉肇→葉）或省略。**不用「他」「其人」** |
| 職務 | **一律全稱**，且用當時的正式機構名（`DESIGN-20` §6.1 原則 4）。番號用國字（第六十六軍），不用阿拉伯數字 |
| 年份 | 西元 + 「年」。**月／日只在來源有確切日期且該日期本身有意義時才寫**（如簽約日、被俘日）。純敘任令的日期進骨架，正文只寫到月 |
| 敘任 vs 任職 | 授階寫「敘陸軍中將」，職務寫「任…師長」。兩者不可混用 |
| 一句一事 | 一個句子只放一種資訊。不在事件句裡塞評價、推測或背景解釋 |
| 主動語態 | 「率部參加淞滬會戰」不是「被派往參加淞滬會戰」 |
| 引號 | 專有名稱（「和平救國軍」「聯軍辦事處」「和平解放大同協議書」）用「」原樣保留，**不當成敘述用語** |

### 4.3 禁止與限制

**禁止**（`DESIGN-20` §6.1 原則 2 的落實）：

```
英勇 卓越 傑出 出色 著名 名將 一代 傳奇 悲壯 忠貞 氣節 民族大義
賣國 罪魁 叛徒 漢奸 偽軍 偽政權 投敵 無能 腐敗 殘暴 正義 邪惡
起義 投誠 反正 剿匪（作為敘述動詞時）
```

⚠️ **「剿匪」的例外**：`贛粵閩湘鄂剿匪軍`、`剿匪總司令部` 是機構全名，**必須原樣保留**
（`DESIGN-20` §6.2 已預告這個誤報）。判準是：**當它是機構名的一部分就保留，
當它是敘述動詞（「率部剿匪」）就換成中性描述。**

**限制使用（帶歸屬才可寫）** —— 這是試寫新增的一條：

> 他人給的稱呼（綽號、時稱、他稱）可以寫，但必須帶歸屬詞（「時人稱」「一般稱」），
> **不得當主語，不得當定性**，而且和其他事實一樣要過兩來源門檻。

- ✅ 「事後未受追究，時人因此稱其為『東陵大盜』。」
- ❌ 「東陵大盜孫殿英…」
- ❌ 張淦的「羅盤將軍」——流傳最廣，但只有 Tier C，**未達門檻，不寫**。
  流傳廣不等於可查證。

### 4.4 衝突怎麼寫

| 情形 | 處理 |
|---|---|
| 兩個**同級**來源互斥（都 Tier B，或都 Tier C）| 正文並列：「一說<A>，一說<B>」。**不加「應以…為準」，不排序，不暗示** |
| 三說以上 | 前綴「未有定論：」再並列（示範見 `#58 吳佩孚` 死因） |
| **Tier A 與 Tier C 互斥** | **採 A，不並列**，分歧記進骨架 `conflicts`（示範見 `#353 葉肇` 的 1949 年任命日期） |
| 一方有、一方沒有（不是互斥，是覆蓋範圍差） | **不是 conflict**。正文寫有的那一方，骨架記一筆說明（示範見 `#158 楊森` 的第十六師師長） |
| 分歧的其中一方**只看到搜尋摘要、沒核對原文** | **只進骨架不進正文**（示範見 `#368 于鎮河` 的集團軍番號） |
| 同一件事的年份與番號**全都**對不上 | 正文只寫事件本身，**年份與番號都不寫**（示範見 `#204 孫殿英` 改隸直魯聯軍） |

最後一條是這批試寫最重要的發現：**「查不到就不寫」比「挑一個看起來合理的」重要。**

### 4.5 待考句（S5b）

當兩批記載**看起來是同一人但沒有來源這樣說**時，用固定句型：

> 另據<來源名稱>，有一名<姓名>…（列該來源的事實）；<相合的點>，是否同一人待考。

同時把人物層 `confidence` 降為 `low`，並在 `conflicts` 記一筆 `field: "identity"`。

**這個句型是試寫時新增的**（`DESIGN-20` §3.6a 的初稿沒有）。
沒有它，`#93 王獻臣` 只有兩個壞選項：安靜地把兩批記載合併（等於捏造一個接合），
或丟掉官職資料庫那批（等於丟掉真材料）。

### 4.6 長度

**以格數定義，不以字數定義。**

`DESIGN-20` §5.2：全形字元占 1 格、半形（數字、拉丁字母、`·`、`—`）占半格；
§5.1：本文區一頁 13 行 × 28 格 = **364 格**。

| | 值 | 理由 |
|---|---|---|
| 硬上限 | **340 格** | 364 減去約一行（24 格）的斷行浪費。超過就砍，砍法見 §4.7 |
| 目標區間 | 150–300 格 | 現有 326 篇中位 157.5 字；`medium`／`low` 自然落在下半 |
| 下限 | **無** | 查到多少寫多少。`#367 許鴻林` 62.5 格是正常結果，不是缺陷 |

⚠️ **不要用字數估格數。** 民國人物傳記的年份密度極高，半形字元占 12–15%，
「355 字」實際只有 330 格。一律跑 §6 的檢查腳本量。

### 4.7 取捨優先序

`#1 蔣中正` 的可查證事實足夠寫三千字，模板必須給出可執行的砍法。
**取捨軸是遊戲時段**——自傳是給玩家在遊戲裡查將領用的：

```
1. 身分（生卒、籍貫、字）                             永不砍
2. 與該人物所屬遊戲期別直接相關的職務與事件            永不砍
   （北伐時期 1926–28／抗戰前期 1937–41／抗日後期 1941–45，
     取自 people.json 的 periods 欄）
3. 該人物的最高職務一項                               次要
4. 結局一句                                          次要
5. 遊戲時段外的細節、非軍政經歷、學術性爭議            最先砍
```

示範：`#1 蔣中正` 砍掉了援閩粵軍作戰科主任、中國國民黨總裁、中山艦事件、清黨、
中原大戰、第二次國共內戰；日本陸軍士官學校學歷的分歧留在骨架不進正文。
`#204 孫殿英` 在名冊裡只出現於北伐時期，因此 1930 年中原大戰那一句最先被砍。

---

## 5. 寫作流程

### 5.1 四步（`DESIGN-20` §3.6a，此處補上可操作細節）

```
1. 抽骨架    從既有素材與可靠來源取出結構化事實 → 填 facts.json
             ↓ 產物是欄位，不是句子
2. 交叉查證  ≥2 個獨立來源對得上才標 high。對不上標 conflict，不裁決
             ↓
3. 依模板成文  ⚠️ 關掉原文，只看骨架寫
             ↓
4. 回查      逐句對照骨架，每句都要能追到某一欄。追不到的刪掉
```

**第 3 步的「關掉原文」是整套機制的支點。** 一邊看維基一邊改寫，產出的必然是衍生作品。
實務上：把 `facts.json` 的該筆貼進一個乾淨的上下文，來源網頁一律不再開。

### 5.2 每一篇的強制步驟

1. 讀 `docs/reference/people/people.json` 該筆的**欄位**（`birth`/`death`/`birthplace`/
   `faction`/`highest_post`/`aliases`/`periods`）當**線索**。
   ⚠️ **`bio_zh` 只用來抽事實，不得作為成文參考。** 那是待汰換的衍生文字。
2. **查 `gpost.lib.nccu.edu.tw`**（強制，見 §3.1 第 3 條）。
3. 查中文維基：先查個人專條，查無再查**部隊沿革條目**（「國民革命軍第 N 軍」「第 N 集團軍」）
   與**事件條目**——`partial` 那 96 筆的材料多半在這裡。
4. 同名風險評估：填 `identity.disambiguation`，或確認無風險後填 `null`。
5. 填骨架 → 關原文 → 成文 → 回查。
6. 跑 §6 的品質閘。

### 5.3 同名與 identity 三條硬規則

1. **`name_ingame` 是槽位的鍵，不是人的鍵**（`DESIGN-20` §2.3）。
   已知三組「兩筆同一人」：`4/290`（閰錫山/閻錫山）、`99/399`（陳儀/陳公俠）、
   `50/289`（龐炳勛/龐炳勳）。撞到這些 id 要看另一筆已寫的版本，**不要各寫一篇矛盾的**。
2. **`#274 無省長` 不是人**，是佔位值。跳過，不寫。
3. **推定的接合一律降 `low` 並用待考句**（§4.5）。
   「時間對得上、地點對得上，所以是同一個人」是查證者的推理，不是來源的陳述——
   兩者的證據等級差一級，不能混。

### 5.4 回查檢核表

成文後逐項打勾，**不打勾不算完成**：

- [ ] 每一句都能指到 `facts.json` 的某個欄位路徑（如 `posts[3].title`）
- [ ] 沒有骨架裡沒有的年份、地名、番號、人名
- [ ] 沒有 §4.3 禁止清單的詞
- [ ] 他稱綽號都帶歸屬詞，且過了兩來源門檻
- [ ] 衝突的處理符合 §4.4 的對應列
- [ ] 職務都是全稱、都是當時的正式機構名
- [ ] 格數 ≤ 340（跑腳本量，不目測）
- [ ] 字元可畫性檢查 0 個不可畫
- [ ] `confidence` 依 §3.2 三條逐條核過，不是憑印象填

---

## 6. 品質閘（機器檢查）

每批交件前跑一次。腳本寫 `/tmp`，用 `tools/py.sh - < /tmp/xxx.py` 送進 docker
（`CLAUDE.md` §9：Python 一律 docker + uv venv）。

```python
import re, sys, unicodedata, json
sys.path.insert(0, 'tools'); import eten

cells = lambda t: sum(1.0 if unicodedata.east_asian_width(c) in 'WF' else 0.5 for c in t)

for text in bios:                       # 每篇正文
    assert cells(text) <= 340, "超過一頁"
    for ch in set(text):
        if ord(ch) < 128:
            continue
        b = ch.encode('big5')           # 編不出 → UnicodeEncodeError
        assert len(b) == 2 and eten.big5_to_font(b[0], b[1]) is not None, f"畫不出：{ch}"
```

四項檢查：

| # | 檢查 | 失敗處理 |
|---:|---|---|
| 1 | 格數 ≤ 340 | 依 §4.7 砍到過為止 |
| 2 | 每個字元都能編 Big5 且倚天有字模 | 換字。**不得引入 `DESIGN-20` §2.4 那 12 個字**（`〇榘衞鲁啓藁献専継荣羣钟`）——尤其 `鲁 献 荣 钟` 四個簡體殘留，既有掃描漏了它們，新文字不准再犯 |
| 3 | `facts.json` 合法且 `id` 對得上 `people.json` | 修 |
| 4 | 禁用詞掃描（§4.3 清單）| 逐篇看，注意「剿匪」的機構名誤報 |

10 篇試寫的實測結果：2,765 字、518 個相異字元，**Big5 編不出 0、倚天無字模 0、
12 個問題字一個都沒出現**。這說明只要照 §4.2 用正式機構名與國字番號寫，
可畫性不會是瓶頸。

---

## 7. 派工規格

### 7.1 分批建議

剩餘 **395 篇**（416 個槽位 − 已試寫 10 − `unknown` 91 篇不寫傳 … 實際待寫量見下）。

按 `02-status.md` 的分佈重算：

| 類別 | 筆數 | 每篇成本 | 建議批量 |
|---|---:|---|---:|
| `confirmed`（有維基專條）| 230 | 中——資料多，成本在**取捨**與查 gpost | **15 篇／批** |
| `partial`（只有部隊沿革）| 96 | 高——成本在**找材料**，常要翻三四個部隊條目 | **10 篇／批** |
| `unknown`（查不到）| 91 | 低——但**不是零**：仍要跑一次 gpost 與異體字重查 | **25 篇／批** |

- `confirmed` + `partial` 已扣掉試寫的 10 篇 → 316 篇 → 約 **24 批**
- `unknown` 91 篇 → 約 **4 批**（多數會維持 `unknown`，但 `#367 許鴻林` 的經驗說明
  gpost 可能撈出一批人，所以這 91 筆**必須全部重跑一次**，不能直接沿用結論）
- 合計約 **28 批**

**為什麼不是「每批 30–50 篇」**：第 2 步交叉查證需要判斷力，
批量一大就會退化成「照抄第一個搜到的」。10–15 篇是一個 agent 能維持查證品質的上限，
也讓每批的回查（§5.4）還做得完。

**批次順序建議**：先做 `partial` 的 96 篇。理由是這批最可能因 gpost 而**改變結論**，
早點知道 gpost 的命中率有多高，才能決定 `unknown` 那 91 筆要投入多少。

### 7.2 派工 prompt 必含項

`CLAUDE.md` §10：邊界要寫進 prompt，沒寫的等於允許。每批的 prompt 至少要有：

1. **本批的 id 清單**與各自的 `periods`（決定 §4.7 的取捨軸）。
2. **必讀**：本規格 §2–§6 + `DESIGN-20` §6.1。
3. **強制查 gpost**，並附上 URL 格式與民國紀年換算提醒。
4. **產出**：`facts-batch-NN.json` + `bios-batch-NN.md`，格式照 sample 兩檔。
5. **邊界**：只能新增這兩個檔；不准改 `people.json` 與其他既有檔；
   不准 git commit/push/add；不准碰 `workplace/`、`~/.claude/`；
   禁止任何 docker image／volume／system／builder prune 與 `docker rmi`；
   只能清理自己建立的 container。
6. **回報**：每篇的 `confidence` + 格數 + 來源 tier + 品質閘結果 +
   **「哪幾筆查不到、下一步該去哪查」**。

### 7.3 交件驗收

主迴圈收到每批後檢查：

- [ ] 品質閘四項全過（自己重跑一次，不採信回報）
- [ ] `low` 的比例合理——**`low` 太少要懷疑硬湊**。
      以試寫的 10 篇為基準，`partial` 母體的 `low` 比例應該不低於 20%
- [ ] 抽 2 篇做回查抽查：隨機挑句子，要能指回骨架欄位
- [ ] 禁用詞掃描的誤報（機構名）確實是誤報，不是漏網
- [ ] `conflicts` 不是空的——**全批零衝突要懷疑沒真的交叉查證**

---

## 8. 試寫發現的規格缺口（回饋 `DESIGN-20`）

⚠️ 以下是試寫實際撞到的問題。**本規格只記錄，不改 `DESIGN-20`**（那是別人的檔）。

| # | 缺口 | 本規格的處理 | 要不要回改 `DESIGN-20` |
|---:|---|---|---|
| G1 | **§6.1 原則 2 沒處理「引述他人的評價性稱呼」。** 「東陵大盜」「羅盤將軍」不是作者的價值判斷，是他人給的稱呼——禁止清單管不到，但放著不管會讓貶義綽號直接進正文 | §4.3「限制使用」：帶歸屬詞、不當主語、且要過兩來源門檻 | 建議補進 §6.1 成為第六條 |
| G2 | **§6.1 原則 3「爭議事件並列說法」沒說 tier 不同時怎麼辦。** Tier A 與 Tier C 互斥時並列，等於把公報日期和自媒體傳言擺同一位階 | §4.4：Tier A vs Tier C **不並列**，採 A，分歧記骨架 | 建議補 |
| G3 | **§3.6a 的模板初稿沒有生卒年的位置。** 但現有 326 篇全部以「（1892年—1953年）」開頭，版面副標也要顯示 | §4.1 S1 明訂括號格式與缺值寫法 | 只是初稿補完，不必回改 |
| G4 | **§3.6a 的模板沒有「兩批記載可能不是同一人」的表達方式。** 這不是罕見情形——`partial` 那 96 筆多半靠部隊沿革拼出來，接合本來就是推定 | §4.5 新增待考句 S5b | 建議補 |
| G5 | **§3.6a 說「長度區間也要定」，但 §5.2 的半形半格規則使字數失去意義。** 355 字只占 330 格 | §4.6 改以**格數**定義 | 建議把 §2.2 的長度統計加註「字數 ≠ 格數」 |
| G6 | **§2.4 的 4 個簡體殘留（`鲁 献 荣 钟`）源於既有掃描的過濾器有洞。** 本規格的品質閘用 `big5_to_font()` 逐字檢查，不用「簡體字表」比對——**這才是正對照**（`~/diagnosis-notes` 02 的同一類問題） | §6 檢查 2 | 建議把 §10 的 U5 標成「新資料已由 DESIGN-21 §6 擋住，只剩既有 326 篇要修」 |
| G7 | **`02-status.md` §4 說「7 筆來源不可靠」，實際上更嚴重的問題是「其中 5 筆的內容在可靠來源裡查得到，只是沒人去查」。** 試寫的 5 筆有 4 筆從 gpost 拿到帶公報日期的任免令 | §3.1 第 3 條：gpost 列為每篇強制查詢 | 建議把「gpost 未被使用」記進 `02-status.md` §5 的方法紀錄，供重跑 |

**另外兩點與 `DESIGN-20` 無關，但派工前要知道：**

- **`DESIGN-20` §6.2 的關鍵詞複審表（約 60 篇）在全面自撰之後大部分作廢。**
  那張表是對「既有 326 篇」做的掃描；自撰版本從骨架長出來，禁用詞在 §4.3 一開始就擋掉。
  仍要保留的只有「事件本身的官方名稱含評價性字眼」這一類
  （如「和平解放大同協議書」是原件名稱，必須原樣保留）。
- **`highest_post` 與 `faction` 兩個欄位也要一起自撰。**
  `#204 孫殿英` 的 `faction` 現在是「多次易幟（直系→鎮嵩軍→…）」——那是敘事性歸納，
  不是機構名，違反 §6.1 原則 4。這類欄位在派工時要一併重填，不能只換 `bio_zh`。

---

## 9. 未決

| # | 項目 | 需要什麼才能決定 |
|---|---|---|
| **V1** | **自撰版本怎麼寫回 `people.json`**：整批取代 `bio_zh`，還是另存一份 `bios/` 由 `gen_people.py` 合併？ | 使用者裁決。本規格傾向後者——原檔保留可對照，且 `people.json` 是研究素材、不該被覆寫 |
| **V2** | **`unknown` 那 91 筆重跑 gpost 之後，救回來的人要不要改期別統計**（`02-status.md` §1 的三段表）| 等第一批 `unknown` 跑完看命中率 |
| **V3** | **`#274 無省長` 何時從名冊移出**（`02-status.md` 已建議）| 屬 `docs/formats/` 的邊界條件，不是本規格範圍 |
| **V4** | **要不要為 `low` 的人物在版面上加更明顯的標示**（`DESIGN-20` §10 U8 的延伸）| 本規格產出的 `low` 比例會比預期高（試寫 10 篇裡有 2 篇），到時再看 |
| **V5** | **`verified_by` 要不要進發行包**（`DESIGN-20` §3.6a 說要放一份參考書目）| 等 §7 全部跑完，看來源總表有多大 |


---

## 9. 第一批（batch01）回饋的訂正 — **2026-08-01**

第一批 10 篇（`bios-batch01.md`）跑完，回報了 10 個規格缺口。
以下三條**已裁決，後續批次照這個做**；其餘列在末尾。

### 9.1 ⭐ G-N2 裁決：`confidence` 評的是**正文**，不是資料完整度

**問題**：任職序列有 gpost 多筆公報佐證（可靠），但生卒年與籍貫**完全查無**。
這個組合掉在 §3.2 四個等級之間——`high` 要求生卒籍貫是 `high`/`medium`（不符）、
`medium` 說的是「只有單一來源／只有 Tier C」（全無比這更差）、
`low` 的四個觸發條件都不符（序列站得住、來源不只一個）、
`unknown` 是「什麼都查不到」（也不符）。

**裁決**：

> **`confidence` 只評「已經寫進正文的內容」可不可信，
> 不評「這個人的資料齊不齊」。**

理由：完全查無的欄位**不會寫進正文**（用 S6 缺漏句「籍貫不詳」處理），
所以它不影響正文的可靠度。讀者看到的是正文，`confidence` 要回答的是
「我讀到的這些話可不可信」，不是「這個人有多少資料」。

資料齊不齊是另一個維度，**用既有的 `gaps` 欄位表達**——那個欄位本來就在。

具體作法：

| 情況 | `confidence` | `gaps` |
|---|---|---|
| 任職序列有 ≥2 個獨立來源（至少一個 Tier A/B），生卒籍貫查無 | **`high`** | 記「生卒年、籍貫查無」 |
| 任職序列只有 1 個 Tier A/B 來源，生卒籍貫查無 | **`medium`** | 同上 |
| 連任職序列都串不成、或全部只有 Tier C、或需要 identity 推定 | **`low`** | — |
| 正文寫不出任何一句 | **`unknown`**，不寫小傳 | — |

⚠️ §3.2 的三條「容易搞錯的地方」全部仍然有效，特別是
**來源可靠度升級 ≠ 信心度升級**——獨立來源數才是判準，不是來源的權威性。

### 9.2 ⛔ 同時廢掉 §7.3 的「`low` 不低於 20%」驗收門檻

那條門檻假設 `low` 應該佔一定比例，而 9.1 的裁決會讓 `low` 大幅下降。
但更根本的問題是：**規定 `low` 的比例，等於給出「為了達標而調低判定」的誘因**，
那與 `CLAUDE.md` §9「推論標籤要誠實」直接衝突。

改成：**每批回報 `confidence` 分佈，異常時檢討原因，不設硬性比例。**
若某一批 `low` 特別高或特別低，那是需要解釋的訊號，不是需要調整的數字。

### 9.3 ⚠️ G-N3：**gpost 以姓名為鍵，不是以人為鍵**

這一條最危險——它會**產生錯誤內容**而不是遺漏內容。

第一批的實例：`#179 許琨` 在 gpost 的頁面有 21 筆，橫跨 **1913–1994 年**，
後 12 筆是監察院職員。**照單全收會生出一個活了八十年的將軍。**

**強制作法**（每一位都要做）：

1. gpost 查到的任免令，先按年份排開，**看時間跨度合不合理**（超過 50 年就是警訊）
2. 用**職務性質**分群：軍職與文職、中央與地方，混在一起的先當成不同人
3. 交叉比對籍貫、字號、同時期是否有兩筆互斥的任職
4. 分不開就**分群後只採與遊戲名冊期別相符的那一群**，並在 `conflicts` 記下
   「同名多群，本篇只採 X 群」，`confidence` 降 `low`

⚠️ **`unknown` 那 91 筆重跑時特別危險**——那批人沒有生卒年可以當篩子，
只能靠職務性質與期別。**那批一律先當成有同名風險處理。**

### 9.4 G-N1 + G-N4：衝突表補兩列

§4.4 的六列表缺兩種本批實際撞到的形狀：

| 形狀 | 處理 |
|---|---|
| **Tier A 對 Tier B 互斥**（gpost vs 有《政府公報》引註的維基）| 退到雙方一致的粒度寫（例：兩邊年份不同但都說任過該職 → 只寫職務不寫年份）。本批撞到 5 次 |
| **同一個來源內部自相矛盾**（依 §3.1 第 1 條，維基各條目算同一個來源）| 同上，退到一致的粒度。⚠️ 這不是同級互斥也不是跨 tier，原表兩條都套不上 |

### 9.5 其餘六條，暫不改規格但要知道

`G-N5` 人名含不可畫字（「榘」只因該人是名冊 #49 才有解）、
`G-N6` 月／日該不該寫的判準太模糊、
`G-N7` 生卒括號沒示範「生年不詳」、
`G-N8` S3/S4 嚴格分段會打亂時間軸（第一版有三篇寫成交錯，回查才抓到）、
`G-N9` §5.3「推定接合一律降 `low`」是二元的但接合強度差很多、
`G-N10` gpost 頁面自帶版權聲明——⚠️ 我們取的是**事實**（誰在哪一天任什麼職），
不是它的表達，所以自撰流程本來就相容；但**不要整段複製它的敘述文字**。

### 9.6 作業面的兩條

- **改用 MediaWiki API 為主**：`action=parse&prop=wikitext` 比 `prop=extracts` 可靠
  （後者對模板多的條目會回空）。WebSearch 只留給查不到的人——第一批把額度用完了。
- **一批 10 篇是一個 agent 的合理上限。** 成本結構：查證 70%、成文與回查 20%、
  品質閘 10%。

---

## 10. 第四批（batch04）回饋的裁決 — **2026-08-02**

`bios-batch04.md` 交件，四項品質閘由主迴圈**獨立重跑**全過
（格數最長 332.0、相異字 417、Big5 編不出 0、倚天無字模 0、12 個問題字 0、
禁用詞 0、10 個 id 與 `name_ingame` 逐筆相符）。回報四個規格缺口，逐條裁決如下。

> ⚠️ **本節從 batch06 起適用。** batch02／03／05 派工時規格還是舊的，
> 不因此退件——差異在複核時人工吸收。

### 10.1 ⭐ G-N11 裁決：`low` 只罰**主敘述**靠推定，不罰待考句

**問題**：§5.3 第 3 條「推定接合一律降 `low`」是**全篇級懲罰**，
但接合可能只發生在尾段。`#111 馮紹閔` 有兩筆 Tier A／Tier B 日期逐字相合
（1921-07-24、1925-01-21），是該批證據最硬的一篇，卻因為尾端多寫一句
關於 1927 年那批記載的待考句，被拉到與「主敘述整段靠推定」的 `#127 陳修爵` 同級。

**這給出了「丟掉真材料換高兩級評等」的反向誘因**——承辦的 agent 明白寫著
自己一度動念只把那段留在骨架。那個念頭本身就是規格的問題，不是它的問題。

**裁決**：

> **推定接合降 `low`，只在它支撐主敘述時成立。**
> 接合只出現在待考句（S5b）、而主敘述不依賴它時，`confidence` 依主敘述評，
> 另在人物層加 `"open_join": true`。

理由與 §9.1 同源：**`confidence` 評的是已經寫進正文的內容可不可信**。
待考句本身就明說待考，讀者不會被誤導；把它拉低是在懲罰誠實。

`open_join` 讓「這個人還有一段沒接上」仍然可查，不必靠壓低評等來表達。

### 10.2 G-N12：同名分群補第四條判準——**官階倒序**

§9.3 的三條硬規則之外補一條：

> **同一個姓名的任免令序列裡出現官階倒退（如中將 → 少校、師長 → 營長），
> 且沒有貶謫記載佐證時，視為兩個人。**

⭐ 這條的好處是**不需要知道這個人是誰**——純粹從序列的形狀判斷。
對 `unknown` 那 91 筆重跑 gpost 特別有用，因為那批本來就沒有身分資訊可用。

### 10.3 G-N13：維基的**消歧義頁與列表頁不是 Tier B**

§3.1 的分級表沒有它們的位置，於是被當成「維基條目」計入 Tier B。

**裁決**：消歧義頁、列表頁、紅連結目標**一律不計 tier**，
查到它們**等同「查無專條」**。`#138 王麒` 的生年與籍貫依此退回未知——那是對的。

### 10.4 G-N14：Tier A 對 Tier B 互斥、又沒有一致粒度可退時，**採 Tier A**

§9.4 第 1 列處理的是「有一致粒度可退」的情形（退到雙方都同意的層級）。
`#120 蔣鎮臣`／`#123 劉寶題` 撞到的是**沒有**：一邊說他指揮一個師、
一邊說他是營長，中間沒有共同的粒度。

**裁決**：

> **Tier A（帶公報日期的任免令）勝。** 分歧記進 `conflicts`，正文採 Tier A。

這條會**推翻既有 bio 的主體**（`#120` 整篇建立在 1922 年吉安戰鬥，
而 gpost 顯示他 1923 年才升團長、該師番號 1925 年才存在），
所以要明寫成規則，不能留給各批自行判斷。

### 10.5 作業面：**按軍系分批，不要按 id 順序**

batch04 刻意把「江西陸軍」與「福建陸軍周蔭人部」兩個小群放進同一批，
結果拿到免費的交叉印證：`#130` 與 `#133` 有兩個同日敘任令，
`#111`／`#120`／`#121` 的師長任命集中在 1925 年 1 月 21–23 三天內。

**按 id 順序切看不到這些。** 後續派工照軍系分群。

### 10.6 兩筆要回填的交叉指引

- `#136 李鳳翔` 解掉了 batch01 `#263 曹萬順` 留下的番號衝突：
  維基〈松口戰鬥〉的第二師與〈第十七軍〉的第三師**兩說都對，分屬兩個時期**
  （gpost 1924-10-08 任第三師師長，維基〈周蔭人〉的 1925-11 序列列第二師）。
- `#116 馬祥斌` 的三組衝突指向同一份原文：劉思祥〈劉和鼎傳略〉，
  《江淮文史》2000 年第 4 期，頁 166–176。取得原文可望一次解掉。

### 10.7 一個方法上的收穫，值得記在規格裡

`#116` 出現了**用單筆任免令否證一個敘述**的例子：維基說他 1927 年 6 月下旬遇害，
但 gpost 有 1927-07-25 的任命令——7 月底仍在任命，6 月下旬遇害不成立。

> Tier A 不只是「補充細節」的來源，**它可以直接推翻 Tier B 的敘述**。
> 查 gpost 時要順手看「有沒有哪一筆的日期讓既有說法變得不可能」。

---

## 11. 第三、五批（batch03／batch05）回饋的裁決 — **2026-08-02**

兩批的四項品質閘都由主迴圈**獨立重跑**全過：

| 批次 | 篇數 | 最長格數 | 相異字 | 不可畫 | 12 問題字 | 禁用詞 | id／`name_ingame` |
|---|---:|---:|---:|---:|---:|---:|---|
| batch03 | 10 | 334.5 | 543 | 0 | 0 | 0 | 全對 |
| batch05 | 10 | 325.0 | 465 | 0 | 0 | 0 | 全對 |

### 11.0 ⚠️ 編號衝突先處理掉

batch03、batch04、batch05 是**平行派工**，三批各自從 `G-N11` 往下編，
於是同一個編號指三件不同的事。本節起改用**批次前綴**：

| 原編號 | 改成 | 出處 |
|---|---|---|
| `G-N11`..`G-N14`（batch04）| `G-04-1`..`G-04-4` | 已裁決於 §10 |
| `G-N11`..`G-N17`（batch03）| `G-03-1`..`G-03-7` | 本節 |
| `G-N11`..`G-N17`（batch05）| `G-05-1`..`G-05-7` | 本節 |

**往後派工的 prompt 要指定前綴**（`G-<批次>-<序>`），平行派工才不會再撞。

### 11.1 ⭐⭐ `#183 劉志陞`：用專案自己的一手資料解掉了

batch05 建議「比對倚天『陸』與『陞』的字模，看原版 `MAN115` 是否刻錯」。
跑了，結論明確：

```
#183 的三個字模反查 → 劉 / 志 / 陞
第三個字模 vs 倚天「陞」： byte-exact，漢明距離 0/240 bit
第三個字模 vs 倚天「陸」： 漢明距離 60/240 bit（25%）
```

> **原版真的刻了「陞」。** 反查沒有誤差，而且兩字在 16×15 點陣上差四分之一的位元
> ——肉眼可辨，不是形近誤植那種錯法。

**裁決**：`name_ingame` 維持「劉志陞」（`CLAUDE.md`：繁中母本保留原版寫法）。
`name_common` 是否填「劉志陸」取決於 identity 接合，那條線仍是 `low` + 待考句，
**不因字模而改變**——字模只證明「不是反查錯」，不證明「當年沒打錯字」。

⭐ 方法上值得記：**專案內的一手資料（字模、資料檔）可以回答外部史料回答不了的問題。**
這一題外面查一百年也查不出來，`tools/eten.py` 兩分鐘就解了。

### 11.2 兩批共同指出的最大阻礙：**`digroc` 403、WebSearch 額度用罄**

`G-03-6` 與 `G-05-7` 是同一件事。規格 §3.1 把 `digroc.pccu.edu.tw`
（中國文化大學「民國人物」）列為 Tier A，但它**對程式化請求一律回 403**，
帶瀏覽器 UA 也一樣。兩批 20 人一次都沒查成。

**這是「生卒年與籍貫全空」的直接原因**——gpost 只給任免令、維基只給部隊沿革，
人物庫才是身分欄的來源。

**裁決**：

1. §3.1 的來源表**加一欄「可及性」**，`digroc` 標「需人工瀏覽器」。
2. **派工 prompt 要寫明** `digroc` 取不到、WebSearch 額度可能為 0，
   主力是 gpost（curl）+ MediaWiki API 的 `insource:` 全文搜尋。
   兩批都證明 `insource:` 足以撐起整批查證。
3. ⚠️ 沒寫明的後果不是「少查一個來源」，是**每一批都會重踩，而且會誤判成
   「查了，沒有」**——`~/diagnosis-notes/docs/02-query-returned-empty` 的同一類問題。

### 11.3 ⭐ `G-03-4` + `G-05-5`：同名分群補**正向**判準

§9.3 與 §10.2 的判準全是**排除法**（哪些不是同一個人）。兩批各自發現一條正向的：

| 來源 | 判準 |
|---|---|
| batch03 | **階級倒退**（已授中將後又授少校）→ 本批 4 例混入有 3 例靠它抓到 |
| batch05 | **成批敘任**：直魯聯軍將領的任免令是同日下達（1918-07-27 方永昌／王棟／張宗輔同授少校、1927-02-01 同授少將、1927-10-09 同授中將）|

⭐ batch05 那條是**正向**判準：查「和誰同一天受令」。

**裁決**：§9.3 增列第 5 條——**查同一日期的其他受令者**。
與 §10.2 的「官階倒序」一樣，好處是**不需要知道這個人是誰**，
對 `unknown` 那 91 筆重跑特別有用（那批沒有生卒年可當篩子）。

（batch03 的「階級倒退」與 §10.2 的「官階倒序」是同一條，已在 §10.2 裁決。）

### 11.4 `G-05-1`：三說以上時**允許並列，不准只挑一個**

§9.4 的退粒度假設衝突是**兩說**。batch05 的 `#180 柴榮陞` 撞到三說、四說
（第八軍軍長有「柴永升／柴云升／祝祥本」），退粒度等於什麼都不剩。

**裁決**：**三說以上時，正文並列全部說法 + 待考句，`confidence` 降 `low`；
不得只挑一個。** batch05 把 `#180` 寫成候選清單而非傳記，那是對的——
**那是誠實的上限，不是偷懶。**

### 11.5 `G-05-2`：識別性事實不套退粒度

`#181 吳奠卿` 的軍番號是「五處說第十軍、一處說第十一軍」，
照 §9.4 退粒度只剩「直魯聯軍軍長」——**把人物最主要的識別資訊退掉了**。

batch05 沒照規格做（採「第十軍」+ 降 `medium` + 理由寫進 `conflicts`），
並主動回報。**這個判斷是對的，違規回報也是對的。**

**裁決**：§9.4 增列——**當爭議欄位是識別性事實（部隊番號、職務名）且來源數量
懸殊（≥4:1）時，採多數說 + 降一級 + `conflicts` 記完整理由**，不退粒度。

### 11.6 `G-05-3`：專案內一手資料入表，但**不得進正文**

batch05 用原版名冊的排序當 identity 旁證（#173–#187 與直魯聯軍軍番號單調對應），
自己新增了來源代號 `S-ROSTER`（tier A）。

**裁決**：**接受，但加限制。** §3.1 增列一行：

> `S-ROSTER`（專案內一手資料：原版名冊順序、字模、資料檔）
> — tier A，**僅限 identity 佐證，不作史實陳述，不進正文**。

理由：名冊順序是**遊戲作者的編排**，不是史料。它能說「這兩筆大概是同一個人」，
不能說「這個人當過第十三軍軍長」。batch05 把它只寫進骨架、不寫進正文，做法正確。

### 11.7 其餘四條，照辦不另立規則

| 編號 | 內容 | 處置 |
|---|---|---|
| `G-03-1` | gpost 的**異體字寫法要逐個都查**，零命中與真的沒有長得一樣 | 併入 §11.2 的派工 prompt 必含項 |
| `G-03-3` | 來源**自己並列兩說**時已標明未定論，不該再套退粒度 | 照辦：原樣轉述，記 `conflicts`，不降級 |
| `G-03-5` | 期別是北伐、材料全在 1930–37（`#56 孫長勝`）時怎麼取捨 | 照 §4.7 以遊戲期別為軸，期外材料寫進骨架不進正文。**反向問題更值得追**：遊戲把他放進北伐名冊的依據是什麼？記進 `02-status.md` 的待查 |
| `G-05-4` | 同一來源同一欄位**重複**（方永昌兩筆「授陸軍中將」）| 照辦：取範圍（只寫年）|
| `G-05-6` | 維基 **HTML 註解**算不算來源 | 算 Tier B，但要註明「出自註解」。⚠️ 這對 §9.6 建議的 `prop=wikitext` 抓法很重要——它**會把註解一起抓回來**，`prop=extracts` 不會 |
| `G-03-2`／`G-03-4` | 「全部都不是本人」需要表達方式 | 照辦：`identity.disambiguation` 記「全數排除」，`confidence` 上限 `medium` |

### 11.8 三批一致的作業結論：**按軍系分批**

§10.5 已從 batch04 得到這條，batch03 與 batch05 各自獨立再說一次
（batch05：七人同軍系共用三個條目，單位成本反而降低）。**確定照辦。**

---

## 12. 第二批（batch02）回饋的裁決 — **2026-08-02**

四項品質閘由主迴圈**獨立重跑**全過：10 篇、最長 328.5 格、相異字 454、
不可畫 0、12 問題字 0、禁用詞 0、id 與 `name_ingame` 全對。
`conflicts` 8/10 非空、`gaps` 全部有值。

編號照 §11.0 改為 `G-02-1`..`G-02-7`。

### 12.1 ⭐⭐⭐ `G-02-2`：**「查到零筆」與「限流」長得一模一樣**

batch02 回報了一件比它自己說的更重要的事：

> MediaWiki API 限流的症狀是「查無此條目」，與真的查無**完全一樣**。
> 本批第一輪就誤判過一次——`#10 馮軼裴` 的 gpost 首查回空，重跑有 5 筆。

**裁決（提升為硬規則）**：

> **任何查詢回零筆，一律重跑一次再下結論。** 零命中不是結果，是狀態。

這正是 `~/diagnosis-notes/docs/02-query-returned-empty` 講的第一種形狀。
規格 §5.2 增為強制步驟，派工 prompt 必含。

⚠️ **這條的殺傷力被低估了**：`unknown` 那 91 筆的結論**全部建立在「查了沒有」上**。
若當初有任何一次是限流造成的，那個結論就是假的。
`02-status.md` §1 的三段表在 91 筆重跑完之前不可引用。

### 12.2 ⭐ `G-02-2` 的附帶發現：**Tier C 從不貢獻 `high`**

batch02 因為 WebSearch 額度用罄，**全批零 Tier C**——結果 `high` 反而最多（4 篇）。

原因不神秘：`high` 要求 Tier A／B 互證，Tier C（自媒體、無引註網頁）
本來就進不了那個判斷。**這說明 Tier C 的實際貢獻是補細節，不是提高可信度。**

**裁決**：不改判準，但 §3.1 加一句說明——**Tier C 不可得時照常交件，
在 `gaps` 記明即可，不算查證不力**。

### 12.3 `G-02-6`：gpost **逐字比對，異寫不通用**

`#10` 查「馮軼裴」5 筆、「馮軼斐」0 筆。

**裁決**：§5.2 明訂——**`name_ingame`／`name_common`／`aliases` 每個寫法都要各查一次**。
與 §11.7 的 `G-03-1`（異體字逐個查）是同一條，合併寫進派工 prompt。

### 12.4 `G-02-1`：`highest_post`／`faction` 正式進 schema

§8 要求重填這兩欄，但 §2.2 的 schema 沒有它們的位置，三批各自擴充。
**裁決**：正式寫進 §2.2，欄位級 `conf` 與 `source` 比照其他欄位。

（這兩欄要上遊戲畫面副標，`DESIGN-20` §5.7。）

### 12.5 `G-02-3`：生卒括號**只放沒有爭議的**

第四種形狀：生年不詳、卒年只知年且月份兩說。

**裁決**：**括號只放沒有未解 conflict 的生卒年；任一端有爭議就整個省略括號**，
把爭議寫進正文的待考句。理由與 §10.1 同源——括號是斷言的位置，不是猜測的位置。

### 12.6 `G-02-4`：同一來源內部矛盾且**退無可退**

§9.4 第 2 列處理「同一來源內部矛盾」，假設可以退到共同粒度。兩說互斥時沒得退。

batch02 改比「同一來源內部各條目的**引註品質**」（`#47 萬選才`：
維基〈靈寶戰鬥〉零引註 vs〈1930年中国〉逐日引註郭廷以 → 採後者）。

**裁決**：**接受，但寫緊**——只有在「一方有逐條引註、另一方零引註」這種
**可機械判斷**的差距時才適用；引註品質相近時退回 §11.4（並列 + 降級）。

⚠️ 不寫緊會變成「挑一個看起來合理的」的後門。

### 12.7 `G-02-5`：回報改報**兩個數**

命中率不足以判斷。batch02：gpost 命中 41 筆 → 3 筆不採用、4 筆依 §4.7 砍、
**34 筆進正文（其中 7 筆掛在待考句下）**。

**裁決**：§7.2 第 6 項改為回報「命中筆數」與「寫進正文筆數」兩個數。

### 12.8 `G-02-7`：同名分群「一群都不相符」時

§9.3 第 4 步預設至少有一群與遊戲期別相符，`#27 謝慕韓` 一群都沒有
（兩批記載分別是 1903 進士與 1936–47 軍職，都不在北伐時期）。

**裁決**：與 §11.7 的 `G-03-2` 同一條——`identity.disambiguation` 記
「全數排除」，`confidence` 上限 `medium`，並在 `gaps` 寫明
「**遊戲裡的這個人是誰尚未解決**」。

⚠️ `unknown` 那 91 筆預期會更常撞到這個形狀。

### 12.9 作業面：**〈19XX年中国〉系列與 gpost 是絕配**

兩者都源自《政府公報》與郭廷以《中華民國史事日誌》，因此**經常同一天對上**
——batch02 四篇 `high` 全部來自這個機制。

**查北伐期的 `partial` 先查它，再查部隊沿革。** 寫進派工 prompt。

---

## 13. 第六批（batch06）回饋的裁決 — **2026-08-02**

品質閘由主迴圈獨立重跑全過（10 篇、最長 319.5 格、相異字 475、
不可畫 0、12 問題字 0、禁用詞 0、id 與 `name_ingame` 全對）。

### 13.1 ⭐⭐ `G-06-5` 用**專案的一手資料**回答了

batch06 觀察到「gpost 命中率按軍系差距極大」（奉系組 80%、直魯聯軍組 25%），
建議先看 `unknown` 那 91 人集中在哪些軍系。

`people.json` 答不了——那 91 筆的 `faction` **全部空白**。
**但 `MAN(1).DAT` 知道**：`+4` 所屬省、`+14` 效忠的勢力領袖，兩格都是 confirmed。

結果見 `docs/reference/people/03-ingame-affiliation.md`：

```
張作霖 27 ／ 吳佩孚 19 ／ 孫傳芳 13 ／ 蔣中正 8 ／ 無 5
袁祖銘 4 ／ 閰錫山 2 ／ 馮玉祥・劉湘・唐繼堯 各 1
```

> **奉系 + 直系 + 五省聯軍佔 59/81（73%）**，正好是實測 gpost 命中率最低的那幾系。

**裁決**：

1. **`unknown` 91 筆的投入預期下修**——命中率合理預期落在 25% 那一端。
2. **改查法比換來源有效**：北洋各系旅團級的任命常常不走北京政府正式令，
   gpost 本來就查不到。要查省志軍事志與《申報》《大公報》的人事報導。
3. **蔣中正那 8 人與袁祖銘那 4 人優先**（中央軍與黔軍檔案狀況好得多）。
4. **這份表列為每批派工的必附資料**——它同時是查證起點與 §5.3 的同名排除依據。
   ⚠️ 但它是 `S-ROSTER`（§11.6）：**遊戲作者的編排，不是史料，不得進正文。**

### 13.2 `G-06-2`：§10.2 與 §11.3 撞車，**寫死優先序**

這是我自己造成的缺陷——§10.2（官階倒序 → 兩個人）與 §11.3（同日受令 → 同一群）
第一次指向相反方向（batch06 的 `#219` 與 `#228` 同日受令，但序列有倒序）。

**裁決**：**官階倒序優先。**

理由：倒序是**該序列自身的內部矛盾**，同日受令只是**外部的巧合性關聯**
——成批敘任一次幾十人，同日不足以證明是同一人；而一個人不會先中將後少校。

> 兩條都是啟發式，撞車時**看哪一條的反例比較難構造**：
> 「同日受令但不同人」很常見，「同一人官階倒退」很罕見。

### 13.3 `G-06-1`：Tier B 要求**引註本身也達 Tier A/B**

「有引註，但引註本身是 Tier C」這一格原本沒規定
（維基〈陸軍士官學校（日本）〉的留學生名單整段只引一篇論壇貼文）。

**裁決**：§3.1 的 Tier B 改為「**有引註，且引註本身達 Tier A/B**」。
引註指向自媒體、論壇、無出版資訊的網頁時，該段降為 Tier C。

### 13.4 `G-06-3`：**逐條引郭廷以並標頁碼的年表另計一個來源**

§3.1 第 1 條把〈19XX年中国〉這種年表與零引註的編制表**合併算一個維基來源**，
等於讓兩者等權。

**裁決**：**接受。** 逐條引第三方著作並標頁碼的維基頁面，
視為「對該第三方著作的轉引」，與其他維基頁面**分別計為獨立來源**。
（依此 batch06 的 `#187` 可升 `high`，但**本輪不追溯改評等**——
規格改動從下一批生效，§10 已有先例。）

### 13.5 `G-06-4`：回報改為**三個數**

§12.7 定的「命中 / 進正文」兩個數不夠——中間缺「採用筆數」。
`#189` 的 12→0（**查證失敗**）與 `#264` 的 23→6（**期別取捨**）在兩個數下長得一樣。

**裁決**：回報 **命中 → 採用 → 進正文** 三個數。
「命中但不採用」是 identity 排除，「採用但不進正文」是 §4.7 取捨，兩者性質完全不同。

---

## 14. 第七、九批的裁決，以及**品質閘升級成工具** — **2026-08-02**

batch07（粵桂湘）與 batch09（中央軍）交件。連同前面各批，
主迴圈用**統一的閘**（`tools/bio_gate.py`）重跑全部八批，**全部通過**。

### 14.1 ⭐⭐ `G-09-1`：閘本身有洞——**不掃會上畫面的欄位**

§6 原本只給一段程式碼片段讓每個 agent 自己抄，而那段片段**只掃正文**。
batch09 自己補跑，在 `highest_post`／`faction`／`aliases` 裡抓到 **7 個不可畫字元**。

> ⭐ 更值得注意的是它的推論：**`DESIGN-20` §10 U5 那批簡體殘留，
> 很可能就是這樣漏進來的**——不是寫的人不小心，是**閘沒看那些欄位**。

旁證：batch09 發現 `#396 高卓東` 既有的 `highest_post`「第一〇一軍軍長」
裡的「〇」倚天畫不出來——**既有欄位就已經違規**，而歷來的檢查都沒抓到。

**裁決**：閘從腳本片段升級成 `tools/bio_gate.py`，第 2 項改掃
**所有進顯示路徑的欄位**（`DESIGN-20` §5.7 的副標欄位），不只正文。
`aliases` 是查詢紀錄、不進顯示路徑，故不納入。

```sh
./tools/py.sh tools/bio_gate.py batch09     # 單批
./tools/py.sh tools/bio_gate.py --all       # 全部
```

### 14.2 `G-09-2`：**12 字清單不是檢查本身**

batch09 撞到第 13 個不可畫字（`勐`，滇西地名）。

**裁決**：清單只是**已知的例子**，用來讓錯誤訊息好讀；
**真正的檢查是 `big5_to_font()` 逐字問倚天字庫**（正對照）。
規格 §6 檢查 2 本來就是這樣寫的，但清單擺在前面容易被當成過濾器
——那正是 `~/diagnosis-notes/docs/02-query-returned-empty` 說的「自己的過濾器有洞」。
工具檔頭已把這件事寫死。

### 14.3 閘同時修掉一個**反向的**問題：禁用詞誤報

跑全批時，`sample` 的兩筆被擋下來，**兩筆都是誤報**：

- `#204`「時人因此稱其為『東陵大盜』」——§4.3 的「限制使用」（帶歸屬詞、不當主語）
- `#356`「贛粵閩湘鄂剿匪軍西路第二縱隊」——**原件的正式番號**

> 一律當違規會逼人刪掉原件名稱，**那與保存的目標相反**。

**裁決**：閘分兩級——**「該擋」**（裸用）與**「該複審」**（帶引號又帶歸屬詞）。
機構／事件的官方名稱直接放行。

### 14.4 ⭐⭐⭐ `G-07-6`：這是**第三次**同一種缺陷

batch07 指出：**分群做得越嚴，可用的 Tier A 筆數越少，`confidence` 反而越低**
（`#397 黃濤` gpost 命中 14 筆、嚴格分群後只剩 1 筆，因此掉到 `medium`）。

把它跟前兩次擺在一起看：

| 次 | 情形 | 誘因 |
|---:|---|---|
| `G-04-1` | 誠實寫待考句 → 全篇降 `low` | **丟掉真材料換高兩級評等** |
| `G-07-6` | 嚴格排除同名 → 可用來源變少 → 降級 | **放寬分群換評等** |
| （§11.5）| 識別性事實退粒度 → 主要資訊被退掉 | 承辦者選擇違規並回報 |

> **三次都是「做對事反而被扣分」。** 這不是巧合，是評等機制的系統性偏差：
> `confidence` 同時被當成「內容可信度」與「工作完成度」在用。

**裁決**：§3.2 加一條**總則**——

> **`confidence` 只評「已經寫進正文的內容可不可信」。
> 為了維持評等而放寬查證、放寬分群、或刪掉誠實的待考句，一律視為違規。**
> 嚴格分群之後降級**是正確結果**，不是失分。

（§9.1 已經對「資料完整度」講過同一件事，這裡把它擴到「查證嚴格度」。）

### 14.5 `G-07-1`：Tier B 的「有引註」二分法對**部隊沿革條目**失效

本批主力材料是「零內文引註、但列出官方書目」的條目
（《抗日戰史紀要》《中國現代軍事史主要戰役表》《廣西通志》）。
照 §3.1 字面它們是 Tier C，但實質可追。

**裁決**：Tier B 的判準改為 **「能不能追到已出版的來源」**，不論引註在內文或書目：

| | 判準 |
|---|---|
| Tier B | 有**內文引註**，或有**已出版書目**且內容可對應 |
| Tier C | 零引註且零書目，或引註／書目指向自媒體、論壇、無出版資訊的網頁 |

（與 §13.3「引註本身也要達 Tier A/B」併用。）

### 14.6 `G-07-2`：**Tier A 的權威在日期與職稱，不在地名**

`#394 汪之斌` 的 gpost 四筆行政督察專員區號連貫、**省名兩兩對不上**。

**裁決**：§3.1 加警語——任免令的權威範圍是**日期、職稱、番號**；
附帶的地名與籍貫要另找佐證。Tier A 內部矛盾時不套 §9.4，退到「區號」層級即可。

### 14.7 其餘三條，照辦

| 編號 | 內容 | 處置 |
|---|---|---|
| `G-07-3` | §12.3 只講「異寫要查」，沒講**查回來可能是別人**（`#251` 不查會漏、`#386` 查了會多）| 併入 §12.3：異寫查回來的結果要**各自**過分群 |
| `G-07-4` | §10.2 官階倒序需要例外：**同來源明載跨系統調任**時不觸發（`#398` 軍長→副軍長）| 照辦。與 §13.2 的優先序不衝突——那條講的是「無明載時」|
| `G-07-5` | §4.2 的國字番號與 §2.4 的不可畫字**是同一條規則**：寫「第一百七十師」同時解掉「〇」| 併進 §4.2，加一句「國字番號順便解決可畫性」|

### 14.8 兩批共同確認的事

- **同日成批任令好用**（§11.3）：batch07 用了四次，三組同日
  （1936-08-07 張達／李振球／張瑞貴、1936-05-30 黎行恕／張光瑋、1935-07-24 陶廣／汪之斌）。
- **限流確實偽裝成查無**（§12.1）：batch07 實測到 `too many requests` 的純文字回應，
  batch09 也遇到 gpost 與 MediaWiki 各回一次 429。兩批都改用指數退避。
- ⚠️ batch09 的提醒要記住：**`unknown` 那 91 筆的「查不到」可信度低於表面**
  ——當初既沒有重跑，也未必查過異寫。

---

## 15. `partial` 池完成（96/96）與第八批的裁決 — **2026-08-02**

batch08 交件，**`partial` 那 96 筆全部寫完**。連同 sample 裡的 4 筆 `confirmed`，
自撰累計 **100 篇**，`tools/bio_gate.py` 九批一次跑完全部通過。

| `confidence` | 篇數 |
|---|---:|
| `high` | 46 |
| `medium` | 40 |
| `low` | 14 |

`low` 佔 14%——依 §14.4 的總則，**這個數字是查證嚴格度的結果，不是失分**。

### 15.1 ⭐⭐ `G-08-3`：**兩個來源涵蓋的欄位不重疊，不算互證**

`#283 郜子舉` 有 Tier A 與 Tier B 各一，但 **A 只給敘階、B 只給職務，
欄位完全不重疊**。照 §3.2 的字面（「兩個獨立來源」）可判 `high`，
承辦者實判 `medium`。

> ⚠️ **這個組合極常見**（gpost 給任免令、維基給部隊沿革，本來就分工），
> 不寫明會**系統性高判**。

**裁決**：§3.2 明訂——

> **「兩個獨立來源」指的是**兩個來源對**同一個事實**都有記載**。
> 各說各的欄位是**互補**，不是互證；互補只能支撐 `medium`。

### 15.2 `G-08-1`：官階倒序遇上**部隊縮編**會誤報

`#262 楊勝治` 的「軍長 → 師長 → 旅長」是**兩次縮編**造成的，不是兩個人。

**裁決**：§10.2 加但書——**同一時期有整編／編遣紀錄時，官階倒序不觸發**。
⚠️ 這不是罕見情形：**1928 年編遣與 1945 年整編會大量產生這種序列**。

（與 §13.2 的優先序、§14.7 的跨系統調任例外併看：官階倒序是三條例外之後才生效的判準。）

### 15.3 `G-08-2`：不計 tier 的來源，**查到的東西仍要登錄**

§10.3 判定消歧義頁與列表頁不計 tier，但沒說「查到的資訊要不要記」。

**裁決**：登為 `tier: "none"` 並在 `gaps` 寫明。
理由與 §12.1 同源——**「查到了但不算數」與「沒查到」讀起來一樣**，
下一輪重跑時會白做一次。

### 15.4 其餘兩條，照辦

| 編號 | 內容 | 處置 |
|---|---|---|
| `G-08-4` | HTML 註解與同條目正文衝突時 | **採正文**（註解是編者的工作筆記，未經同儕檢視）|
| `G-08-5` | 同一份材料對不同人物的可用性不同（金鄉戰鬥 infobox 對 `#267` 番號相符、對 `#268` 對不上）| **同批共用材料要逐人判**，不得整批套用 |

### 15.5 ⭐ 作業筆記：**抓取失敗與零筆的形狀不同，但兩種都要防**

§12.1 講的是「零筆要重跑」。batch08 發現一種更隱蔽的：

> gpost 第一輪有 **6 個姓名回傳沒有計數表的頁面**——那不是零筆，是**抓取失敗**。
> 重跑後其中 3 個實際有資料（曹福林 3 筆、李家鈺 9 筆、陳鼎勳 6 筆）。

**裁決**：解析器只信 `共有(\d+)筆`，**抓不到那個計數就當失敗並重試**，
不得當成零筆。

另一條實測結論，對 `unknown` 91 筆重跑很重要：

> **簡體寫法在 gpost 全部 0 筆，維基 `insource:` 那邊剛好相反。**
> 兩邊的收字習慣相反，**兩種寫法都必須各查一次**。

### 15.6 字模反查第三次派上用場

`#297 李家銓`：`MAN215` 第 33 槽第三字模與倚天「銓」**byte-exact（0/240）**、
與「鈺」86/240 → 反查無誤，原版確實刻「銓」。

與 `#183 劉志陞`（§11.1）同一個手法、同樣的結論邊界：
**只排除「我們讀錯」，不排除「1992 年打錯字」**，所以仍 `low` + 待考句。

> 三次使用（`#183`、`#297`、以及 §13.1 的效忠領袖表）都指向同一件事：
> **專案內的一手資料能回答外部史料回答不了的問題。**
> 遇到 identity 問題時，先問「遊戲檔案裡有沒有線索」。

---

## 16. 下一階段：`confirmed` 226 筆與 `unknown` 91 筆

| 母體 | 筆數 | 每篇成本 | 主要工作 |
|---|---:|---|---|
| `confirmed` | 226 | 中 | **取捨**（材料多）＋ 查 gpost 補任免令 |
| `unknown` | 91 | 低但不是零 | 重跑 gpost（含異寫與簡繁）＋ 名冊全表反查 |

### `unknown` 那 91 筆的三個已知條件

1. **效忠領袖已經知道**（§13.1）：奉系 27／直系 19／五省聯軍 13，佔 73%
   ——正是 gpost 命中率最低的那幾系，**投入預期要下修**。
2. **「查不到」的可信度低於表面**（§12.1、§15.5）：當初既沒重跑，也未必查過異寫。
3. ⭐ **名冊全表可以反向用**（batch09 的建議）：
   〈保定陸軍軍官學校〉〈黃埔一期〉〈青天白日勳章授勳人員列表〉
   〈國民革命軍集團軍序列〉〈抗戰勝利勳章〉〈陸軍大學〉——
   拿 91 個姓名批次比對，成本是一次下載，
   **是唯一「不需要先有線索」的路徑**。

**建議先做 `unknown`**：它有明確的方法（名冊反查），而且做完才知道
`02-status.md` §1 的三段表要不要重寫。

---

## 17. `unknown` 池第一批（U3）的裁決 — **2026-08-02**

孫傳芳系＋黔滇川＋無領袖，24 個槽位。品質閘四項全過（23 篇、最長 331.0 格、
相異字 570、不可畫 0、禁用詞 0）。

**23 個真人裡救回 15 個（65%）**，`high` 2／`medium` 11／`low` 2／`unknown` 8。
（`#274 無省長` 是 UI 佔位詞不是人物，依 §5.3 不建骨架——`02-status.md` §V3 的建議由此坐實。）

### 17.1 ⛔ 我在 §13.1 下修的預期，**對這一組是錯的**

§13.1 裁決 1 寫「`unknown` 91 筆的命中率合理預期落在 25% 那一端」。
**實測 74%（17/23）。**

錯在哪很明確，而且承辦者已經指出來了：

> 差別在**母體**不在運氣。孫傳芳系多出身陸軍第二師與江蘇、安徽地方陸軍，
> 是**走《政府公報》正式令的正規序列**；batch06 測的直魯／奉系旅團級本來就不走。

**裁決**：§13.1 的 25% 預期**只適用於直魯／奉系旅團級**，不得外推。
剩下 68 筆裡的奉系 27＋直系 19 仍照 25% 估，其餘另計。

> ⚠️ 這是「拿一個子群的觀測去推整個母體」的典型錯誤。我當時有 batch05／batch06
> 兩批直魯與奉系的資料，就把它當成 `unknown` 全體的特性——
> 而那 91 筆的組成本來就不同質（§13.1 自己那張表就寫著）。

### 17.2 ⭐⭐ `G-U3-2`：**日期完全一致，反而是同源的訊號**

§15.1 剛裁決「兩個來源對**同一個事實**都有記載才算互證」。U3 指出一個反例：

> `#129 李生春` 的兩筆逐日相合，看起來是最強的互證——
> 實際上**兩份都在轉載同一份《政府公報》**。

承辦者提出一個可機械判斷的訊號：

> **日期精確到「日」而且完全一致 ≈ 同源轉載。**

**裁決**：§15.1 補一條——**兩個來源對同一事實給出完全相同的精確日期時，
先假設同源**，要另找「其中一方有獨立資訊」的證據才算互證。

反過來說：**互證最強的形狀是「同一件事、不同粒度、對得起來」**
（如一方給日期、另一方給番號與地點，而兩者不矛盾）。

### 17.3 ⛔ `G-U3-3`：§11.3 的「同日成批受令」**假陽性率遠高於預期**，降級

我在 §11.3 把它採納為**正向判準**，依據是 batch05 在直魯聯軍身上用得很準。
U3 的實測：**6 次觸發只有 1 次同軍系**——純敘階的大批令一天跨數省數十人。

**裁決**：**從「判準」降為「線索」。** 同日受令**不足以**把兩批記載接成一人，
只能用來提示「去看看這批人是不是同一個系統」。

> 這是我第二次因為單一批次的成功就把某條啟發式升級
> （第一次是 §13.1 的 25%）。**兩次都是樣本不足。**
> 往後採納新判準前，要先問「它在哪種母體上被驗過、那種母體有多普遍」。

（§13.2 已裁決它撞上官階倒序時讓步；現在它連獨立成立的資格都沒有。）

### 17.4 `G-U3-1`：§10.3 與 §14.5 在同一頁面上打架

§10.3 說「消歧義頁與列表頁不計 tier」，§14.5 說「Tier B 的判準是能不能追到已出版來源」
——一個列表頁若列出已出版書目，兩條給出相反答案。

**裁決**：**§10.3 優先。** 頁面**型別**先判（消歧義／列表／紅連結 → 不計 tier），
再判引註品質。理由：列表頁的問題不是來源品質，是**它不保證所列各項指同一人**
——那是 identity 風險，不是可信度風險，追得到書目也解不掉。

### 17.5 其餘三條，照辦

| 編號 | 內容 | 處置 |
|---|---|---|
| `G-U3-4` | §4.4 缺「同一人在**不同事件**上番號不同」這一格 | 不是衝突，是**部隊改編**。記進 `posts` 的時序，不進 `conflicts` |
| `G-U3-5` | Tier C 沒分級，漏掉「提供可被 Tier A 印證的**具體識別碼**」這種高價值用法 | 照辦：Tier C 若提供番號／日期等可被 Tier A 驗證的識別碼，**該識別碼的效力隨印證它的 Tier A 走** |
| — | 北洋公報的兵種異體字 **`礮`／`礟` 編不進 Big5** | 加進 `DESIGN-20` §5.4 的正規化清單（→「砲」）。⚠️ 現有清單只列簡體與日式殘留，沒有這一組，而任免令一多必然撞到 |

### 17.6 `S-ROSTER` 值多少：79% 對 0%，但**因果方向未定**

| 組別 | 人數 | 救回 | 救回率 |
|---|---:|---:|---:|
| 有效忠領袖 | 19 | 15 | **79%** |
| 無領袖（`+14 = 0`）| 5 | 0 | **0%** |

承辦者沒有把這個落差直接當成「`S-ROSTER` 很有用」的證據，而是寫下：

> **因果不能反著讀。** 更可能的解釋是：**遊戲作者對這 4 人也沒有資料，
> 所以既沒給勢力也沒給省份**——`+14 = 0` 本身就是「作者也不知道他是誰」的副產品。
> 若是如此，`S-ROSTER` 的貢獻不是「提供線索」而是「忠實反映資料稀薄度」。

**兩種解釋都與觀察相容，本輪分不開。** 這個判斷是對的，照原樣記著。

### 17.7 ⭐ 字模反查第四次用上：**排除「我們讀錯」**

U3 把 24 個槽位共 **72 個字模**全部跑過倚天反查，**每一個都只有唯一候選**。

> 所以那 8 筆查無**不是把字讀錯造成的**。剩下只有兩種可能：
> 史料真的沒有，或 1992 年原版打錯字——後者任何外部來源都答不了。

與 §11.1（`#183`）、§15.6（`#297`）、§13.1（效忠領袖表）合起來，
**專案內一手資料已經是這條線上的常規工具**，不是偶爾想到才用。

---

## 18. `unknown` 池 U2 的裁決 — **2026-08-02**

直系（吳佩孚）19＋晉綏 2＋西北軍 1，22 人。品質閘四項全過。
**16 人成篇（73%）**，`high` 2／`medium` 9／`low` 5／`unknown` 6。

### 18.1 ⛔ `G-U2-3`：**閘本身與規格衝突**，而且是我寫的

`tools/bio_gate.py` 第 3 項要求「篇數 == id 數」，
**與 §3.2「`unknown` 不寫小傳」直接衝突**。

前九批母體是 `partial`、10 人 10 篇，所以從沒浮現；
**`unknown` 池是第一個必然撞到的母體**。

⭐ 承辦的 agent **選擇回報而不是改我的工具**（那超出它的派工邊界），
並用「一行狀態說明」讓閘通過、正文一字未編。**兩個判斷都對。**

**已修**：第 3 項改成

```
立傳篇數 == confidence != "unknown" 的筆數
且 unknown 的 bio_zh 必須是空的      ← 新增：查不到就是查不到，不准補話
```

十一批一次跑完全部通過。

### 18.2 ⭐⭐ §13.1 的預測變數**選錯了**——正確的不是「哪一系」

§17.1 我把「25% 命中率」的適用範圍縮到「直魯／奉系旅團級」。U2 給出更好的變數：

> **不是「哪一系」，而是「這個人在不在北洋正規建制的敘階序列裡」。**
> 毅軍、將軍府、陸軍師這類**正規建制**走《政府公報》；
> 直魯聯軍那種**臨時編組**才不走。

這解釋了兩件我原本歸給「系別」的事：U2 的直系 73%、U3 的孫傳芳系 74%
——兩者都是正規建制；而 batch06 的直魯聯軍 25%、batch05 的 `#180` 全空，
是臨時編組。**同屬奉系的正規部隊（東北陸軍）與直魯聯軍，命中率會差很多。**

**裁決**：§13.1 裁決 1 改寫——預測變數是**建制性質**，不是派系。
剩下的 U1（張作霖 27 人）要分開看：**東北陸軍正規序列**與**安國軍／直魯聯軍臨時編組**
不是同一群。

### 18.3 `G-U2-2`：「查到了但全排除」要與「真的查不到」分開

U2 的 6 個 `unknown` 裡，**2 個是「查到了但全數排除」**
（`#76 郭振甲` 官階倒序、`#84 李鵬` 15 筆分三群橫跨 68 年且全屬文職）。

兩者在 `people.json` 裡都寫成 `unknown`，**讀起來一樣但重跑成本差很多**
——後者已經證明「這個名字在 gpost 裡的東西都不是他」，重跑只是白花。

**裁決**：`facts.json` 加布林欄位 `all_candidates_excluded`。
與 §15.3（不計 tier 的來源也要登）同一個道理：**把「已經查過」保留下來。**

### 18.4 `G-U2-1`：同日受令的但書

§17.3 已把它從判準降為線索。U2 再補一條可機械判斷的但書：

> **同日受令者階級差距超過兩級時不觸發。**
> 反例：1913-11-30 `#89`（少將）與 `#88`（步兵中尉）同日。

### 18.5 ⛔ 派工單的一個錯誤：河南是 **17 人不是 14**

我在 U2 的派工 prompt 裡寫「這 22 人裡有 14 個所屬省都是 19（河南）」——
**實際是 17 人**（`#76`–`#96` 全部）。我目測名單時數錯了。

承辦者不但訂正，還**驗證了省編號的索引基準**（用 `#164 何光烈` 省 29→四川／劉湘、
`#273 唐繼麟` 省 34→雲南／唐繼堯 反查），這正是 `CLAUDE.md` §7 第 3.5 條
「省編號一律從資料取」的正確做法。

### 18.6 ⭐ 那 17 個河南人**不是同一支部隊**——而且結論是可證偽的

我在派工單裡猜「他們很可能是同一支部隊的軍官群」。U2 的答案是**不是**，
至少分屬四個獨立單位（毅軍、河南陸軍第三師、1927-05-01 被收編的豫軍各旅、豫軍萬選才部），
只是被 1927 年 3 月奉軍入豫繳械開封毅軍、5 月收編剩餘豫軍這兩件事掃進同一段歷史。

⭐ 更值得記的是它給出了**可證偽的下一步**：

> 抓《政府公報》**1926-10-16 當日的完整受令名單**。
> 若結論成立，應看到一批集中在河南、同一保薦系統的軍官，且 `#88`／`#90`／`#91` 在其中
> ——**做不出來就等於推翻這個結論**。

這是本專案在史料側第一次寫出**帶否證條件**的下一步，值得當成範例。

### 18.7 字模反查第五次，用法是新的：**驗異寫查詢有沒有漏**

前四次都是「驗證我們有沒有讀錯字」。U2 反過來用：
把六組候選異寫（甲/才、閻/閰＋日/曰、峰/鋒、迺/乃、運/連、鳳/風）
全部拿去比對——**六組全部 byte-exact（0/240）**，對照字漢明距離 36–95/240。

> 也就是說：**遊戲刻的就是那六個字，異寫查詢的範圍沒有漏。**

結論邊界不變（只排除「我們讀錯」，不排除「1992 年打錯字」），
但這個用法把「異寫要各查一次」從**沒有終點的窮舉**變成**有界的清單**。

---

## 19. `unknown` 池 U4 的裁決 — **2026-08-02**

中央軍 8＋抗戰兩期 10，18 人。品質閘四項全過。**救回 15 人（83%）**，
`high` 4／`medium` 4／`low` 7／`unknown` 3。

83% 不能外推——本批是**刻意挑的**對照組（§13.1 裁決 3）。
但它證實了 §18.2 的新變數：中央軍與抗戰期都在正規敘階序列裡。

### 19.1 ⭐⭐ `G-U4-1`：§4.7 是「**超長時的砍法**」，不是「收錄門檻」

§4.7 的取捨軸是遊戲期別，§11.7 的 `G-03-5` 又裁決「期外材料不進正文」。
兩條合起來在 `unknown` 母體會**把小傳清空**——`#35 郭思演` 與 `#242 莫希德`
的材料**一筆都不在北伐時期**，而 `#35` 還是 `high`。

**裁決**（承辦者的判定，採納）：

> **§4.7 是「篇幅超過 340 格時砍什麼」的優先序，不是「哪些材料可以收」的門檻。**
> 期外材料**可以進正文**，但要標明時序；只有在需要砍的時候才優先砍它。

§11.7 的 `G-03-5` 隨之改寫：**期外材料先寫進正文並標時序，砍的時候第一個砍**。

> ⚠️ 這是規則互相作用產生的缺陷，兩條單獨看都合理。
> 與 §14.4 的三次「做對事反而被扣分」同型——**規則交互作用要另外驗**。

### 19.2 ⭐⭐ `G-U4-3`：§12.1 的「零命中重跑」不夠——**重跑要換寫法**

> **同一個查詢重跑只排除得了限流，排除不了收字習慣。**

U4 實測四次：`#248`／`#366`／`#393`／`#417` **繁體 `insource:` 0 筆、簡體才有**。
U2 也有五個同型（`陳德麟`／`王都庆`／`刘自珍`／`王相贤`／`马文德`）。

**裁決**：§12.1 補完整——

```
查詢回零筆 → 重跑一次（排除限流）
           → 換寫法再查（簡繁、異寫、gpost 與維基兩邊的收字習慣相反）
兩者都做完才能寫「查無」
```

⚠️ 這直接影響 `02-status.md` §1 那張三段表的可信度：
**當初的 91 筆 `unknown` 兩件事都沒做。** U2、U3、U4 合計 63 人裡救回 46 人（73%），
就是最直接的證據。

### 19.3 ⭐ 名冊全表反查**值得推廣**，而且補的正是 gpost 補不到的欄位

U4 的實測：**13 筆事實，成本 7 次下載**。

| 名冊 | 貢獻 |
|---|---|
| 保定軍校 | 學歷 3 |
| 日本士官學校 | 學歷 2 + **籍貫 2** |
| 陸軍大學 | 1 |
| 抗戰勝利勳章 | 2 |
| 集團軍序列 | 2 |
| 1938 戰鬥序列 | 1 |
| **青天白日勳章授勳人員列表** | **零命中 → 下批可跳過** |

> ⭐ 關鍵不是數量，是**互補性**：本批**明確籍貫 2/2、學歷 6/6 全部來自名冊，
> gpost 貢獻 0**。成本是常數、不隨人數增長，而且**不需要先有線索**。

**限制**：名冊全屬 Tier C，只能支撐**欄位級** `low`，不能靠它升 `confidence`。

**裁決**：名冊反查列為 `unknown` 池的**標準第一步**（U1 之後的批次照做），
並把「哪些名冊零命中」記進 §19.5 的 `coverage`。

### 19.4 `G-U4-5`：`S-ROSTER` 的**順序**比 §11.6 想的更有用

`MAN115` 的排列與維基〈國民革命軍北伐〉1926 年戰鬥序列**高度平行**：
`#244`–`#248` = 第七軍第一至第四旅旅長、`#41`–`#43` = 程潛／鄧彥善／胡謙
（第六軍軍長與兩師長）、`#12`–`#14` = 錢大鈞／蔡仲笏／譚延闓。

**4 筆 identity 推定有 3 筆靠它找到人選，而且一個字都沒進正文。**

**裁決**：§11.6 補一句——**名冊順序可用來推「該去哪一份編制表找」**。
「不得進正文」的限制**不變**。

### 19.5 其餘兩條

| 編號 | 處置 |
|---|---|
| `G-U4-4` | §12.8 只給 `confidence` 上限未給下限。裁決：分群後 gpost 全排除、正文只剩 Tier B 時，**下限是 `low`**——有 Tier B 撐著就不是 `unknown` |
| `G-U4-6` | `facts.json` 加**批次級 `coverage` 欄**，記「某名冊全表比對零命中」這種**有價值的負面結果**。與 §15.3、§18.3 同一個道理：**把「已經查過」保留下來** |

### 19.6 字模反查第六次，**兩次直接改變方向**

- `#303`：與「鄧寶珊」漢明距離 **106/240** → 否掉最直覺的猜測
- `#416`：確認第二格是**全零填充格**，「博郎」是完整兩字姓名，不是漏字

> U4 把它從「偶爾用的技巧」升格成**標準第一步**。這與 §17.7、§18.7 一致
> ——`unknown` 母體先要證明「遊戲寫的那三個字查不查得到」，
> 而那件事只有專案內的一手資料答得了。

---

## 20. `unknown` 池跑完（90/90）與 U1 的裁決 — **2026-08-02**

奉系 27 人，品質閘四項全過，救回 15（56%），**gpost 命中 19/27（70%）**。

### 20.1 ⛔ 結算：**「91 筆查不到」有三分之二是假的**

| 批次 | 母體 | 真人 | 救回 | 比例 |
|---|---|---:|---:|---:|
| U1 | 奉系 | 27 | 15 | 56% |
| U2 | 直系＋晉綏 | 22 | 16 | 73% |
| U3 | 五省聯軍＋黔滇川 | 23 | 15 | 65% |
| U4 | 中央軍＋抗戰期 | 18 | 15 | 83% |
| **合計** | | **90** | **61** | **68%** |

（`#274 無省長` 是 UI 佔位詞，不計。）

**自撰累計 190 篇**：`high` 55／`medium` 73／`low` 33／`unknown` 29。

> ⛔ **`02-status.md` §1 的三段統計表就此作廢。** 原本記 91 筆 `unknown`，
> 實際只有 **29 筆**是真的查無——**三分之二是查法造成的**。

原因在 §19.2 已經定案，這裡只補一句：**兩件事都沒做**
——零命中沒重跑（限流偽裝成查無）、沒換寫法（簡繁與異寫）。

⛔ **我在 §13.1 下修的 25% 預期，四批全部打臉**（56/73/65/83%）。
§17.1 我把它縮到「直魯／奉系旅團級」，§18.2 又改成「正規建制 vs 臨時編組」
——**連 U1 的奉系都有 70% 命中**。真正的原因不是母體特性，是**原始結論本身不可信**。

> 三次修正、三次都在找「哪一群比較難查」，而**真正的變數是「當初查得夠不夠仔細」**。
> 這是把**測量誤差**當成**母體特性**的典型錯誤。

### 20.2 ⭐⭐ §11.3 的「同日成批敘任」——**要跟番號綁在一起才有力**

§17.3 我依 U3 的假陽性率把它從判準降為線索。U1 給出它**決定性生效**的一次：

> `#211 李振聲`／`#217 張作舟`／`#223 常堯臣` 在 **1918-11-27 同日受
> 同一個「陸軍第二十七師」（奉天部隊）的任命**，又在 1927-08-06 同日受敘階令。

對照 U3 的假陽性（6 次只有 1 次同軍系）——差別很清楚：

**裁決**：

| 形狀 | 效力 |
|---|---|
| 同日 + **同一個部隊番號** | **判準**（可據以聚群）|
| 同日，純敘階大批令 | **線索**（一天跨數省數十人，§17.3）|
| 同日但階級差距超過兩級 | **不觸發**（§18.4）|

⭐ 這條的價值在於**不需要知道這些人是誰**——純靠日期與番號把散落的任免令聚成群。
U1 就是這樣聚出六組。

### 20.3 `G-U1-2`：名冊反查的命中率**取決於頁面，不取決於人**

U1 的實測把這件事釘死了：

| 頁面型別 | 例 | 命中 |
|---|---|---:|
| **編制序列型**（有完整名單）| 〈直鲁联军〉1926 序列、〈东北军〉1931 編制表、〈张作霖〉師旅表 | **8** |
| 敘事型 | 〈奉系〉〈安國軍〉 | 0 |
| **只列著名學員** | 〈東北講武堂〉（只有 6 人）| **0** |
| **條目不存在** | 〈國民革命軍集團軍序列〉 | — |

**有效命中 8/27（30%），撈到的全是旅長以上、gpost 只查得到敘階查不到職務的人**
——正好補 gpost 的盲區（與 §19.3 的結論一致）。

⛔ **後兩列是我的派工單寫錯**：〈東北講武堂〉零命中**什麼都不代表**（它沒有名冊），
〈國民革命軍集團軍序列〉**條目根本不存在**。我把兩者都當成「該查的名冊」列進 prompt。

**裁決**：名冊反查照做，但**先確認那一頁有沒有完整名單**；
挑**編制序列型**頁面，不是機構的敘事條目。**零命中要先分「沒名單」與「有名單但沒這個人」。**

### 20.4 `G-U1-6`：`礮`／`礟` 是**規則交互作用**的第三例

兩個字**連 Big5 都編不出來**，而且來自 **Tier A 的原始公文用字**
——照 §4.2「職務用當時的正式機構名」寫，就會把它們帶進來。

與 §19.1（§4.7 × `G-03-5`）、§14.4（三次「做對事反而被扣分」）同型：
**兩條規則各自都對，交互作用產生缺陷。**

**裁決**：§4.2 補一句——**正式機構名裡的不可畫異體字要正規化**
（`礮`／`礟` → `砲`），並在 `facts.json` 的欄位註記原始用字。
已加進 `tools/bio_gate.py` 的已知清單與 `DESIGN-20` §5.4。

### 20.5 其餘四條

| 編號 | 內容 | 處置 |
|---|---|---|
| `G-U1-1` | `unknown` 池需要「寫不寫傳」的判準 | 採納 U1 的自訂法：**分群後無一群落在該期別的時間窗、且無該系統番號 → `unknown`** |
| `G-U1-3` | 「命中數很大但可用為零」（`#227 長興` 1,143 筆**全是地名**）沒有位置放 | 併進 §19.5 的 `coverage`：記「命中數／可用數」與原因 |
| `G-U1-4` | 異寫查到確有其人且系統相符的候選怎麼收（孫德全／孫德荃等三例）| **一律不採用**（U1 的做法正確），但寫進 `gaps` 當下一輪的起點 |
| `G-U1-5` | `highest_post` 只有敘階可填時 | 填敘階（如「陸軍少將」），並在欄位註明「僅有敘階、無職務」|

### 20.6 下一輪最值得追的三條（U1 留下的）

1. **異寫「孫德荃」**（`#212`）：gpost 5 筆 + 1931 東北軍編制表「独立第十九旅旅长孙德荃」，只差一個字。
2. **異寫「陳玉昆」**（`#222`）：1926–27 奉軍東北陸軍第九旅旅長，與 `#211` 同屬第六方面軍團。
3. **`#226 吳泰來` 的「督辦黑龍江軍務」**：〈黑龙江行政长官列表〉把他列在吳俊陞與萬福麟之間，
   但列表頁依 §10.3 不計 tier——**找到帶引註的來源就會改寫這一篇的評等**。
