# 機制：戰鬥

推論等級的定義見 `00-index.md`。

> **這一份幾乎全是未知。** 戰鬥系統是 M3 最大的一塊，也是目前最沒進展的一塊。
> 不要因為社群資料寫得很具體就當成事實——`CLAUDE.md` §4 的 oracle 優先序裡
> 社群資料是最低位階，而且這款遊戲三十年來有大量修改版流傳。

---

## 1. 已知的硬事實

### 戰場是每省一張 14×14 格 — **confirmed**

三個檔案各管一件事，見 `docs/spec/01`（地名）與 `docs/spec/04`（地物）：

| 檔案 | 每格存什麼 |
|---|---|
| `WARPOS.DAT` | 屬於哪個鄰省（0 = 本省腹地）|
| `TERNAME.DAT` | 地名索引，指向 `TN15.<省>` |
| `NWMAP.DAT` | 地物編號，決定畫哪張圖塊 |

### 戰鬥狀態記錄 469 bytes — **confirmed（結構）／未知（語意）**

`MEM_WAR.DAT` 與 `SAVE(N).DT2` 同結構，見 `docs/re/05`。
10/10、20/20、200/200 三組成對，**很可能是攻守雙方各一份**。

⚠️ 不能拿 `MEM_WAR.DAT` 當戰前基準去 diff 部隊欄位——**8 個省有未初始化的殘料**，
緬甸那筆裡甚至是 8086 機器碼。

### 戰鬥觸發在主迴圈 — **confirmed**

見 `70-ai.md` §2。三個 OR 條件，任一成立就打。

### 戰鬥模組的位置 — **confirmed**

`sub_39B6E`，主迴圈直接呼叫。生命週期是
載入地圖 → 載入戰鬥狀態 → （主迴圈）→ 寫回戰鬥狀態，見 `docs/re/04`。

`+18F` 到 `+6F4` 之間是戰鬥主迴圈，裡面有 `KEYPRESSED`（輸入）與
`SETACTIVEPAGE`（雙緩衝）。

---

## 2. 六角格 — **假說**

社群資料說「戰鬥用六角格，數字鍵 1–6 對應六個方向」。

目前唯一的**實物旁證**是 `NEWICON.TPC` 的飛機圖示有**六個朝向**
（`docs/formats/05` §3），綠紅各一組。六個朝向配六個方向是合理的，
但那也可能只是動畫幀。

⚠️ **戰場資料是 14×14 的方格**，不是六角。若戰鬥真的用六角，
那是在 14×14 的資料上做六角鄰接（奇偶列偏移），還是另有一張地圖，
**未知**。這是矛盾點，不要跳過。

---

## 3. 全部未知的部分

| 問題 | 下一步 |
|---|---|
| 移動與機動力 | 讀 `sub_39B6E` 的 `+18F`–`+6F4` |
| 地形對防禦的修正 | 同上；地物編號已解（`docs/spec/04`），找誰讀它 |
| 戰損公式 | 同上 |
| 每方最多幾支部隊 | 469 B 記錄的槽數是 10，但未證實 |
| 部隊的 20 B 欄位 | **要打進一場實際戰鬥取 `.DT2`** |
| 勝負判定 | 見 `80-victory.md` |

### 最有效的下一步

打進一場實際戰鬥，存檔後 diff `.DT2`：兩個 200 B 區從全 0 變成有值，
直接看出每單位的欄位。這是 oracle 優先序第一位的方法。

卡點是**出兵流程還沒走通**（`20-military.md` §4）。
