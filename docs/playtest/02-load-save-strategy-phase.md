# 載入存檔進政略階段：畫面欄位與規則的第一批硬事實

> 路徑：`play.bat` → 標題 → `2` 載入遊戲 → `1` 進度一 → 密碼 → 政略畫面
> 工具：`tools/dosbox.sh`
> 截圖：`workplace/dosbox/shots/`（**不進版控**，含原版美術）
> 日期：2026-08-01

---

## 1. 防拷密碼：過得去

載入存檔後、進政略畫面前會問：

```
輸入密碼   說明書上
第36頁倒數第2行
第2個字
1. 在   2. 有   3. 上
```

**三選一，選 `1` 通過。**

這解除了 `CLAUDE.md` §7 第 11 條的風險（大富翁2 就是卡在密碼畫面，
DOSBox 實測整條線停擺）。這款遊戲的密碼是**三選一**，最差也只要試三次。

⚠️ **只試了 `1` 這一個選項就通過，沒有驗證「是不是隨便選都過」。**
要確認得再跑 `2` 和 `3` 看會不會被擋。在確認之前，不要斷言「密碼形同虛設」。

密碼問的是說明書第 36 頁的內容，我們沒有說明書。若日後要自動化驗收管線，
兩條路：暴力試三次，或從 `WAR.EXE` 找答案表（驗證邏輯一定在執行檔裡）。

---

## 2. 政略畫面的欄位

左側面板（湖北省，載入 `SAVE(1)` 的狀態）：

| 標籤 | 7 月 | 8 月 | 備註 |
|---|---:|---:|---|
| 黃金 | 4200 | 4150 | |
| 糧食 | 18050 | 17950 | |
| 彈藥 | 8787 | 14787 | |
| 燃料 | 12048 | 12048 | 未變 |
| 煤礦 | 13000 | 7000 | |
| 鐵礦 | 14031 | 8031 | |
| 地價 | 22 | 22 | 未變 |
| 人口 | 1825 萬 | 1825 萬 | 單位是「萬」|
| 城市數 | 5 | 5 | |
| 兵工廠數 | 3 | 3 | |
| 兵力 | 97500 | 97500 | **超過 u16 上限** |
| 將領數 | 15 | 15 | |
| 人民忠誠度 | 79 | 79 | 0–100 |

頂端另有：省名、狀態（`正常`）、司令（`吳佩孚`）、省長（`吳佩孚`）。
右側直排：`民國 15 年 8 月 秋 第 26 省`。

這 13 個標籤全部出自 `2.15`／`3.15` 的詞表
（`docs/formats/01-glyph-text.md` 還原出來的那批），互相印證。

---

## 3. 三件規則層面的硬事實

### 3.1 省編號 = `3.15` 的索引 + 1

畫面顯示「第 **26** 省」，而湖北省在 `3.15` 的 39 個省名裡是第 26 個（0-based index 25）。
**省編號是 1-based，順序就是 `3.15` 的順序。**

這連帶確認 `TN15.N` 的 N 就是省編號（`TN15.26` 應該是湖北的地名表——
實際內容是「漢口 武昌 漢陽 襄陽」，全在湖北 ✓）。

### 3.2 政略指令有 15 個

```
司令，請下命令？(1-15)
```

`CLAUDE.md` §1.5 的社群資料說「命令 5 是徵兵」，**指令總數 15 是這次實測到的**。
15 個指令各是什麼還沒逐一試。`2.15` 的詞表裡有
`調動 軍事 運補 徵稅 徵兵 查閱 開發 政策 外交 談判 秘密 商業 練兵 休息 特殊 其他`
——**這 16 個詞看起來就是指令選單**，但**數量對不上（16 vs 15）**，
不要直接對號入座。

### 3.3 命令數上限確實存在

```
指令數：2
```

`CLAUDE.md` §1.5 記「每個省份有命令數上限，用完換下一個省份」，
標為社群資料、待驗。**現在確認機制存在**，畫面右下角就顯示剩餘指令數。

上限怎麼算（固定值？依省份？依將領？）**未知**。

### 3.4 兵力超過 u16

`兵力 97500` 超過 65535，所以存檔裡不是單一 u16。
可能是 u32、或是各部隊兵力的加總（顯示時才算）。**未確認。**

---

## 4. 存檔對照：資源在 `.DT1`

拿畫面上的 7 月數值去搜 `SAVE(1).DT1`，找到**連續排列**的欄位：

| offset | 值 | 欄位 |
|---:|---:|---|
| 929 | 4200 | 黃金 |
| 931 | 18050 | 糧食 |
| 933 | 8787 | 彈藥 |
| 935 | 13000 | **煤礦** |
| 937 | 12048 | **燃料** |
| 939 | 14031 | 鐵礦 |

全部是 u16 little-endian，連續 6 個。

### [雷] 資料順序與畫面顯示順序不同

畫面是「燃料、煤礦」，資料是「煤礦、燃料」——**第 4、5 個欄位對調**。
remake 的 UI 要照畫面順序排，但解碼要照資料順序讀。

### 每省的記錄大小還沒定出來

`SAVE(1).DT2` 是乾淨的 39 × 469（`docs/re/03`），但 `.DT1` 不是：
14,683 不能被 39 整除。試過「湖北 index 25、黃金在記錄開頭、定長記錄」
的假設，掃 N = 20…78 都沒有給出合理的 39 省黃金分布。

可能的原因（**都未驗證**）：`.DT1` 有檔頭、記錄不定長、
或黃金不是記錄的第一個欄位。要拿第二個省的實際數值來定間隔——
下一輪切換省份再截一次圖就能解。

---

## 5. 下一步

1. 切換省份，拿第二組數值定出 `.DT1` 的記錄間隔。
2. 逐一試 15 個政略指令，對照 `2.15` 的詞表。
3. 確認密碼是不是隨便選都過（試 `2` 和 `3`）。
4. 命令數上限的計算方式。
5. `兵力 97500` 在存檔裡怎麼存。
