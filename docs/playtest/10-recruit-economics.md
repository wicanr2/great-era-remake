# 徵兵的成本與上限：四兵種匯率

日期：2026-08-01
路徑：政略指令 **5 徵兵** → 1 徵兵 → 1 徵步兵 → 輸入數量
存檔：進度一（`SAVE(1)`），載入後過了一個月

`docs/playtest/09` 的端到端實跑暴露「規則層只有消耗沒有補充」。
徵兵是原版五條補充管道裡最直接的一條，這一輪把它量出來。

---

## 1. 選單結構

```
指令 5「徵兵」
  ├ 1. 徵兵          → sub_29B46
  └ 2. 重新整編      → sub_29DDF
       ↓（選 1）
  1. 徵步兵    2. 徵裝甲兵
  3. 徵砲兵    4. 徵騎兵
       ↓
  司令，欲徵多少步兵？ (0-41500)
       ↓（輸入 1000）
  共須黃金 100
  司令，您確定嗎？ (Y/N)
```

**「重新整編」**印證了 `CLAUDE.md` §1.5 的社群說法
「另有整編可在部隊間分配兵力」——它確實存在，是徵兵選單的第二項。

⚠️ 兵種的**選單順序**（步兵／裝甲兵／砲兵／騎兵）與**兵種編號**
（步 1、砲 4、裝甲 5、騎 6）不同。UI 用前者，規則層用後者。

---

## 2. ✅ 步兵 10 人 = 1 黃金 — confirmed（兩個樣本 + 一次計算）

| 省 | 黃金 | 畫面的上限 | 比值 |
|---|---:|---|---:|
| 湖北（26）| 4,150 | `(0-41500)` | **×10** |
| 河南（19）| 5,950 | `(0-59500)` | **×10** |

再輸入 1,000 步兵：

```
司令，欲徵多少步兵？   (0-59500)      1000
共須黃金  100
```

`1000 ÷ 10 = 100` ✓

**兩個獨立樣本的上限 + 一次實際計算，零誤差。**

### 對照反組譯

### 2026-08-02 反組譯勘誤：不能把步兵匯率套到所有兵種

四支玩家側函式的整數乘除與省份扣金已逐條讀過：

| 兵種 | 玩家函式 | 黃金決定的上限 | 輸入後成本 |
|---|---|---:|---:|
| 步兵 | `sub_260C7` | 黃金 × 10 | 人數 ÷ 10 |
| 裝甲兵 | `sub_28259` | 黃金 ÷ 10 | 人數 × 10 |
| 砲兵 | `sub_271F8` | 黃金 ÷ 1 | 人數 × 1 |
| 騎兵 | `sub_29494` | 黃金 × 5 | 人數 ÷ 5 |

以上均是**已證實**：同一支函式中可直接連起讀取省份黃金、乘除、顯示成本，
以及尾端扣除黃金的資料流。玩家騎兵的 5 人／金與電腦側 `sub_18AED` 的
2 人／金不同，兩條路徑不可合併成一張共用匯率表。

`sub_28259`（裝甲兵徵兵）末尾：

```asm
mov     ax, [di+7A8Eh]          ; 將領 +17 兵力
...
mov     [di+7A8Eh], cx          ; 寫回兵力
...
sub     [di-6235h], cx          ; 省份 +0 黃金 -= cx
```

減黃金的動作對得上；現在中間的整數乘除也已逐行確認。

---

## 3. 分配規則只解了一半

`sub_28259` 掃該省將領時檢查五個條件：

```asm
cmp     byte ptr [di+7A92h], 5      ; +21 兵種
mov     al, [di+7A81h]              ; +4  所屬省
cmp     byte ptr [di+7A8Dh], 1      ; +16 可用 == 1
cmp     word ptr [di+7A8Eh], 0C8h   ; +17 兵力 vs 200（裝甲兵滿員）
mov     ax, [di+7A8Bh]              ; +14 勢力
```

**「補到滿員為止、只補同兵種」**是清楚的。
沒解的是**補的先後次序**——那會決定誰先補滿。
規則層照將領清單的順序補（攻擊力降序，`docs/re/13` §1），
標為 remake 差異。

---

## 4. 進了規則層

`internal/game/recruit.go`：

```go
const RecruitSoldiersPerGold = 10

func RecruitAffordableLimit(branch, gold) int         // 兵種別匯率
func (w *AIWorld) RecruitLimit(p, branch) int          // min(黃金上限, 部隊缺額)
func RecruitCost(branch, n int) int                    // 兵種別成本
func (w *AIWorld) Recruit(p, branch, n) (int, error)  // 補到滿為止
func (w *AIWorld) RecruitToFull(p, branch) int        // ← remake 的便利函式
```

`RecruitToFull` **原版沒有**（原版一次只徵一個兵種、玩家輸入數量），
它存在只是為了讓 `cmd/aisim -recruit` 的長跑不會枯竭。標為 remake 行為。

---

## 5. 還沒量的四條補充管道

| 指令 | 函式 | 狀態 |
|---|---|---|
| 4 徵稅 | `sub_2C6C6`（440 行）| 未量 |
| 7 開發 | `sub_24D59` | 子選單已知（墾地／建兵工廠／挖金礦），數值未量 |
| 12 商業活動 | `sub_1FA1C` | 未碰 |
| 13 練兵 | `sub_1C916`（262 行）| 未碰 |
| 14 慰勞軍民 | `sub_1C606` | 未碰 |

**手法都一樣**：走到會印數字的那一格，抄下來，對比前後狀態。
徵兵這一輪花的是三次 DOSBox 跑。
