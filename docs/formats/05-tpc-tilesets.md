# TPC 圖集：戰場圖塊、兵種圖示、戰鬥插圖

輸入檔：`NEWTERR.TPC`、`NEWICON.TPC`、`AC.TPC`
相關：`docs/formats/04-tpc-bgi-image.md`（單張 `.TPC` 的格式）
日期：2026-08-01

---

## 1. 圖集分割已驗證

`docs/formats/04` 記過「差額巨大的是圖集」，但逐張分割當時標為未驗證。
現在確認：圖集就是 `[圖][palt 20 B]` 反覆到檔尾，**每張尺寸相同，零剩餘**。

| 檔案 | 大小 | 每張 | 單張 + palt | 張數 | 剩餘 |
|---|---:|---|---:|---:|---:|
| `AC.TPC` | 94,248 | 178×60 | 5,544 | **17** | 0 |
| `NEWICON.TPC` | 5,328 | 32×17 | 296 | **18** | 0 |
| `NEWTERR.TPC` | 8,976 | 32×24 | 408 | **22** | 0 |

三個檔案都整除到 byte，沒有尾料。解碼實作在
`internal/assets/bgi.go` 的 `DecodeBGISet`。

`RAIL.TPC`（8,064 B）**不是這個格式**：檔頭讀出來是 1×1，用同一套規則解不動。
格式未解。

---

## 2. `NEWTERR.TPC` — 戰場地形圖塊（22 張 32×24）

| # | 內容 |
|---:|---|
| 0 | 平原（草地）|
| 1 | 丘陵 |
| 2 | 水（河／海）|
| 3 | 森林 |
| 4 | 村落／城鎮 |
| 5 | 雪山 |
| 6 | 沙漠／黃土 |
| 7 | 橋（縱向，跨水）|
| 8 | 橋（橫向，跨水）|
| 9 | 岩地／荒地 |
| 10 | 關隘城門（紅門建築 + 城牆）|
| 11–17 | 城牆：四種彎角、橫向、兩種斜坡 |
| 18–19 | 道路／鐵路（縱向）|
| 20 | 城牆（斜）|
| 21 | 關隘城門（另一款）|

一整套城牆圖塊（橫、縱、彎角、斜坡）加關隘，是長城的組件。

### ⚠️ 22 張與地名索引的 0–22 沒有關係

`TERNAME.DAT` 的值域上限 22 是「地名最多的省有 22 個地名」，
與這裡的 22 張圖塊是巧合（`docs/spec/01` §4 的訂正）。

**「哪個地名畫哪一張圖塊」的對照表還沒找到**，所以上表的編號語意是
從圖像本身讀出來的，不是從程式邏輯驗來的。畫戰場之前必須先解出那張表。

---

## 3. `NEWICON.TPC` — 兵種圖示（18 張 32×17）

綠、紅成對，兩色應該就是敵我兩方：

| # | 內容 |
|---:|---|
| # | 內容 | 兵種代號 |
|---:|---|---:|
| 0 / 1 | 鋼盔 綠／紅 | **1 步兵** |
| 2 / 3 | 戰車 綠／紅 | **5 裝甲兵** |
| 4 / 5 | 馬頭 綠／紅 | **6 騎兵** |
| 6–11 | **大砲**，綠，六個朝向 | **4 砲兵** |
| 12–17 | **大砲**，紅，六個朝向 | **4 砲兵** |

### [訂正] 6–17 不是飛機，是大砲

原本記成「飛機，六個朝向」。放大渲染之後看得很清楚：**砲管、輪子、砲架**
都在，是側視的野戰砲。

這同時解掉了原本記的疑問「沒有明顯的炮兵圖示」——**它就在那裡**，
只是被誤認成飛機。四個兵種的圖示現在全部對上：

- 步兵（滿員 20,000）→ 鋼盔
- 裝甲兵（滿員 200）→ 戰車
- 騎兵（滿員 10,000）→ 馬頭
- 砲兵（滿員 2,000）→ 大砲

兵種代號的證據見 `docs/spec/02` §6（四個滿員數與社群說法一起對上）。

### 六個朝向是砲兵的射擊方向

砲兵是**遠程兵種**（`docs/re/09`），而執行期記錄的 `+31` 是決定
攻擊範圍的參數。六個朝向的圖示配上六角格的六個方向，
強烈暗示 **`+31` 是朝向（1..6）而不是射程**——第一期 274 筆全是 1，
就是「初始都朝同一個方向」。

⚠️ 仍未證實：`sub_58854`（494 行，產生格子清單的那支）還沒讀完。

---

## 4. `AC.TPC` — 戰鬥場景插圖（17 張 178×60）

騎兵衝鋒、步兵推進、砲擊、城鎮巷戰之類的橫幅插圖，應該是戰鬥過程中的
過場演出。內容尚未逐張比對實機。

**配色需要各自的 `palt`**：用 EGA 預設調色盤渲染出來顏色是錯的。
每張圖後面那 20 bytes 就是它自己的調色盤映射，這正是 `palt` chunk 存在的理由。
`palt` 的 16 個值是 VGA DAC 索引（0–63），還要配合 `.RGB` 檔設定的 DAC
才能得到真實顏色——這條鏈還沒接起來。

---

## 5. 未解

- `RAIL.TPC` 的格式（8,064 B，檔頭解出 1×1）
- 地名 → `NEWTERR` 圖塊的對照表
- `palt` 的 DAC 索引要配哪一個 `.RGB` 檔
- `AC.TPC` 17 張各在什麼時機播放
- 炮兵的圖示在哪
