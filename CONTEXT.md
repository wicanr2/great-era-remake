# CONTEXT — 專案脈絡與文件索引

> **這份是全專案的單一入口。** 對話被壓縮、或換一個新 session 接手時，先讀這份，
> 就能重建完整全局，再依索引跳到需要的文件。硬規則與工作紀律在
> [`CLAUDE.md`](./CLAUDE.md)。
>
> 最後更新：2026-08-01（M0 完成、省份資料表全解、`TERNAME` 訂正為地名索引、戰場圖塊全解）
>
> ### ⚡ 每一輪都要做的事：機制歸檔
>
> **解出任何遊戲機制，當場寫進 `docs/mechanics/` 對應的屬性檔**
> （`CLAUDE.md` §6.5）。碰到**電腦 AI 的判斷邏輯**一律進
> [`docs/mechanics/70-ai.md`](./docs/mechanics/70-ai.md)——
> 那是 remake 完成後做優化、改難度、強化電腦對手的唯一依據，漏掉就得重挖一次。
>
> 只要看到這些，就是該記的時候：電腦選指令的優先序、出兵目標的挑選、
> 派將規則、撤退／求和門檻、**任何帶 `Random` 的判斷**、難度參數。

---

## 1. 這個專案在做什麼

把漢堂國際資訊《大時代的故事》(1992, DOS) 完整逆向，在 Go / Ebiten 上乾淨重寫引擎，
還原原版繁中文本並另做英文與日文語系。定位是文化資產保存。
目標、原則、里程碑見 `CLAUDE.md`；原作背景與外部資料索引見 `CLAUDE.md` §1.5。

---

## 2. 現況一覽

| 領域 | 狀態 |
|---|---|
| 原版素材 | ✅ 已搬進 `workplace/orig/`（gitignore）：`game/` 148 檔 + js-dos zip |
| 發行資料 | ✅ 漢堂國際資訊，1992-10-15，回合制戰棋（`CLAUDE.md` §1.5，維基百科）|
| 首輪偵查 | ✅ 執行檔鏈、packer、BGI 繪圖、資產檔頭（`CLAUDE.md` §3）|
| **`.TPC` 圖像** | ✅ **confirmed**：BGI `getimage`，逐列交錯 plane、plane 0 為最高位。**逐像素 round-trip 通過**；`+20` 差額 = `palt` |
| `.15` 字模格式 | ✅ **已驗證**：16×15、30 B/字，51 檔全部整除 30 |
| **文本儲存機制** | ✅ **已解**：不是索引，是**定長槽位的字模序列**。見 `docs/formats/01-glyph-text.md` |
| **詞表還原** | ✅ **已完成**：51 檔 6,174 字模，倚天命中 4,799、空白 1,374、例外 1（位移的逗號）|
| **敘事文本** | ✅ **是畫成圖的**：`NEWSDATA.DAT` = 17 條新聞橫幅（215×16 BGI 圖）。不是字模序列 |
| `.RGB` 調色盤 | ✅ 純文字，16 行 RGB **百分比**（不是 DAC）。見 `docs/formats/04-tpc-bgi-image.md` |
| **`.GLB`／`.GTB` 圖庫** | ✅ **confirmed**：PKWARE DCL implode + 13 B 目錄，45 筆全解。**逐像素 round-trip 通過**。見 `docs/formats/02-glb-image-library.md` |
| **執行檔反組譯（M2）** | 🔶 4/5 進 IDA（`SDFA` 解不開）。**戰鬥模組已定位**：`sub_39B6E` ← `PROGRAM+537`，主迴圈在 `+18F`–`+6F4`。見 `docs/re/04` |
| **`PLACD.SAV`** | ✅ **是 ZIP**：整包遊戲的 1993 年備份，145/147 檔與 `game/` byte-identical。見 `docs/formats/03-placd-archive.md` |
| `.MUS`／`.TIM` | ⬜ 未開始；音源未確認 |
| **`.DAT` 資料表** | 🔶 **39 省是核心維度**：`TERNAME`／`WARPOS` 每省 196 B、`MEM_WAR` 每省 469 B、`TOWN(N)` 每省 37 B。`MAN(N).DAT` 33 B/人 |
| **將領番號** | ✅ **`+25`/`+26`/`+27`/`+28` 全解**：前綴／編號／後綴／勢力名，三個索引都指向 `FAN(1).15` 的槽位（1-based）。11 個勢力名對上 1926 史實。見 `docs/spec/02` |
| **將領能力值** | ✅ **三個全部定名**（實機「查閱將領」）：`+0` 帶兵能力、`+1` **忠誠度**、`+2` **政治手腕**。連同 `+17`／`+19`／`+20` 共六欄零誤差。見 `docs/playtest/08` |
| **省份資料表** | ✅ **已解**：`TOWN(N).DAT` = 39×37 初始狀態、`SAVE(N).DT1` 前段同結構（相位差 4 B）。資源／人口／城市／地價／兵工廠／忠誠／司令／省長／鄰省全部定名，實機零誤差。見 `docs/spec/03-provinces.md` |
| **戰場地名** | ✅ **已解**：`TERNAME.DAT` 是**地名索引**不是地形編號，指向 `TN15.<省>`。39/39 零例外（§5.6）|
| **TPC 圖集** | ✅ **分割已驗證**：`NEWTERR` 22 張地形圖塊、`NEWICON` 18 張兵種圖示（**四個兵種全部對上**：鋼盔／戰車／馬頭／**大砲六朝向**）、`AC` 17 張戰鬥插圖 |
| **戰場圖塊** | ✅ **已解**：`NWMAP.DAT` 每省 392 B = 196 格 u16。地形 1–22 對 `NEWTERR`，鐵路編碼 `30+25×圖塊+地形`。城市數 39/39 零誤差，**戰場已能畫成原版樣子**。見 `docs/spec/04` |
| **`RAIL.TPC`** | ✅ **已解**：不是 BGI，是 4-bit chunky，21 張 32×24 鐵軌圖塊（縱橫彎角 T 字十字車站）|
| 存檔 `.DT1`/`.DT2` | 🔶 `.DT1` **整塊讀進記憶體**（recsize=14683）；**前段 4..1446 已解 = 39 省 × 37 B**，檔頭 4 B 是民國年月。1447 之後與 `.DT2`（39×469）語意未解 |
| **DOSBox oracle** | ⚠️ **按鍵回歸**：`docs/playtest/02` 的操作現在重現不了，三種送鍵方式都無反應。推測 Xvfb 裡沒有 WM 導致焦點拿不到。見 `docs/playtest/03` |
| **顯示模式** | ✅ **兩種並用**：開場／標題 320×200 256 色（mode 13h）、主遊戲 640×350 16 色（BGI）|
| 防拷／磁片檢查 | ✅ **有密碼但過得去**：載入存檔後問說明書第 36 頁，三選一，選 `1` 通過。磁片提示只是 I/O 失敗的後備路徑 |
| **Go 解碼層** | ✅ `internal/assets`：DCL、BGI、`.RGB`、`.15`、`.GLB`、**省份地圖**。測試全綠，**輸出對實機逐像素相同** |
| **政略規則（M3）** | ✅ **15/15 指令全部定位**（`docs/re/27` 畫面地圖）。公式已讀：徵稅／徵兵／開發三項／慰勞／調動／攻打。未讀：運補、商業、外交、秘密行動、談判、練兵的數值 |
| **文本還原鏈（M1）** | ✅ **四步全數完成**：字模反查 → **索引來源＝執行檔裡的立即數（1-based）** → 逐像素 round-trip（640×350 全畫面 224,000 像素零差異）→ 例外記帳。見 `docs/re/24` |
| **AI 決策鏈（M3）** | ✅ **結構 confirmed**：`sub_15F3C` 是一條**由上而下的優先序**（一步決定就跳過其餘），依「有沒有敵對鄰省」分前線／後方兩條鏈（4 步 vs 6 步）。`byte_6FFCA` bit 2 啟用最後三步。見 `70-ai.md` §6d |
| **滿員的算法（M3）** | ✅ **confirmed**：滿員 = **20000 ÷ 兵種權重**，原版明確算的（`sub_14761` × `sub_349C1`）。`10000` 也是藏在 48-bit Real 裡的字面常數（`0x8E/0/0x1C40`），2000 與 200 是除出來的 |
| **屯兵上限 15（M3）** | ✅ **confirmed**：`sub_1527A` 增援前線時，目標省的可用將領數 **≥ 15 就不去**。這是目前最具體的 AI 行為參數（同一函式在戰鬥增援用的是 100，實質恆真）。見 `70-ai.md` §6f |
| **AI 決策層（M4）** | ✅ **決策鏈 + 三道守門 + 執行層都實作了**，`AIWorld.Step` 把「調動失敗就繼續往下」的回饋接了起來（`sub_14F9A`）。**後方鏈的第一步 `sub_15018`（救援前線）已補上**。連續跑 20 回合：攻打 330／撤回後方 10／救援前線 4、搬動 33 人次。見 `docs/playtest/05` |
| **省份層尋路（M3/M4）** | ✅ **`sub_5BF74` + 權重模式 11 都解了**：成本一律 1 = 無權最短路，只走同勢力的省——**與規則層的 BFS 一致**。⚠️ 模式 10（`sub_5BA56`，344 行）未讀；同分時的走訪順序仍是差異 |
| **DOSBox oracle** | ✅ **恢復可用**：能載入存檔、過防拷密碼、進政略畫面、逐回合觀察。環境備忘記在 `playtest/06` §5 |
| **電腦會不會攻打（M3）** | ✅ **confirmed：會**。**實機抓到畫面**「江西省 8月1日／蔣中正 攻擊 孫傳芳」，玩家該回合只下練兵（`docs/playtest/14`）。機制：決策鏈 A 的 `sub_174C9` 填 `ds:A358h` 參戰部隊表，與玩家派將同一張，戰鬥主函式讀它。⛔ 舊結論「玩家主動制的戰棋」**作廢**——呼叫圖零命中只證明沒有控制流的邊，兩者靠**資料流**交接 |
| **電腦回合結構（M3）** | ✅ **`sub_1ACCC` 骨架 confirmed**：建清單 → 內政 → 遷都 → 兩條決策鏈 → 迴圈直到命令數用完。命令數 = 將領數÷8+1。**決策鏈 A（`sub_17ADA`）已解**：六個步驟，⛔ **不含攻打**（它是 `sub_1ACCC` 閉包的子集，`docs/re/12` 早就證明過）。見 `docs/re/28` |
| **調動執行層（M4）** | ✅ **`sub_1408F` 全解**：八個模式、三道兵力門檻（滿員／半滿／四分之一）、目標省將領上限 100、**調動即佔領**。滿員公式在政略模組拿到第三個獨立印證。見 `docs/re/10` |
| **疑似原版 bug（M3）** | ⚠️ `sub_13F4E` 用**省編號索引將領表**（`mul 21h` 而非 `25h`、將領表而非省份表）。型別相容所以不會崩，只是讀到不相干的將領。**處理原則：照抄並標記**——bug 也是原版行為（`70-ai.md` §6m）|
| **省份層尋路（M3）** | 🔶 **強證據**：`sub_5BF74` 配置 **1521 = 39×39** 的矩陣做省份層路徑搜尋（與戰場層的 196×196 是同一手法）。權重函式與節點取用未讀 |
| **五省之謎（M3）** | ⚠️ `{7, 11, 20, 21, 36}` 同時出現在 `sub_36BC7` 的硬編碼清單與 `WARPOS` 的哨兵 40——**兩處毫不相干卻完全相同**，那五個省有共同的特殊性質，語意未定（`70-ai.md` §6m）|
| **命令數上限（M3）** | 🔶 **強證據**：`狀態[-22Eh]` 在四支函式被檢查（門檻 1/2/3/4），形狀符合社群說的「每省有命令數上限」。**沒找到它遞減的地方**，未證實 |
| **AI 的核心傾向（M3）** | ✅ **confirmed**：**分散而非集中**——三處獨立的上限（前線 15 將領、只往人少的省分、排序挑戰力最小）。要讓電腦更會集中兵力，改這三處最直接。見 `70-ai.md` §6i |
| **三種調動（M3）** | ✅ **confirmed**：滿員的往前線送（`sub_1527A`，該省 <15 將領）、缺員的撤回後方（`sub_1541E`，挑戰力最弱的後方省）、一般調動（`sub_150FC`）。**AI 的評估函式就是攻擊力公式 `sub_5A0B9`**。見 `70-ai.md` §6e–§6h |
| **調動指令（M3）** | ✅ **confirmed**：`sub_150FC` 挑目標省（四道守門，跳過無主省與正在打仗的省），類型依目標處境定（前線 4／往前線 1／往後方 0）。`sub_14F9A` 執行——**目標省的司令改成調過去的將領**。見 `70-ai.md` §6e |
| **地圖／地名（M3）** | ✅ **SPEC-01 READY**：39 省鄰接表（對稱性 + 實機雙重驗證）、每省 14×14 戰場格子、**地名索引 39/39 驗證**。座標方向已由 `sub_50FF5` 確認 |
| **戰場幾何（M3）** | ✅ **confirmed**：**六角格**，`col=cell%14`／`row=cell/14`，`x=col×32`、`y=row×24+(奇數欄 +12)`。整張 448×348 塞進 BGI 640×350。單位記錄 `+5` = 格編號。**六鄰位移表與左右緣裁切規則全解**。見 `docs/re/07` |
| **部署規則（M3）** | ✅ **confirmed**：`WARPOS.DAT` 是戰場分區圖，攻方從自己來源省的那 10 格進場（39 省零例外）。落點旁邊不能有敵人，找不到就硬塞。`.DT2` 的 `+468` = 攻方來源省 |
| **移動／機動力（M3）** | ✅ **confirmed**：`+6`=上限、`+7`=剩餘；三道 gate；**完整地形成本表**（鐵路 2／平原 3／城市沙漠 4／丘陵森林 5／長城 10／河海 12／高山 255 不可通行）。長城阻隔、`+21==1` 可穿越 |
| **地形 × 兵種表（M3）** | ✅ **confirmed**：`sub_503BB`／`sub_501A3` 兩張表全解（12 種地形 × 4 兵種，攻防各一）。**攻守各看自己站的地形**。長城上只有兵種 1 打得出傷害 | 
| **攻擊力公式（M3）** | ✅ **confirmed**：`sub_5A0B9` 整條解出，含 7 個 Real 常數（**20000 藏在 48-bit Real 裡**，grep 不到）、兵種權重 1/4/10/100、名將加成五級。實作 + 測試在 `internal/game/strength.go` |
| **難度參數（M3）** | ✅ **confirmed**：`byte_6FFCA & 0x70` 是三選一的戰力加成（×2／×1.5／只給蔣中正）。**調難度最乾淨的入口**。見 `70-ai.md` §6c |
| **遠程攻擊（M3）** | ✅ **強證據**：兵種 4 有攻擊範圍（`sub_42BC3`／`+31`）、**不吃反擊**、**森林不能發動**（兩處獨立證據）。守方彈藥 < 5000 放棄攻擊。見 `docs/re/09` |
| **戰損（M3）** | ✅ **confirmed**：兩條路徑全解。勢均力敵＝雙方損失比例合計恆為 1/4；一面倒＝攻擊×10 vs 人力值，過門檻全滅。**沒有隨機數**。兵種 6 在兩張權重表不同（4 vs 2）。見 `docs/re/08` §5 |
| **勢力與增援（M3）** | ✅ **confirmed**：執行期單位 `+14` = **效忠的勢力領袖**（與省份 `+20` 司令直接比較，三個偏移自洽）、`+4` = 所屬省（與 `MAN(N).DAT` 同）。增援來源 = 同勢力、未交戰的鄰省，SAVE(1) 驗過 |
| **戰鬥旗標（M3）** | ✅ 執行期單位 `+8` = **攻方 1／守方 0**（兩條獨立證據）。`byte_6B969` = 戰鬥動畫顯示旗標（bit1 攻方、bit3 守方）。`sub_53C28` = 孤立檢查（高山上的友軍不算）|
| **AI 尋路權重（M3）** | ✅ **confirmed**：尋路矩陣暫時改寫三種地形（河海 30、沙漠 **100**、鐵路 1），與真實成本（12／4／2）分離。**調難度改這組最安全**。見 `70-ai.md` §6b |
| **將領（M3）** | ✅ **SPEC-02 READY**：33 B/人。**執行期記錄就是檔案記錄**（三個哨兵值印證）。能力值、所屬省、兵力、**兵種**、戰力公式的四個欄位全部對上。剩 16 bytes 未解 |
| **兵種（M3）** | ✅ **confirmed**：`+21` = 1 步兵(滿員 20000)／4 砲兵(2000，遠程)／5 裝甲兵(200，全遊戲 1 個)／6 騎兵(10000)。**社群的四個滿員數第一次被原版資料證實，零例外** |
| **Go 規則層（M4）** | 🔶 `internal/game`：地圖、將領、省份、戰場圖塊、戰鬥狀態、**六角格 + 部署 + 移動 + 攻擊力 + 戰損**。**戰鬥能端到端跑完**（`cmd/battlesim`，見 `docs/playtest/04`）。存檔寫回已驗收 |
| **AI「被包圍就突圍」（M3/M4）** | ✅ `sub_5A9F6`：**每個有效鄰省都是敵方** → 步驟 6 出兵突圍。[雷] `sub_5A907` 收集敵對鄰省時**無主省不算敵人**，與攻打候選的規則正好相反——被無主省環繞不算被包圍 |
| **AI 優勢比門檻（M3/M4）** | ✅ **`sub_16F89`：電腦要 3–4 倍戰力優勢才出手**（步驟 1 拿它決定出兵還是只調動）。⚠️ **高難度反而更保守**（4 倍 vs 3 倍），與補給門檻同方向——高難度的電腦不亂送兵，更難對付。`byte_6FFCA` bit 0 的第四個用途 |
| **AI 補給門檻（M3/M4）** | ✅ **`sub_16B44` 六步裡出現四次**：每個花資源的步驟都先問「補給夠嗎」。彈藥 ≥15000 + 燃料 ≥10000 是**硬門檻**（錢再多也擋）。難度影響養兵成本估算（÷50 vs ÷70），`byte_6FFCA` bit 0 的第三個用途、bit 2 的第二個用途 |
| **AI 決策鏈 A 第一步（M3/M4）** | ✅ **第一個打開的黑盒**：`sub_16E77` = 要不要慰勞軍民。條件是「同勢力＋可行動＋**半滿以上**的將領，體力或士氣低於門檻」。**難度分兩檔**（高 80/70、低 60/50，`byte_6FFCA` bit 0）。電腦**不慰勞殘兵** |
| **AI 擴張目標（M3/M4）** | ✅ **決策鏈 A 的十支判斷函式全部解完**。最後一支 `sub_16047` 是**依省份戰力總和就地排序**（鍵是 `sub_5B983`，即攻擊力公式——第四個獨立例子）：步驟 5 用 mode 1 升序取**戰力最弱的鄰省**打，`sub_1655C` 用 mode 2 取最強的。步驟 5 門檻高（bit 1 + 兵多 + 補給），是**兵多將廣時的擴張，且柿子挑軟的捏**。⚠️ 三種出兵模式（步驟 6→1、步驟 4→2、步驟 5→3）的**參數語意未解** |
| **AI 突圍方向（M3/M4）** | ✅ `sub_17236`：被包圍時**往「打下來之後能連回自己領地」的鄰省打**（兩層鄰省搜尋，第二層找我方省）。門檻我方省份數 ≥3。⛔ 連帶訂正決策鏈 A 開頭的特判——`ss:[+0Ah]` 是**當前省**不是勢力，`25h/26h/27h` = **臺灣省／海南島／緬甸**，三省各只有一個鄰省，跳過條件還要 `byte_6FFCA & 2`。步驟 6 另有一份只含 37/38 的清單（緬甸陸路相連）|
| **AI 目標挑選三層優先序（M3/M4）** | ✅ 決策鏈 A 步驟 4 收完候選鄰省後依序試三種挑法：**被包圍的（`sub_170C0`）> 我方接壤最多的（`sub_17135`，門檻 ≥2）> 黃金最多的（`sub_14EBA`）**。中間那層只在 `byte_6FFCA & 4` 才試（bit 2 的第三個用途）。讀作**先救急、再求穩、最後求利**。⚠️ 兩層的並列規則都是「保留較前面的候選」，所以**鄰省表的排列會影響行為**，remake 不可重排 |
| **AI 軟柿子判定（M3/M4）** | ✅ **`sub_17019` = 那個省缺糧「且」被包圍**——⚠️ 問的是**敵方目標省**不是自己的省（步驟 3 對每個敵對鄰省問、出兵準備對已挑好的目標再問一次）。與 `sub_16F89`（我方壓倒它）是同一類判斷的兩種角度，步驟 3 **二選一成立就收進候選**。⚠️ `sub_5A9F6` 在步驟 6 問「**我的**省被包圍嗎→突圍」、在這裡問「**它的**省被包圍嗎→進攻」，同一支函式換個對象問就換個意思。門檻 黃金 < 兵力/100、糧食 < 兵力/10（1:10 比例，除數 100 比 `sub_16B44` 的 50/70 寬鬆一倍以上）。順帶解出 `sub_306CF` = 某省兵力總和（32-bit，篩選條件與將領數 `sub_5A881` 逐字相同），其開頭「交戰中就歸零」是**死碼** |
| **授權自治（M3/M4）** | ✅ 省份 `+32` **bit 5 = 自治**，是個 toggle。代價是「一個月只能下一個指令」（`sub_10193` 看到 bit 5 就把剩餘數歸零）。⛔ 訂正 `40-economy.md` §7：那條把它誤記成「開發佔滿整個月」，但全檔只有 `sub_22E25`（授權自治）設 bit 5，開發三支都不碰 |
| **學潮的效果（M3/M4）** | ✅ **三個欄位各 ×0.8**（將領士氣/忠誠、省的人民忠誠），係數是 Real `80 CD CC CC CC 4C`，`CCCC` 循環＝0.8 的指紋。⚠️ 用 **Round 不是 Trunc**。⛔ 訂正：`sub_5C441` 是 19 個指令共用的**訊息函式不改狀態**，效果就在 `sub_20447` 自己裡（第一次掃漏了 `mov [di-6222h], cl` 形式）|
| **運補（M3/M4）** | ✅ **搬運全解**：四支各 100 行對應黃金/糧食/彈藥/燃料。⭐ **超過上限的部分退回來源省、總量守恆**——與 `AddResource` 的截斷是不同行為，不可混用。🔍 新線索：調動 `sub_1408F` 也呼叫這四支，形狀像「調動連資源一起帶」，未定論 |
| **談判停火（M3/M4）** | ✅ **判定全解**：用攻擊力公式累加雙方戰力，**佔上風 70%／劣勢 20%**——你強對方才肯停火。這是「AI 評估函式＝攻擊力公式」的第二個獨立例子。順帶找到戰爭記錄表 `ds:B346h`（60 B/省）與停火狀態表 `ds:BCA5h`（1 B/省），兩張相鄰 |
| **外援與償還外債（M3/M4）** | ✅ 外援 70%（`Random(10)≤6`），援助國決定除數 1/3/5（**美國最慷慨**，假說）；黃金夾 6000。⭐ **史實特例：張作霖民國 17 年 2–6 月一律拒絕**（皇姑屯那年）。償還外債與貸款**完全對稱**（每 500 金 ±1 信用度，上限 100）|
| **外交貸款（M3/M4）** | ✅ **核貸公式解出**：`Random(10) + 額度÷500 > 12` 就拒絕 → ≤1999 必過、≥6500 必拒、6000 約 10%。**「信用度」位址找到了**（`ds:BDDBh` 的 byte 表，每 500 金扣 1），索引語意未解 |
| **秘密行動（M3/M4）** | ✅ **機率與效果解出**：游擊隊 40%（`Random(10)≤3`）、學潮 20%（`≤1`），**錢在判定前就扣、失敗照付**。游擊隊成功 → 目標省黃金/糧食/彈藥各剩 70%（**先除後乘**）+ 1/5 炸兵工廠。⚠️ 學潮成功的效果在 `sub_5C441`（329 行）未讀 |
| **商業活動（M3/M4）** | ✅ **匯率全解**：彈藥 10／煤鐵 20／糧食燃料 30 換 1 黃金，**買賣同價無價差**，保底 1。煤鐵只能賣。實作 + 測試在 `internal/game/trade.go` |
| **戰鬥面板（M5）** | ✅ **六個標籤逐像素零差異**（`internal/ui/render/battlepanel.go`）：版面全部量自實機（`docs/playtest/14`）。[雷] 三字標籤 520 起字距 18、**二字標籤 530 起字距 20**——二字不是把三字置中，是「黃　金」分散排版。另做一套 6×14 小數字（戰鬥面板與政略面板的數字大小不同）|
| **Ebiten 呈現層（M5）** | 🔶 **政略 + 戰鬥都可操作**：面板 13 欄位對實機零誤差、15 項指令選單、**戰鬥畫面（六角格 + 依兵種畫圖示 + 游標，1–6 六方向移動、Enter 攻擊、空白換回合）**。五段狀態機，ESC 只退回、F10 才離開 |
| **政略回合（M4/M5）** | 🔶 **能跑一整年**：徵稅（每月限一次）／開發三項／慰勞／結束回合＋年度結算都接上了，固定種子可重現。⚠️ 只接規則層 confirmed 的指令，其餘按了沒反應。回合結構仍是簡化版（原版是逐省、有命令數上限）|
| 多語系 | ⬜ 未開始。字形來源已定案：倚天點陣字（使用者指示 2026-08-01）|

---

## 3. 文件索引

| 路徑 | 內容 | 目前 |
|---|---|---|
| `docs/formats/01-glyph-text.md` | **`.15` 字模與文本儲存機制**（定長槽位、51 檔用途、例外記帳）| ✅ |
| `docs/formats/02-glb-image-library.md` | **`.GLB`／`.GTB` 256 色圖庫**（DCL implode、調色盤、透明色、round-trip）| ✅ |
| `docs/formats/03-placd-archive.md` | **`PLACD.SAV` = 1993 年整包 ZIP 備份**（真實年代、逐檔比對）| ✅ |
| `docs/playtest/01-dosbox-probe.md` | DOSBox 探路（兩種顯示模式、磁片檢查範圍）| ✅ |
| `docs/formats/glyph-tables/` | 還原出的字表與詞條（`_slots.json` 為完整詞條）| ✅ |
| `docs/re/01-war-exe-turbo-pascal.md` | **`WAR.EXE` 是 Turbo Pascal**；字模槽寬、圖集分割、雙磁碟路徑、磁片提示 | ✅ |
| `docs/re/02-grt-exe-borland-c.md` | **五支執行檔盤點**：編譯器、角色、PKLITE 自動解包、DCL 字串佐證 | ✅ |
| `docs/re/03-war-exe-is-the-game.md` | **`WAR.EXE` 是遊戲本體**；39 省資料表結構、存檔 `.DT2` = 39×469 | ✅ |
| `docs/re/04-battle-module.md` | **戰鬥模組定位**：`sub_39B6E` 主函式、呼叫圖、32 個函式清單 | ✅ |
| `docs/re/05-mem-war-record.md` | **戰鬥狀態 469 B 的欄位切法**（讀寫兩邊對稱驗證）+ 未初始化殘料的記帳 | ✅ |
| `docs/spec/04-battlefield-tiles.md` | **戰場圖塊規格**：`NWMAP.DAT` 每省 196 格、地形 1–22、鐵路編碼（READY）| ✅ |
| **`docs/mechanics/`** | **遊戲機制，按屬性分類**（`CLAUDE.md` §6.5）。`00-index.md` 是索引與狀態總表 | 🔶 骨架完成 |
| **`docs/mechanics/70-ai.md`** | **電腦 AI 的判斷邏輯**——優先度最高。勢力輪替、戰鬥 gate、53 個 `Random` 的分佈、覆滅清空資源 | 🔶 |
| **`docs/mechanics/01-vocabulary.md`** | **從 339 條原版 UI 詞彙反推的機制地圖**。地形名稱因此升級為 confirmed | ✅ |
| `docs/re/06-main-loop-battle-gate.md` | **主迴圈與進入戰鬥的三個條件**；`sub_5C7FE` = 司令是否為十大勢力之一 | ✅ |
| `docs/re/07-battlefield-geometry.md` | **六角格幾何 + 格編號 + 部署 + 移動成本 + 勢力欄位**；`sub_50FF5` 座標、`WARPOS` 是分區圖、`sub_506B0` 成本表 | ✅ |
| `docs/re/08-combat-damage.md` | **攻擊力公式全解**（含 20000 的來歷）、兵力扣減點、地形加成、難度參數、4 倍門檻、兩張地形×兵種表 | ✅ |
| `docs/re/09-ranged-attack.md` | **遠程攻擊**：兵種 4 的射程與不吃反擊；訂正 `sub_51D68` 的 E／F 對應 | ✅ |
| `docs/playtest/02-load-save-strategy-phase.md` | **政略畫面實測**：13 個省份欄位、15 個指令、命令數上限、密碼可過 | ✅ |
| `docs/playtest/04-battlesim-dryrun.md` | **規則層第一次端到端跑起來**（湖北打河南）；實跑暴露兵種 4 特例的方向疑點 | ✅ |
| `docs/re/10-troop-transfer.md` | **調動的執行層**：`sub_1408F` 八個模式、兵力門檻、調動即佔領；推翻「剩餘命令數」 | ✅ |
| `tools/callgraph.py` | **呼叫圖遞移閉包**：查「這條路徑會不會走到 X」。讀骨架會漏分支，呼叫圖不會 | ✅ |
| `docs/re/14-acted-flag-and-last-step.md` | **`+32` bit 2 = 回合分流開關**（推翻「已徵過稅」）；`sub_15CE3` 讀完也是調動——決策鏈八支零例外 | ✅ |
| `docs/re/23-rescan-with-fixed-pattern.md` | **修好 pattern 重掃**：城市數零寫入✅、**人口會成長**（訂正）、年份門檻 17/28/33、省份 `+33..+36` 定位（當時猜「政策四維度」，已由 `re/26` 訂正）| ⚠️ 部分訂正 |
| `docs/re/24-glyph-draw-family.md` | ⭐ **M1 文本鏈打通**：四支繪字函式 ↔ 詞表槽寬、索引是執行檔裡的立即數且 **1-based**、字距 20、每像素畫兩次是**加粗不是加寬**；924 個呼叫點全還原 | ✅ |
| `docs/re/25-year-end.md` | **年度結算 `sub_30003`**：人口每年 +10%／上限 100 萬、跨年清 bit 1+7、三期歷史檢查（不過就砍半能力）、**東北易幟事件**（第一條敘事文本）| ✅ |
| `docs/re/26-policy-is-output-allocation.md` | **指令 8「政策」＝產能分配百分比**（黃金／鐵礦／煤礦／石油／糧食，剩餘 = 100 − 四欄）；訂正 `re/23` §5 | ✅ |
| `docs/re/29-ai-does-prepare-attacks.md` | ⛔ **重大訂正：電腦會出兵**。`ds:A358h` 60 B/筆是**參戰部隊表**，玩家派將 `sub_2D812`、電腦 `sub_174C9`、戰鬥主函式三方共用。呼叫圖零命中只證明沒有控制流的邊 | ✅ |
| `docs/playtest/14-computer-attacks-confirmed.md` | ⭐ **實機證實電腦主動攻打玩家**。同一張截圖還驗到戰鬥面板的四種資源＝`sub_174C9` 複製的四格（攻守成對）、六方向鍵配置 | ✅ |
| `tools/map_diff.py` | **比對政略畫面地圖區的勢力顏色面積**，量「有沒有省份易主」。內建裁切避開面板（第一次量到 9.86% 全是面板數字造成的假訊號）| ✅ |
| `docs/playtest/13-new-game-settings.md` | **開新遊戲流程**：「電腦個性 和平/好戰」（`re/29` 的實機第二證據）、十大勢力編號 0-9、⛔ `space` 不推進回合（240 次局面沒動）| ✅ |
| `tools/field_writes.py` | **列出函式改了哪些記錄欄位**，內建正對照自我驗證（`--self-test` 拿 `sub_3412B` 驗過濾器）。⛔ `docs/re/22` 的教訓：過濾器有洞時「零命中」看起來像結論 | ✅ |
| `docs/reference/people/` | **417 位將領的自傳資料庫**（`people.json` 結構化／`01-biographies.md` 可讀／`02-status.md` 進度與缺漏）。confirmed 230／partial 96／**unknown 91**（缺漏集中在北伐期的北洋旅師長級）。供之後「查看人物自傳」功能用。⚠️ 22 筆遊戲寫法與通行寫法不同，**已驗證是原版事實不是反查誤差** | ✅ |
| `docs/re/30-sortie-preparation.md` | **出兵的完整判定鏈**：派誰（兵種 4 一個都不收——原版 `inc` 排在比較之前，疑似 bug；攻擊力 <10 不帶）、派多少（**留一個守家**、最多 10 人、湊到目標省戰力的 **2.5 倍**）、**出兵閘門**（人數 ≥3 且「戰力 ≥ 目標 ×2 或 目標是軟柿子 或 家底夠厚」）。**低難度看人頭（3 人）、高難度看戰力（2.5 倍）**。模式 1 走完全不同分支 | ✅ |
| `docs/re/28-ai-chain-a.md` | **決策鏈 A 六步全解**：⛔ 不含攻打（早被閉包證明，是舊摘要行沒同步）。**`sub_174C9` 被嚴重低估**——627 行，扣四種可流動資源、改省份司令 | ✅ |
| `tools/glyph_calls.py` | **把畫字呼叫點還原成中文**：掃 `WAR.EXE.asm` 全部繪字呼叫，配詞表印出畫面文字。定機制不必再讀千行組語 | ✅ |
| `docs/re/27-screen-map.md` | ⭐ **政略層畫面地圖**：93 個函式的畫面文字一次現形。**定機制前的第一動作就是查它**。當場解掉四兵種名、將領 11 項屬性清單、貿易品項、外交五子系統 | ✅ |
| `docs/re/22-comfort-and-scan-blindspot.md` | **慰勞軍民全解**（體力+10／士氣+能力÷5 上限 **80**／忠誠+20／民心+政治÷10）+ ⛔ **掃描 pattern 漏相對定址**，前三輪的清單要訂正 | ✅ |
| `docs/re/21-develop-and-general-state.md` | **開發三項全部定位**（墾地地價上限 200／成效=政治手腕÷10）、`+16` 三個值的寫入者、慰勞四條路全排除。⛔ §3「人口只減不增」**已作廢**（`re/25` §2）| ⚠️ 部分訂正 |
| `docs/re/20-field-writers.md` | **掃欄位寫入點的手法**：建兵工廠 5000 金／上限 5 座、城市數**零寫入**（靜態）、慰勞軍民兩條都排除 | ✅ |
| `docs/re/19-loyalty.md` | **忠誠度的五個寫入點全掃**：+20 回補（電腦內政）、徵稅兩段、秘密行動 ×0.8、戰鬥扣。慰勞軍民**不改**省的忠誠度 | ✅ |
| `docs/re/18-tax-formula.md` | **徵稅公式全解**：`Random(Round(Sqrt(人口)×5))+300` clamp [1500,6500]、忠誠度 −30。**五次乾淨取樣全部對上**：黃金 5/5 打上限、糧食 5/5 落在範圍內、忠誠度兩段扣法零誤差、bit 7 = 已徵過稅 | ✅ |
| `docs/re/17-random.md` | **亂數全解**：Turbo Pascal LCG `seed×134775813+1`、`RandSeed` 在 `ds:072Ah`、`Randomize` 用系統時間。**序列完全可重現** | ✅ |
| `docs/playtest/12-tax-command.md` | **徵稅實測 ×3**：只動黃金與糧食、忠誠度大跌。**同省同操作結果不同 → 亂數 confirmed**，所以不能寫成確定公式 | ✅ |
| `docs/re/16-game-over.md` | **勝負判定全解**：覆滅／統一／**第一期時限民國 26 年 7 月（七七事變）**；結局代碼寫進 `Config.dat` 給 `GRTE.EXE` | ✅ |
| `docs/playtest/11-invented-rule-cost.md` | ⛔ **一條自己發明的規則讓長跑差了兩個勢力**；移除後 4→2、拿下省 63→81 | ✅ |
| `docs/playtest/10-recruit-economics.md` | **徵兵：10 兵 = 1 黃金**（兩樣本+一次計算零誤差）；選單結構、「重新整編」確認存在 | ✅ |
| `docs/playtest/09-full-loop-dryrun.md` | **端到端跑完 300 回合**：穩定不崩，但勢力 9→3 後凍結——暴露「只有消耗沒有補充」 | ✅ |
| `docs/re/15-pathfinding-weights.md` | **權重模式 11 = 無權最短路**（成本一律 1，只走同勢力的省）；規則層的保留可解除 | ✅ |
| `docs/playtest/08-view-generals-screen.md` | **「查閱將領」一張表定名六個欄位** + **攻擊力公式 5/5 實機零誤差** | ✅ |
| `docs/playtest/07-field-naming-by-screen.md` | **用實機畫面給欄位定名**的手法；`MAN` `+2` = 開發能力（15/15 零誤差）| ✅ |
| `docs/playtest/06-dosbox-restored.md` | **DOSBox oracle 恢復**：命令數公式實機零誤差（將領 15 → 指令數 2）、兵工廠煤鐵轉彈藥、bit 2 訂正 | ✅ |
| `docs/re/13-turn-structure-and-roster.md` | **電腦回合結構**：命令數 = 將領數÷8+1（社群說法平反）、清單依攻擊力降序、遷都、攻打是玩家指令 1 | ✅ |
| `docs/re/12-ai-chain-has-no-attack.md` | ⛔ **決策鏈六步全是調動**：`sub_15925` 不是攻打，是「往富省集中」（§5.24 訂正方向）。⛔ 該檔 §6 的「玩家主動制」已作廢（§5.23）——電腦靠 `sub_174C9` 填參戰部隊表出兵 | ✅ |
| `docs/re/11-rescue-and-pathfinding.md` | **救援快撐不住的前線省**（決策鏈第一步）+ 省份層尋路取第一步；`byte_6FFCA` bit 0 解了 | ✅ |
| `docs/playtest/05-aisim-dryrun.md` | **政略 AI 連續跑二十回合**；實跑抓到「兩省互搬兵」與決策鏈缺回饋 | ✅ |
| `docs/playtest/03-input-regression.md` | **[已解決] DOSBox 按鍵送不進去**——根因二是誤診（`xmodmap` 不存在）。經過見 `playtest/06` | ✅ |
| `docs/spec/01-map-and-terrain.md` | **地圖規格**：39 省鄰接表、14×14 戰場格子、**地名索引**（全 READY）| ✅ |
| `docs/spec/02-generals.md` | **將領規格**：33 B 記錄、三個能力值、兵力 u16、省兵力 = 將領加總（READY）| ✅ |
| `docs/spec/03-provinces.md` | **省份規格**：37 B 記錄、資源／人口／司令／省長／鄰省、1926 割據圖驗證（READY）| ✅ |
| `docs/formats/05-tpc-tilesets.md` | **TPC 圖集**：地形圖塊、兵種圖示、戰鬥插圖的分割與內容 | ✅ |
| `docs/playtest/` | 實跑驗收紀錄與截圖 | 空 |
| `docs/reference/` | 外部中文資料整理（來源 + 抓取日期）| 空 |
| `translations/glossary.md` | 譯名表（繁中母本 → en / ja）| 骨架 |

---

## 4. 術語表

| 詞 | 定義 |
|---|---|
| 定長槽位 | 原版存文字的方式：每 `w` 個字模為一個詞條，不足補全零字模。`w` 依檔案固定 |
| 排版填充 | 槽位放不滿時的空白。對齊方式依檔案而異：靠左（地名）、靠右（日將名）、分散（中將名）|
| 數字佔位 | `FAN(2)/(3).15` 詞條**中間**的空白，執行期填入 `1.15` 的數字字模（「第␣集團軍」）|
| 場景字模檔 | 51 個 `.15` 族檔案。每個場景／類別各帶一份自己的詞表 |
| 字模索引 | ⚠️ **已推翻的說法**，見 §5.1。原本以為文字存索引，實際字模序列本身就是文本 |
| 地名索引 | `TERNAME.DAT` 每格的值：`k>0` 指向該省 `TN15.<省>` 的第 k 個地名，0 = 無地名 |
| 司令／省長 | 省份記錄的兩個將領 ID（1-based）。司令是勢力領袖，省長是實際駐守的人 |
| 相位差 4 B | `SAVE(N).DT1` 的省份陣列從 offset 4 起，`TOWN(N).DAT` 從 0 起。用 `k×37` 直接切 `.DT1` 會拿到跨兩筆的碎片 |
| 字模加粗 | 原版畫中文前會把字模水平膨脹 1 px（`row \| row>>1`），字距 20 px 而非字模寬 16 px |
| BGI 緩衝 | Borland `getimage` 的記憶體格式，`.TPC` 用的就是它（4-plane、16 色 planar）|
| DCL implode | PKWARE Data Compression Library 的壓縮法，`.GLB` 每一筆各自是一段。header `00 06` |
| 透明色索引 | `.GLB` 圖像的索引 0 不寫入，露出底圖。書法標題就是這樣疊上去的 |
| DAC 6-bit | VGA 調色盤是 6-bit（0–63）。轉 8-bit 要用 `(v<<2)\|(v>>4)`，不是 `v*255/63` |
| 命令數 | 政略階段每個省份可下的指令上限（社群資料，待驗）|
| oracle | 判斷「原版怎麼做」的證據來源，優先序見 `CLAUDE.md` §4 |
| 原版 bug | 原版程式碼裡看起來是錯誤的行為。**照抄並標記**，不順手修——`CLAUDE.md` §1「不得默默改動遊戲規則」。要修是 remake 差異，由使用者決定 |

---

## 5. 已被推翻的斷言

### 5.1 「文字以字模索引儲存」→ 實為定長槽位的字模序列

- **原結論**（`CLAUDE.md` §3.5、§5）：文字以「第 N 個字模」的索引儲存，
  畫面上的中文是查表畫出來的。
- **當初憑什麼**：五個執行檔、`GRT.GLB`、所有 `.DAT` 用 Big5 解碼命中數為零，
  加上字模檔是「每場景一份子集」，推論出中間有一層索引。**推論方向對，但多推了一層。**
- **新證據**：51 檔全量反查後，字模序列直接讀出來就是通順的詞條
  （`蔣中正 何應欽 馮玉祥`、`調動 軍事 運補 徵稅`），且空白的分布完全符合定長欄位的
  排版填充。**沒有索引那一層**——程式從 `k × w × 30` 直接取 bytes 送 BGI 即可。
  Big5 零命中的原因不是「存了索引」，而是**原始碼裡本來就沒有碼點**。
- **受影響**：`CLAUDE.md` §3.5 假說、§5 還原鏈步驟 2（「索引來源」對這 51 檔不適用）。
  已在 `docs/formats/01-glyph-text.md` §6 記錄。
- **仍成立的部分**：`1.15` 的數字字模被 `FAN(2)/(3).15` 的佔位引用，
  這是目前唯一觀察到的索引式取字。敘事文本若在 `NEWSDATA.DAT`，仍可能是索引流。

### 5.2 「46 個字模檔」→ 實為 51 個

- **原結論**（`CLAUDE.md` §3.4）：46 個 `.15` 族檔案，清單列出
  `TN15.1`–`39`、`FAN(1)`、`FAN(2)`、`JAPAN1`、`MAN115/215/315`。
- **新證據**：完整列舉後是 **51 個**，原清單漏了 `FAN(3).15`、`JAPAN2.15`，
  且沒列 `1.15`–`4.15` 這四個。
- **為什麼會漏**：`TN15.9` 的副檔名是 `9` 不是 `15`，用 `ls *.15` 或
  `grep '\.15$'` 篩會篩掉大半——**過濾器自己有洞**。
  這正是 `rulebook/83`「同類檔先列舉全再做 pipeline」的踩坑點。
- **受影響**：`CLAUDE.md` §3.4 已更正。

### 5.5 「`GRT` 是政略主程式、`WAR` 是戰鬥模組」→ 反了，`WAR` 是遊戲本體

- **原結論**（`CLAUDE.md` §3.7）：`GRT` 政略主程式、`WAR` 戰鬥模組
  （375 KB 最大，且戰棋的戰鬥系統最重）。
- **當初憑什麼**：`play.bat` 的順序 `grt → war`，加上「戰鬥系統最重」的直覺。
  **兩者都只是類比，沒有證據。**
- **新證據**：比對每支引用的資料檔字串——`WAR.EXE` 引用 80 個以上
  （全部字模、全部 `.RGB`、`nwmap`／`tername`／`warpos`／`mem_war`／`NewsData`、
  以及 `save(1).dt2` 到 `save(10).dt2`），另外三支各自只引用兩三個檔。
  `GRT`／`GRTE` 的字串表全是資源載入與記憶體管理，沒有任何政略或戰鬥的東西。
- **受影響**：M3 規則規格要挖的東西全部集中在 `WAR.EXE` 一支。
  `CLAUDE.md` §3.7 已更正，詳見 `docs/re/03-war-exe-is-the-game.md`。

### 5.3 「繪圖是 BGI，與 mode 13h 完全是兩套」→ 兩種模式並用

- **原結論**（`CLAUDE.md` §3.2）：遊戲用 BGI 繪圖，640×350 或 640×480 16 色 planar，
  「與大富翁2 的 mode 13h chunky 完全是兩套」。
- **當初憑什麼**：`EGAVGA.BGI` 存在，且 `.TPC` 經驗算確實是 BGI `getimage` 的
  4-plane 16 色格式，`w=640` 也對得上。**這些證據本身沒錯，錯在外推。**
- **新證據**：DOSBox 實測用 `xdotool getwindowgeometry` 量到——標題階段視窗是
  **320×200**、單張畫面 217 個獨立色（mode 13h 256 色）；進主選單才切成 **640×350**、
  5–16 色（BGI）。兩種模式都在用。
- **為什麼會錯**：從「`.TPC` 是 BGI 格式」推到「整個遊戲都是 BGI」，
  正是 `CLAUDE.md` §7 第 9 條的「不要跨檔案外推」。`.TPC` 只證明了 `.TPC` 自己。
- **受影響**：`CLAUDE.md` §3.2 已改寫；remake 呈現層要支援兩種模式。
- **順帶的線索**：開場的 256 色圖不可能是 `.TPC`，存在別處；
  `GRT.GLB` 熵 7.96 bit/byte 一直沒解釋，壓縮過的 256 色圖是合理候選。

### 5.4 「`WAR.EXE` 是 Turbo C 編譯的」→ 是 Turbo Pascal

- **原結論**（`CLAUDE.md` §3.1）：`WAR.EXE` 未打包，Borland Turbo C runtime。
- **當初憑什麼**：檔案裡有 `Copyright (c) 1983,90 Borland` 字串。
- **新證據**：IDA 自動辨識出的 RTL 全是 Turbo Pascal——`BlockRead`、`Concat`、
  `Copy`、`Insert`、`ASSIGNCRT`、`IOResult`，以及 **Graph unit**（`CLOSEGRAPH`、
  `BAR`、`CIRCLE`、`DRAWPOLY`）。字串也是 Pascal 長度前綴形式
  （`db 33,'Please remove Disk MARK-A from A:'`）。
- **為什麼會錯**：Borland 的版權字串在 Turbo C 與 Turbo Pascal 都有，
  光憑它分不出來。
- **受影響**：**後續每一份反組譯筆記**——呼叫慣例是 Pascal（參數由左至右壓棧、
  被呼叫者清棧），字串是長度前綴而非 NUL 結尾。用 C 的直覺讀參數順序會全部反過來。
  `CLAUDE.md` §3.1、§4.1 已更正。

### 5.7 「地物編號 10 是岩地／荒地」→ 是高原

- **原結論**（`docs/spec/04` 舊版）：從 `NEWTERR.TPC` 的圖塊顏色判讀，
  灰褐色的地塊看起來像岩地或荒地。
- **新證據**：`2.15` 的詞條 194 是「地形」，195–205 依序是
  平原、丘陵、河海、森林、城市、高山、沙漠、橋樑、橋樑、高原、關口
  ——與地物編號 1–11 一一對應，**連橋樑重複兩次都對上**。編號 10 是**高原**。
- **為什麼會錯**：視覺判讀有極限。灰褐色地塊看起來像岩地，但遊戲裡它是高原。
- **受影響**：`docs/spec/04` §2 的地物表升級為 confirmed（不再是視覺判讀），
  `assets.TileRock` 改名 `TilePlateau`，`TileGate` 改 `TilePass`（關口）。
  詞表整理見 `docs/mechanics/01-vocabulary.md`。

### 5.6 「`TERNAME.DAT` 的格值是地形編號 0–22」→ 是地名索引

- **原結論**（`docs/spec/01` §4，DRAFT）：每格存地形編號，值域 0–22。
- **當初憑什麼**：掃過 39 省的值域是 0–22，且河南的格子分區明顯不是雜訊
  （左上一片 `7`、左下一片 `8`、縱向的 `5`／`6` 帶狀）。
- **新證據**：每省用到的**最大值恰好等於該省 `TN15.<省>` 地名表的詞條數**，
  39/39 零例外（廣西 22 個地名→上限 22、海南島 7 個→上限 7）。
  湖北的值 9 是「長江」佔 33 格、值 6 是「大別山」佔 21 格、
  值 1–4 是漢口／武昌／漢陽／襄陽各佔 1 格。
- **為什麼會錯**：分區明顯這點沒錯，地名本來就連續成塊。錯在把
  **39 省的聯集值域**當成一張共用的類型表——實際上每省各有自己的地名表，
  上限各不相同。`NEWTERR.TPC` 恰好有 22 張圖塊，加深了誤判，但那是巧合。
- **受影響**：`internal/game` 的 `Terrain` 改名 `PlaceIndex`、
  `Battlefield.Terrain` 改為 `Place`；呈現層的地形配色移除。
  `docs/spec/01` §4 已改寫並升為 READY，測試在 `internal/game/place_test.go`。
- **連帶好消息**：`TN15.N` 的用途因此確定——它是**該省戰場上各據點的名字**
  （城市、山、河、鐵路），不只是詞表。

### 5.10 「`20000` 這類滿員數搜不到，可能是公式算出來的」→ 是被編碼成 Real

- **原說法**（`CLAUDE.md` §1.5）：`20000`／`10000`／`2000`／`200` 是很好的
  grep 起點，但搜不到是正常的——「可能是公式算出來的」。
- **實際情形**：`20000` **就在程式裡，而且是一個字面常數**，
  只是編碼成 Turbo Pascal 的 48-bit `Real`：`8F 00 00 00 40 1C`
  （`docs/re/08` §4a）。bytes 裡沒有 `4E20`，也沒有十進位字串，
  所以 grep `20000` 或 `4E20` 一輩子都不會中。
- **原說法錯在哪**：它假設「搜不到 = 不是常數」。實際上第三種可能是
  **常數在，但被編碼掉了**。這一版的教訓比原本那條更強：
  遇到浮點運算的老程式，要找的不是整數字面值，而是 `Real` 常數的載入樣式
  （`mov cx, <exp>` / `mov di, <mantissa>` 接 `@$brmul` 或 `@$brdiv`）。
- **仍成立的部分**：`10000`（社群說的騎兵滿員）確實沒找到，
  公式裡對應的權重 4 換算出來是 5000。社群數值不全出自 1992 原版
  這一點沒有改變。

### 5.11 「兵種號碼與兵種名沒有證據」→ 四個滿員數同時對上

- **原說法**（`docs/re/08` §4c）：兵種權重 1／10／100 與社群傳的滿員數
  20000／2000／200 成反比，三項吻合；但權重 4 對應的 5000 與社群說的
  「騎兵師 10000」對不上，所以**不拿來定名**。
- **錯在哪**：用錯了表。攻擊力公式（`sub_5A0B9`）給騎兵的權重是 4，
  但一面倒結算（`sub_5190E`）給的是 **2** ——`2 × 10000 = 20000` ✓。
  當時已經知道兩張表在兵種 6 不同，卻沒把它與滿員數連起來。
- **新證據**：直接量 `MAN(1).DAT` 各兵種的兵力最大值，
  **20000／2000／200／10000 四個一起對上，零例外**（`docs/spec/02` §6）。
  四個獨立的數字同時吻合不可能是巧合。
- **連帶解釋**：`sub_5190E` 的權重 × 滿員 = **20000** 對四個兵種全部成立
  ——那正是攻擊力公式的分母。所以它是**兵力正規化表**，
  而 `sub_5A0B9` 把騎兵的戰力權重特意調高一倍。兩張表的差異因此有了解釋。
- **仍成立的部分**：社群數值不全出自 1992 原版這一點沒有改變
  ——這次是四個數字**恰好都對**，不是社群一向可信。

### 5.12 「`NEWICON` 的 6–17 是飛機」→ 是大砲，而且就是那個「找不到的炮兵」

- **原結論**（`docs/formats/05` §3）：6–17 是飛機，綠紅各六個朝向；
  並記「沒有明顯的炮兵圖示，是炮兵用了別的圖、還是戰車其實是炮兵，未確認」。
- **當初憑什麼**：只看了縮圖。長條狀 + 六個朝向，直覺是飛機。
- **新證據**：放大 3 倍渲染，**砲管、輪子、砲架**清清楚楚，是側視的野戰砲。
- **連帶解掉的疑問**：「找不到炮兵圖示」不成立——它一直都在，
  只是被誤認了。四個兵種的圖示現在全部對上（鋼盔／戰車／馬頭／大砲）。
- **為什麼會錯**：縮圖判讀。這與 §5.7（地物編號 10 看圖判成岩地，
  實際是高原）同一類錯誤——**視覺判讀有極限，要放大或找旁證**。
- **連帶佐證**：砲兵是遠程兵種（`docs/re/09`），六個朝向配六角格的六個方向，
  支持 `+31` 是**朝向**而不是射程。

### 5.8 「戰場資料是 14×14 方格，與社群說的六角格矛盾」→ 不矛盾

- **原結論**（`docs/mechanics/30-combat.md` §4）：社群說戰鬥用六角格，
  但資料是 14×14 方格，兩者矛盾，可能另有一張地圖。
- **新證據**：`sub_50FF5` 算螢幕座標時，**奇數欄整體下移 12 px**（半個格高）：
  `y = row×24 + (col 為奇數 ? 12 : 0)`。這就是 column-staggered 六角格。
- **為什麼會錯**：把「資料佈局」與「幾何排列」當成同一件事。
  六角格的儲存本來就是矩形陣列，六角性質在**繪製與鄰接**上，不在資料形狀上。
- **仍未解**：六角鄰接怎麼算（奇偶欄的偏移方向）。移動函式還沒讀，
  **不要用幾何常識補**。

### 5.9 「原版把地名索引畫成地形圖塊，缺一張對照表」→ 問題問錯了

- **原結論**（`internal/ui/render/battlefield.go` 註解）：原版一定有一張
  「地名 → 圖塊」的對照表，找不到所以不能畫。
- **新證據**：`sub_50FF5` 讀的是 **`NWMAP` 的格值**（`& 0FFFh`），
  經 `sub_50151` 解出地形碼，直接取 `NEWTERR.TPC` 的第 N 張。
  **地名索引從頭到尾沒有參與繪製。**
- **為什麼會錯**：`TERNAME` 與 `NWMAP` 都是每省一張 14×14，看起來像同一件事的兩面，
  於是假設地名要能推出圖塊。實際上地名管「這格叫什麼」、地物管「這格畫成什麼」，
  是兩個獨立的圖層。
- **連帶確認**：`sub_50151` 的解碼流程正是 `docs/spec/04` 那條鐵路編碼
  `v = 30 + 25×圖塊 + 地形` 的逆運算——當初從 7,644 格統計推出來的公式，
  現在有原始實作背書，從「強證據」升為 **confirmed**。

---

### 5.13 「`狀態[-22Eh]` 是剩餘命令數」→ 是當前省的將領清單長度

- **原結論**（`docs/mechanics/70-ai.md`，標**強證據**）：四支函式檢查它
  `== 1`／`>= 3`／`>= 4`／當分母，形狀像「每省命令數上限」那個機制。
- **當初憑什麼**：`CLAUDE.md` §1.5 記過社群說法「每個省份有命令數上限」，
  而一個逐格遞減、不同指令要求不同剩餘量的計數器正好是那個形狀。
  當時就註明「沒有找到它遞減的地方」。
- **新證據**：`sub_1408F` 把同一格當 **Pascal 陣列的長度欄**用——
  `arr[0]` 是元素數、`arr[1..n]` 是將領編號，取出來立刻餵給
  `sub_349C1`（兵種權重）與 `×33 + 0x7A7D`（將領記錄定址）。
  **找不到遞減處，是因為它根本不遞減。**
- **為什麼會錯**：兩個假說在當時那批證據上**完全等價**——
  四道門檻讀成「將領數」或「剩餘命令數」都通。是後來的**定址方式**
  把它們分開的。這是「掃常數不要掃結果」的變形：
  光看比較值分不出來，要看那個值**被拿去做什麼**。
- **副作用（好的）**：§6h 那道沒解的門檻因此解了——
  `狀態[-236h] ÷ 將領數 ≥ 10000` = **平均兵力 ≥ 半個滿員步兵師**。
- **社群那條說法一度回到未證實**，但**後來證實了**——它在 `狀態[-232h]`，
  公式是 `將領數 ÷ 8 + 1`，`sub_1398D` 與 `sub_174C9` 會 `dec` 它，
  `sub_1ACCC` 的主迴圈跑到它歸零才換省（`docs/re/13` §2）。
  當初不敢下結論的理由（「找不到遞減的地方」）是對的——**格子找錯了**。

### 5.14 「一般調動的目標是任何有主的鄰省」→ 只往**無主**省

- **原結論**（`docs/mechanics/70-ai.md` §6e）：`sub_150FC` 挑目標時
  「無主省不調」。
- **當初憑什麼**：讀到 `cmp word ptr [di-6221h], 0` 後**沒有跟到 `jnz` 跳去哪**。
- **新證據**：`jnz short loc_15219`，而 `loc_15219` 是迴圈尾的 skip 標籤
  ——不等於 0 就跳過，留下來的只有**司令 == 0** 的省。配上
  `sub_14F9A` 調動成功後把目標省司令改成我方，語意是**往無人的省份擴張**。
- **怎麼抓到的**：**實跑，不是讀碼也不是測試**。連續跑二十回合時
  兩個省每回合互相搬同樣的 3 個人、局面永遠不變（`docs/playtest/05` §3）。
  單元測試全綠——因為每一步單看都合法。
- **同一輪順帶訂正**：`sub_15667` 分兵漏了 `sub_13F4E` 守門，
  且調動模式應為 **3** 不是 4。

### 5.15 「決策鏈的 `sub_15925` 是攻打」→ 也是調動，六步全是調動

- **原結論**（`docs/mechanics/70-ai.md` §6i）：「決策鏈的六個步驟裡有五個
  呼叫 `sub_14F9A`（調動），只有 `sub_15925` 是攻打。」
- **當初憑什麼**：`sub_15925` 收集鄰省候選、排序、挑一個——**形狀**與
  「選攻打目標」一模一樣。標它為攻打時**沒有跟到最後那個 `call`**。
- **新證據**：`sub_15925` 的最後一行是 `sub_14F9A(狀態, 2, 目標)`，
  而目標篩選 `sub_1588C` 只放行「無主省」與「自己的省」，
  第二個分支就把所有敵省擋掉了。它真正的語意是**把部隊送去
  自己最缺錢的鄰省**（依省份記錄 `+0` 黃金升序挑）。
- **怎麼抓到的**：`cmd/aisim` 接上戰鬥層後跑 25 回合，61 次「攻打」
  失敗於「省 N 沒有可出兵的將領」——決策層與執行層條件不一致，
  回去讀原始碼才發現讀錯了東西。
- **順帶解出**：一道很硬的前置門檻（前線省將領數 ≥ **25**、後方 ≥ **5**）。
  25 幾乎不可能達到（增援上限才 15），所以**前線鏈這一步等於關閉**。
- **後果**：**電腦怎麼發動戰爭，目前是未解的。** 規則層不假裝它會攻打；
  `cmd/aisim -fight` 是明確標記的探索工具，不是原版行為。
- **同類錯誤**：與 5.14（讀到 `cmp` 沒跟 `jnz` 跳去哪）一樣，
  都是**在推論的最後一步停下來**。

### 5.16 「省份 `+32` bit 2 = 本月已徵過稅」→ 是「本回合已處理過」

- **原結論**（`docs/spec/03` §2b，標假說）：`4.15` 詞條 42 有「已徵過稅」，
  而徵稅每月限一次正好需要一個旗標；`SAVE(1)` 25 省、`SAVE(2)` 4 省設它，
  無主省一律沒設。
- **新證據**：四處程式碼把它講清楚了——`sub_10193`（玩家下完命令）與
  `sub_13D23`（電腦開始跑這個省）**設**它，`sub_10AB5`（主迴圈）看到就
  **跳過玩家選單**，`sub_383EF` 對全 39 省**清**它。
  **它是玩家與電腦的分流開關**。
- **為什麼原本的觀察仍然成立**：存檔當下那一輪已經輪過 25 個省；
  無主省不設，是因為它兩條路徑都不進。**同一批觀察，兩種解釋都通**
  ——分辨它們的是程式碼，不是統計。
- **「已徵過稅」的旗標後來找到了：`+32` bit 7**（`sub_2C6C6` 的
  `or al, 80h`，`docs/re/18` §3c）。bit 2 與 bit 7 各歸各位。

### 5.17 「DOSBox 的 keymap 是空的」→ 誤診，`xmodmap` 根本不存在

- **原結論**（`docs/playtest/03` §3 根因二）：`xmodmap -pk` 輸出 0 行，
  所以 X server 沒有鍵盤映射表。
- **新證據**：`xmodmap` **在那個 image 裡不存在**（`command -v` 回 MISSING），
  「0 行」是命令缺失造成的。`setxkbmap`／`xkbcomp` 都在，
  `/usr/share/X11/xkb` 資料完整。
- **真正的根因只有一個**（PointerRoot 焦點），而修它的改動當時就已經
  寫進 `tools/dosbox_runner.sh`——**只差重測**。
- **教訓**：用一個**不存在的工具**去量，得到 0，然後把 0 當證據。
  診斷指令本身要先驗證它會回答問題（`~/diagnosis-notes` 的
  「查詢回空」四種形狀之一：自己的量測器有洞）。

### 5.18 「`MAN` `+2` = 開發能力」→ 是「政治手腕」，那是用途不是名字

- **原結論**（`docs/playtest/07`，標 confirmed）：指令 7「開發」派將時
  列出的就是 `+2`，湖北 15 人 15/15 全對，所以 `+2` = 開發能力。
- **數字沒錯，名字錯了**：「查閱將領」的詳細表直接寫著那一列叫
  **政治手腕**（`docs/playtest/08` §2）。開發會用到它，但它不叫開發能力。
- **教訓**：從「某功能會用到這個欄位」反推欄位名，很容易把**用途**
  當成**名字**。要找到直接寫名字的畫面才算數。
  同一輪還定名了 `+1` = **忠誠度**（原本標「未知」）。

### 5.19 ⛔ 自己發明的規則：「兵力歸零的將領標為不可用」

- **不是推翻原版的結論，是推翻我們自己加的一條**。`internal/game/resolve.go`
  的 `writeBack` 曾經在兵力歸零時設 `Active = false`，
  註解寫著「原版陣亡的處理還沒解，這裡只確保它不會再被派上場」。
- **原版沒有這條**：`sub_28259`（徵兵）檢查的是 `+16 == 1`，
  **與兵力無關**——兵力 0 的將領照樣能被補兵。
- **代價可量化**：長跑 200 回合，移除它之後最終勢力從 4 收斂到 **2**、
  拿下省從 63 升到 **81**、無將領的省從 25 降到 18（`docs/playtest/11`）。
- **為什麼會犯**：「兵力歸零的部隊不該再上場」聽起來理所當然，
  而且不加的話 `BattleSim` 會收到兵力 0 的單位。**標了「未解」
  不等於可以自己補一條**——這是 `CLAUDE.md` §9 的字面要求。

### 5.20 「省份 `+33..+36` 是政策四維度」→ 是**產能分配百分比**

- 出處：`docs/re/23` §5。當時只知道那四格恆為 `25 25 25 25`，
  猜是社群提到的「政體、政治研究」，並說要讀 `sub_23511` 的 1,147 行才知道。
- **不必讀那 1,147 行**。用 `tools/glyph_calls.py` 掃該函式的 24 個繪字呼叫，
  畫面上直接寫著「本省產能」「目前 ％」「剩餘」，五個項目是
  黃金／鐵礦／煤礦／石油／糧食（`docs/re/26`）。
- 第五個不是欄位，是 `100 − 四欄之和` 當場算的。初始 25×4 = 100 → 剩餘 0。
- ⚠️ **哪一格對應哪一種資源仍未定**，不要順手補上去
  ——這正是 §5.18（`+2` 被誤名為「開發能力」）的同型錯誤。

### 5.21 「人口只減不增」→ 每年成長 10%

- 出處：`docs/re/21` §3，成因是掃描 pattern 漏掉相對定址（§5.22）。
- 正證據在年度結算 `sub_30003`：人口 < 100 萬時 `人口 += Trunc(人口 ÷ 10)`，
  **一年一次**（`docs/re/25` §2）。因為不是每回合，先前逐回合找當然找不到。

### 5.22 掃描 pattern 只涵蓋絕對定址 → 連帶作廢四條結論

- `add di, 7A7Dh` 之後用 `es:[di+1Dh]` 這種**相對定址**形式沒被 pattern 抓到，
  導致 `docs/re/19`／`20`／`21` 連續三輪「排除某某」的結論全部落空（`docs/re/22`）。
- 修法：**追基址不追偏移**，並且**先驗過濾器**（確認它抓得到已知命中）再信結果。

### 5.23 ⛔ 「電腦不會攻打，這是玩家主動制的戰棋」→ 電腦會出兵

- 出處：`docs/re/12` §6，曾標 confirmed 並寫進 §2 現況表。
- 推翻的證據：電腦決策鏈 A 的 `sub_174C9` 填 `ds:A358h` 的**參戰部隊表**，
  與玩家派將 `sub_2D812` 是同一張表、重疊的欄位（+4/+6/+12/+16 =
  黃金/糧食/彈藥/燃料），而戰鬥主函式 `sub_39B6E` 的子函式讀它（`docs/re/29`）。
- **為什麼會錯**：呼叫圖掃描本身沒問題（子樹確實零命中戰鬥主函式），
  錯的是從「控制流沒有邊」外推到「行為不存在」。**中間隔著資料流**——
  填表的人不呼叫戰鬥，戰鬥由主迴圈的 gate 觸發。
- **通則**：要問「A 會不會導致 B」，呼叫圖只答得了「A 會不會呼叫 B」。
  共用資料結構要另外掃（`tools/callgraph.py` 檔頭已補上這條限制）。
- ⚠️ 新結論標**強證據**不是 confirmed：還沒把「表被填」與 gate 的條件接起來。
  實跑十幾個月看電腦會不會打過來，從「錦上添花」升級為**必要驗證**。

### 5.24 ⛔ 「`sub_15925` 把兵送去最窮的鄰省」→ 送去**最富**的

- 出處：`docs/re/12` §5，曾標 confirmed；實作成 `poorestTransfer`。
- 推翻的證據：`sub_14EBA` 的交換條件是 `cmp ax, cx / jbe 不換`，
  其中 `cx` 是外圈 `候選[i]` 的黃金、`ax` 是內圈 `候選[j]` 的黃金。
  `ax ≤ cx` 才跳過 → **只有內圈比外圈大才交換** → 降冪 → 回傳最富的。
- **為什麼會錯**：把 `jbe` 的方向與 `ax`／`cx` 的歸屬一起讀反。
  排序方向由**單一指令**決定，重讀同一段組語就能確認，
  不需要新的 oracle——當初沒有再看一次。
- **旁證支持新結論**：`sub_15A9A`「往補給充足的省調動」（§6k）早已 confirmed。
  舊說法讓兩支調動方向相反，訂正後一致：**往養得起兵的地方集中**。
- ⚠️ **整套測試在錯誤實作下全綠**。測試只驗「目標是不是鄰省」，
  從沒驗排序方向；行為統計也看不出來（兩個方向都產生調動）。
  補了 `TestRichestTransferPicksRichest` 並用正對照確認它在舊實作下會紅。

### 5.25 ⛔ 「`sub_1588C` 與攻打規則同一套過濾（無主省否決）」→ 無主省是**放行**的

- 出處：`docs/mechanics/70-ai.md` §6d 的 `sub_1588C` 偽碼。
- 推翻的證據：`cmp word ptr [di-6221h], 0 / jz short loc_15912`，
  而 `loc_15912` 設的是 `var_1 = 1`（通過）。完整語意是
  「無主省放行；敵省否決；自己的省要**人夠少**才放行
  （前線 ≤ 15、後方 ≤ 2）」。
- **為什麼會錯**：`jz` 跳去的標籤沒跟到底，直接照「與 `sub_5B7DC` 同一套」
  的印象寫偽碼。與 `docs/re/12` §8 記過的「在推論的最後一步停下來」同型。
- ⚠️ **程式碼一直是對的**（`internal/game/ai.go` 的 `transferable`），
  錯的只有文件。`docs/re/12` §3 的偽碼也一直是對的——
  **同一件事在兩份文件裡一對一錯**，這正是 `rulebook/63` 的失效模式。

## 6. Worklist（狀態的單一真相來源）

### 6.0 一句話現況

**M0 資產解密完成**：`.15` 字模、`.TPC`／`.RGB`、`.GLB`／`.GTB`、`PLACD.SAV`、
`NEWSDATA.DAT` 全部解開，其中 `.TPC` 與 `.GLB` 通過逐像素 round-trip。
**Go 解碼層 `internal/assets` 上線**，輸出對實機逐像素相同。
DOSBox oracle 可走到「選擇歷史背景」。

**敘事文本的答案是「畫成圖」**：`NEWSDATA.DAT` 是 17 條新聞橫幅的點陣圖
（「調動軍隊至」「爆發大規模示威遊行」），不是字模序列。所以 remake 的翻譯
工作要處理的是「把圖轉回文字」，而不是改字串表。

**M2 已起步**：`WAR.EXE` 是 Turbo Pascal（不是 Turbo C），字模槽寬與圖集分割
都已從 `Reset` 的 recsize 直接讀出來，把先前 5 個弱證據升級為 confirmed。

**省份資料表全解**：`TOWN(N).DAT` 與 `SAVE(N).DT1` 前段是同一個 39×37 結構
（相位差 4 B）。14 個欄位定名，實機湖北的 11 個畫面數值零誤差，
而司令欄位解出來就是 1926 年的割據圖——孫傳芳的五省聯軍一個不多一個不少。

**`TERNAME.DAT` 不是地形表，是地名索引**（§5.6）。連帶確定 `TN15.N` 是
該省戰場各據點的名字。真正的地形資料在 `NWMAP.DAT`。

**戰場已經能畫成原版樣子**：`NWMAP.DAT` 每省 392 B = 196 格 u16，
地形編號 1–22 直接對 `NEWTERR.TPC` 的圖塊，鐵路用 `30 + 25×圖塊 + 地形`
編碼疊在地形上。城市數對省份表 39/39 零誤差；視覺驗收看得到湖北的長江橫貫、
鐵路穿過五座城市、跨江處是鐵橋，河北的長城橫貫北緣配五座關隘。

**六角鄰接也解開了**：位移表在 `sub_4E65C`（偶數欄 −1/+14/+1/−15/−14/−13、
奇數欄 +13/+14/+15/−1/−14/+1，方向語意一致），邊界規則在 `sub_510E0`
——左右緣要裁兩個方向，上下靠 `0..195` 的範圍檢查擋掉。
`sub_55CEC` 用這張表數「周圍有幾個敵人」，部署時要求是 0。

**戰場是六角格，而且整條繪製鏈解開了**：`sub_50FF5` 把格編號拆成
`col = cell mod 14`、`row = cell div 14`，算出 `x = col×32`、
`y = row×24 + (奇數欄再 +12)`——**奇數欄下移半格就是六角格**。
整張戰場 448×348，差 2 px 頂到 BGI 640×350 的底部，右邊剩 192 px 給面板。
執行期單位記錄最熱的 `+5`（423 次存取）就是**格編號 0..195**。

**`WARPOS.DAT` 首先是戰場分區圖，鄰接表只是它的副產品**：
攻方從自己來源省的那一片格進場，每個鄰省的分區恰好 **10 格，39 省零例外**，
正好對上戰鬥記錄的 10 個部隊槽。`.DT2` 的 `+468` 就是攻方來源省編號——
河南那筆是 11（河北，張作霖系），與存檔裡「河南正被張作霖系攻打」對得上。

**M1 文本還原鏈打通**：`docs/re/24` 解出四支繪字函式（差別只在一次畫 1／2／3／5 個字，
正好對上詞表槽寬），並確定**索引是編譯進 `WAR.EXE` 的立即數、1-based**。
`tools/glyph_calls.py` 把 **924 個畫字呼叫點全部還原成中文**，
另有 95 個索引來自變數（照實列為未解）。這把機制推斷從「這個詞存在」
升級成「這一句話長這樣、是哪個函式印的」——`docs/re/26` 就是靠它
一次定出指令 8 的整個畫面，省掉讀 1,147 行組語。

**第一條敘事文本解出來了**：民國 17 年的年度結算印
「**張作霖　更改　番號　為　安　國　軍**」——程式碼（效忠 166 的將領番號改成 3）、
詞表（`MAN115` 詞 166 ＝ 張作霖、`FAN(1).15` 詞 3 ＝ 安國軍）、
史實（張作霖 1927 就任安國軍政府大元帥）三方吻合。

**年度結算全解**（`docs/re/25`）：人口每年 +10%（上限 100 萬）、跨年清兩個旗標、
三期各有一道歷史檢查（沒過就把玩家全將領帶兵能力砍半），
連帶定出三期的年份骨架（民國 17／28／33）。

**十五個政略指令全部定位**（`docs/re/27`）：畫面地圖一次解掉商業活動
（進口 3 種／出口 5 種，煤鐵只能賣不能買）、運補（只搬黃金糧食彈藥燃料）、
外交五子系統（貸款／外援／償還外債／談判停火／授權自治）、
秘密行動兩項（游擊隊／學潮）、練兵（提升士兵戰技）。
四個兵種名（步兵／砲兵／裝甲兵／騎兵）拿到第二條獨立證據；
「游擊隊」不是兵種，是秘密行動的道具。

**電腦出兵的完整鏈解開了**（`docs/re/28`／`docs/re/30`）——這是 M3 的核心成果：

```
動機     步驟 1 **不是為了擴張，是為了打通去救援的路**——sub_14E85 回傳的是
         「往我方快垮的前線省」的下一跳，那一跳是敵省就打下來、是自己人就調動
挑目標   步驟 4 三層優先序：被包圍的 > 我方接壤最多的(≥2) > 黃金最多的
         步驟 5 挑戰力最弱的鄰省（唯一的主動擴張，門檻最高）
         步驟 6 挑能連回自己領地的方向突圍
派誰     兵種 4 一個都不收（原版 inc 排在比較之前，疑似 bug）、攻擊力 <10 不帶
派多少   至少留一個守家、最多 10 人、湊到目標省戰力的 2.5 倍
出不出   人數 ≥3 且（戰力 ≥ 目標 ×2 或 目標是軟柿子 或 家底夠厚）
```

**難度不是只調數值，是換一套判準**：低難度看人頭（湊 3 人就打），
高難度看戰力（湊到 2.5 倍、還要 ×2 大於最強敵對鄰省）。
`byte_6FFCA` bit 0 至此有**五個**用途、bit 1 有兩個、bit 2 有三個。

決策鏈 A 的**十支判斷函式全部解完**，AI 的評估函式一律是攻擊力公式
（`sub_5B983`／`sub_5A0B9`，五個獨立例子）。

### 6.1 下一步

**M2 是現在的關鍵路徑**——規則層與 Ebiten 呈現層必須等 M2/M3 出規格才能動手
（`CLAUDE.md` §2：spec 齊了才實作）。

| # | 項目 | 說明 |
|---|---|---|
| 1 | **戰鬥主迴圈** | 骨架已解：逐一處理 `word[0x750+i*2]` 的單位清單，`sub_54E3B` 是九步驟結算（`30-combat.md` §3b）。**第一條公式已解**：`+30` 欄位 > 40 時每回合減 20%（§3c）。其餘八步未讀 |
| 1g | 執行期單位記錄 | 基址 `0x7A7D`、33 B/筆。已知 **`+5` = 戰場格編號 0..195**（0xFF = 不在場上）、**`+9` = 所屬省**、`+16`(bit 0 在場、bit 2 部署)、`+30`(衰減)。**與 `MAN(N).DAT` 的欄位對不上**，是兩套結構 |
| 1i | **`sub_55CEC` 的呼叫端** | 函式本身已解（數周圍敵方單位，`n=1` 六格／`n=2` 連距離 2 共 18 格）。呼叫它的 8 個地方各在判什麼未追——ZOC、撤退門檻、砲兵射程都可能在裡面 |
| 1k | 196×196 成本矩陣的用途 | `sub_4FCCC` 建了 38,416 B 的全對全矩陣（`dword_6A454`），但誰讀它、怎麼用來規劃路徑還沒追 |
| 1l | **`+21` 兵種編號的對應** | 值域含 1/4/5/6，`==1` 可穿越長城。哪個號碼是哪個兵種沒有證據 |
| 1a | **NWMAP 的旗標** | bit 15–12 語意未解。城市一定帶旗標（`0x4000` 60 個／`0x2000` 31 個），高山與沙漠一定不帶。`0x4000` **不是**兵工廠（39 省只有 9 個吻合，是巧合）|
| 1b | **`.DT1` 的 1447 之後** | 前段 4..1446 已解（39×37 省份表）。其後 13,236 B 未解——將領區與其他狀態。檔尾 22 B 有個 `Seek` 單獨讀的欄位 |
| 1b | 其餘 13 個政略指令 | 已確定 1=調動、6=查閱。**一次 timeline 只測一個**，別靠 ESC 回主選單（ESC 語意不明，上次因此誤判，見 `docs/playtest/02` §5.4）|
| 1c | **`.DT2` 的部隊欄位** | 469 B 結構已解、`+28/+48` = 攻守參戰單位、**`+468` = 攻方來源省**（`docs/re/07` §5）。兩個 200 B 區已訂正為 **100 × u16**，但每格裝什麼未解。**要打進實際戰鬥取 `.DT2`**，而那條路目前被按鍵問題擋住（`docs/playtest/03`）|
| 1h | **修 DOSBox 輸入** | 候選：Xvfb 裡跑輕量 WM（最正規）、用 DOSBox 的 `-c`/mapper 繞過 X 層、或直接改存檔進入想要的狀態 |
| 2 | 模組間交接 | `GRT`→`WAR`→`SR`→`GRTE` 靠哪些檔案傳狀態。`Config.dat`（2 B，四支都讀）是最可能的媒介 |
| 2b | `SDFA.EXE` 解包 | IDA 自動解包對這支失效（0 函式）。要動態 dump 或自寫解包器。優先度低 |
| 3 | `MAN(N).DAT` 欄位 | 33 B/人，已解 `+0/+1/+2` 能力值、**`+4` 所屬省**、`+17` 兵力。**其餘 27 B 待解**——詞表列出至少八個屬性（能力/經驗/體力/士氣/戰力/戰技/武裝/帶兵）|
| 3b | 省份記錄剩餘欄位 | `+32` 解出四個位元（bit 2 已處理／bit 6 交戰中／bit 7 已徵稅／bit 1 未解）。**`+33..36` = 產能分配百分比**（`docs/re/26`），但**哪一格對哪種資源未定** |
| 3c | 三份 `TOWN` 的差異 | 三期初始資料多數欄位相同，只有嫩江省黃金不同。完整 diff 未做 |
| 4 | `byte_6FE88` 語意 | 值 1/4 才載字模，推測是幕別或階段（`docs/re/01` §2）|
| 5 | `sub_10AB5` | MARK-A → MARK-C 換片流程，確認是否有主動防拷 |
| 6 | 戰鬥規則 | 機動力已解；**地形防禦已驗**（`docs/re/08` §3a）。矛盾點 (1)「方格 vs 六角」**已解消**（§5.8，是 column-staggered 六角）；(2) 仍成立：`WAR.EXE` 裡唯一的數字鍵分派是 `sub_38DFE` 的**七項選單**，不是六方向——見 `docs/mechanics/30-combat.md` §3 |
| 6a | **電腦 AI 決策** | ✅ **政略側大致完成**：兩條決策鏈全解、決策鏈 A 的十支判斷函式全解、出兵的挑目標→湊兵→閘門整條鏈解完（`docs/re/28`／`docs/re/30`）。**剩下的是戰鬥中的 AI**（怎麼走位、怎麼選攻擊對象、撤退門檻）與低難度湊兵的四支子函式 |
| 6b | **政略流程各步驟的內容** | **優先序鏈已解**（`70-ai.md` §6d），分岔條件確認是 `sub_5B7DC` = 有沒有敵對鄰省。**`sub_150FC` = 調動已全解**（§6e）。**`sub_1527A` = 增援前線**（§6f）、**`sub_14761` 守門**（§6g）已解。**六支全部解到方向**（§6e–§6k）：五支是調動的變體、一支是攻打。**三道守門已讀**（§6m）：`sub_1401E` = 目標省比當前省窮、`sub_36BC7` = 硬編碼五省清單、`sub_13F4E` = 掃鄰省。`sub_5B76E` = 鄰接檢查已解。另 `sub_15CE3`／`sub_1408F`／`sub_5BF74` 的權重函式未讀 |
| 6d | `byte_6FFCA` 剩餘位元 | **bit 4–6** = 難度加成（`70-ai.md` §6c）；**bit 2** = 啟用決策鏈最後三步（§6d）；**bit 1** = 讓離島三省跳過決策鏈 A ＋ 啟用步驟 5（`docs/re/28` §2）；**bit 0** = 救援調動在某條件下用模式 7 而非 5（`docs/re/11` §6）。bit 3／7 未見使用。誰寫這個 byte 未追 |
| 6e | 24 格 word 陣列 | `sub_15018` 遍歷 `ds:[j*2 - 534Ah]` 的 24 格清單查將領。24 對不上勢力數(10) 或省數(39)，裝什麼未知 |
| 6f | ⚠️ **執行期結構 ≠ 檔案結構** | `sub_15018` 讀 `將領[id].+0` 的 bit 0，但檔案的 `+0` 是 0–100 的能力值，檢查奇偶無意義。**不要把檔案偏移直接套到記憶體偏移**（`70-ai.md` §6）|
| 6c | `byte_7003A`／`byte_7003B` | 戰鬥 gate 的另外兩個條件，方向相反（`sub_5C7FE` 裡是否決、主迴圈裡是肯定）。找所有寫入點 |
| 7 | 存檔格式 | `SAVE(1)` 1993-03-23、`SAVE(2)` 1993-01-10，兩份可做 diff；`MEM_WAR.DAT` 與 `.DT2` 同為 18,291 B |
| 8 | `NEWSDATA` 字型 | 17 張新聞圖用倚天反查全部落空，字形較粗、字高 14，來源待查 |
| 10 | 開場動畫轉場 | `.GLB` 的圖與截圖索引對不上（Jaccard ≈ 0.01），轉場機制未確認 |
| 11 | 找其他 `.GLB` | 只有 `GRT`／`GRT2` 兩組，`WAR`／`GRTE` 的 256 色圖裝在哪未查 |
| 12 | **`AC.TPC` 的 `palt` → `.RGB`** | 17 張戰鬥插圖各帶自己的 `palt`（16 個 DAC 索引），但要配哪個 `.RGB` 檔才是真實顏色，這條鏈未接 |
| 13 | 整理外部資料 | 巴哈攻略百科等頁面人工抓下來放 `docs/reference/`，標來源與日期 |
| 14 | `JAPAN1` vs `JAPAN2` | 各 85 人，兩檔差異未比對 |

### 6.2 已建好的工具

| 工具 | 用途 |
|---|---|
| `tools/py.sh` | Python 執行包裝（docker + uv venv，venv 持久化在 `workplace/.venv-py`）|
| `tools/go.sh` | Go 執行包裝（docker，自建 `dsds-go` image 含 X11/OpenGL 供 Ebiten CGO 用）|
| `tools/ida.sh` | IDA Pro 9.4 headless（`analyze` / `script` / `raw`）|
| `tools/dosbox.sh` | DOSBox oracle。沿用現成 image，原版唯讀，寫入導到 `workplace/dosbox/drive_c` |
| `tools/dosbox_runner.sh` | 容器內的 Xvfb + DOSBox + timeline 送鍵截圖（`wait`／`key`／`type`／`shot`）|
| `tools/deny_scan.sh` | 原版資產 deny-list 掃描。commit 前與發行前都要跑 |
| `tools/eten.py` | 倚天字庫讀取 + Big5 分區索引（含 oracle 自我驗證）|
| `tools/glyph_lookup.py` | 字模反查 Big5（`oracle` / `dump` / `match`）|
| `tools/glyph_slots.py` | 槽寬偵測與詞條切分 |
| `tools/entropy.py` | 全檔熵與結構特徵掃描（判「壓縮或加密」前先跑這支）|
| `tools/dcl.py` | PKWARE DCL implode 解壓（`blast.c` 移植，過官方測試向量）|
| `tools/glb.py` | `.GLB` 圖庫解包（`list` / `extract`），輸出 PNG |
| `tools/bgi.py` | BGI `getimage` 解碼（`.TPC`／`NEWSDATA.DAT`）與 `.RGB` 調色盤 |
| `cmd/assetdump` | Go 版解碼器的 CLI：把原版素材匯出成 PNG（139 張），端到端驗證用 |
