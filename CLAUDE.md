# 大時代的故事 remake（漢堂國際資訊，1992）

## ⚡ 動手前先讀 CONTEXT.md

`CONTEXT.md` 是全專案的單一入口：現況一覽、文件索引、術語表、oracle 優先序、
**已被推翻的斷言清單**、worklist。對話被壓縮或新 session 接手時先讀它。
（目前尚未建立——第一輪工作就要把它生出來，格式照本檔 §11。）

這份 `CLAUDE.md` 只放「不論做什麼都要遵守」的目標、原則與硬規則。

**反組譯一律先用 IDA Pro 9.4**（環境與規則見 §4.1），Ghidra 只當交叉驗證。

---

## 1. 專案目標

把漢堂國際資訊《大時代的故事》(1992, DOS) 完整逆向，在 **Go / Ebiten** 上乾淨重寫成
可跨平台執行的引擎，還原原版繁體中文文本，並另做**英文版與日文版**。定位是文化資產保存
——這是台灣自製戰棋的代表作之一，也是漢堂走上正軌的那一部。

四條主線：

1. **文本還原**——這一款最難也最關鍵的一條，見 §5。原版畫面上的中文**不是 Big5**，
   是字模索引，原文必須從點陣字模反查回來。
2. **資料格式全解**：`.TPC` 圖像、`.15` 字模、`.RGB` 調色盤、`.MUS`／`.TIM` 音樂、
   `.DAT` 資料表、`.GLB` 封裝、`.DT1`／`.DT2` 存檔。
3. **規則還原**：五個執行檔各自的模組行為，行為對齊原版。
4. **引擎重寫**：Go + Ebiten，跨平台，不依賴 DOSBox；外掛英文與日文語系。

### 定位：還原 + 現代化外殼

核心規則一律對齊原版。外殼允許現代化——高解析呈現、視窗縮放、現代操作、存檔相容——
但**每一項改動都要在文件裡標記為 remake 差異**，且不得默默改動遊戲規則。

### 不做的事

- 不散布原版執行檔、資料檔、美術或音樂。公開產出只有引擎程式碼與翻譯文本。
  原版資料一律 gitignore。
- 不做玩法設計改動。這是保存專案。

---

## 1.5 這款遊戲是什麼（網路查證，2026-08-01）

| 項目 | 內容 | 來源 |
|---|---|---|
| 發行商 | 漢堂國際資訊 | 維基百科 |
| 發行日 | 1992-10-15（臺灣）| 維基百科 |
| 平台／類型 | MS-DOS／回合制策略（戰棋）| 維基百科 |
| 背景 | 民國初年，國民革命軍北伐與對日抗戰 | 維基百科 |
| 分期 | 北伐時期、抗戰前期、抗日後期 | 維基百科 |
| 程式設計 | 林明輝、沈友瑋 | 維基百科 |
| 音樂 | 吳宗凱、游戲工場 | 維基百科 |
| 定位 | 漢堂第二部原創作品，扭轉前作《隋唐群雄傳》的評價；此後才有《天外聖劍錄》《炎龍騎士團》 | 維基百科 |
| 精神續作 | 2009 年鄭立以本作為藍本製作《民國無雙》 | 維基百科 |

已知的玩法輪廓（**社群資料，oracle 位階最低，只能當「往哪裡找」的提示**）：

- 回合制，各勢力輪流下政略指令；**每個省份有命令數上限**，用完換下一個省份。
  ✅ 命令數上限機制已實測確認（畫面右下顯示「指令數：N」）。
- 命令 5 是徵兵，依將領徵召兵種；另有整編可在部隊間分配兵力。
  ✅ **「命令 5 = 徵兵」已實測驗證**，完整 15 個指令清單見 `docs/playtest/02` §5。
- 兵種滿員數：步兵師 20,000、騎兵師 10,000、炮兵旅 2,000、裝甲部隊 200。
  ✅ **四個全部驗證通過，而且有兩個獨立來源**：資料側是 `MAN(N).DAT` 三期的
  最大值（`docs/spec/02` §4），程式碼側是 `sub_18A67` 把四個數字直接寫在
  `cmp` 裡（`docs/re/32` §7）。兵種欄位是將領記錄的 **`+21`**，
  值域 1 步兵／6 騎兵／4 砲兵／5 裝甲。
- 戰鬥用**六角格**，數字鍵 1–6 對應六個方向移動游標與部隊；有機動力概念，地形影響防禦。
- 其他被提到的機制：政體（如「德模克拉希」）、政治研究、省份開發、將領忠誠度。

**這張表的每一個數字都要在執行檔或資料檔裡找到出處才算數。**
社群攻略常混入不同修改版的數值——`20000`／`10000`／`2000`／`200` 這四個滿員數是很好的
grep 起點，但**搜不到是正常的**（可能是公式算出來的，見 §7 第 2 條）。

### 中文資料索引

| 資料 | 網址 |
|---|---|
| 維基百科：大時代的故事 | https://zh.wikipedia.org/zh-tw/大時代的故事 |
| 巴哈姆特 攻略百科（遊戲簡介）| https://wiki2.gamer.com.tw/wiki.php?n=19132 |
| 巴哈姆特 遊戲介紹（a91000800）| https://home.gamer.com.tw/artwork.php?sn=3916730 |
| 巴哈姆特 論漢堂 | https://home.gamer.com.tw/artwork.php?sn=1724320 |
| 民國無雙 哈啦板（大時代心得串）| https://forum.gamer.com.tw/C.php?bsn=23236 |
| 遊戲基地 討論串 | https://www.gamebase.com.tw/forum/1207/thread/12070002 |
| 中文 DOS 遊戲資料庫 | https://cdosgame.simagame.me/ |

巴哈姆特的頁面對自動抓取回 403，要人工開瀏覽器看。**抓回來的攻略內容整理進
`docs/reference/`，並標明來源與抓取日期**，不要散在對話裡。

---

## 2. 兩條硬性原則

1. **完整性 > 投報。** 不得以「成本高、效益低」為由跳過任何素材、任何格式。
   卡關就換方法，記錄「卡在哪、試過什麼」，不寫「暫緩／低投報」當結論。
2. **SDD：spec 齊了才實作。** 反組譯 → 收攏成規格（`docs/spec/`）→ 才動手寫程式。
   **只有標 READY 的規格可以動手。**

---

## 3. 已確認的事實（2026-08-01 首輪偵查）

素材：`game/`（約 130 檔）與 `大時代的故事-jsdos.zip`（同一份，js-dos 打包）。
檔案時間戳全是 2021-12-11，**是重新打包的日期，不是原版日期**。

### 3.1 執行結構

`play.bat` 依序跑五個執行檔：

```
sdfa > nul     →     grt     →     war     →     sr     →     grte
```

| 檔案 | 大小 | 觀察 |
|---|---|---|
| `WAR.EXE` | 375,568 B | 最大的一個，**未打包**，**Borland Turbo Pascal**（RTL + Graph unit，見 `docs/re/01`）。字串表裡有大量小寫檔名（`book1.tpc`、`menu1.tpc`、`head1.rgb`）與 `b:` 前綴的雙磁碟路徑 |
| `GRT.EXE` | 34,708 B | PKLITE 打包，**IDA 自動解得開**。**Borland C++ 1991**，178 函式。GF 資源載入（`grt.GTB`）、DCL 解壓 |
| `GRTE.EXE` | 57,168 B | 同上，**Borland C++**，191 函式。與 `GRT` 幾乎同一套程式但用 `grt2.GTB` + `final` 音樂 → **結局** |
| `SDFA.EXE` | 10,243 B | PKLITE 打包，**IDA 解不開**（0 函式 0 字串）。`play.bat` 第一個跑且丟棄輸出，疑似環境偵測 |
| `SR.EXE` | 36,288 B | **Turbo Pascal**，118 函式。**劇情過場**：`scene141/142/271/272.tpc` + 五首音樂 |

反組譯前 PKLITE 那三個原本要先解包，**但實測 IDA 9.4 對 `GRT`／`GRTE` 會自動解開**，
只有 `SDFA` 解不開。`WAR.EXE` 沒打包，直接進 IDA。

DOSBox 設定 `machine=svga_s3`，但這是打包者選的，不代表原版需要 SVGA。

### 3.2 繪圖：BGI 與 mode 13h **兩種都用**（DOSBox 實測）

`EGAVGA.BGI` 是 Borland Graphics Interface 的 EGA/VGA driver 2.00（1988-03-21）。
主遊戲用 Turbo C 的 BGI 繪圖，**640×350、16 色 planar**。

但開場動畫與標題畫面是 **320×200、256 色（mode 13h）**——2026-08-01 的 DOSBox
實測量到視窗在標題階段是 320×200、單張畫面 217 個獨立色，進主選單才切到 640×350。
所以**不是「BGI 一套走到底」**，兩種模式並用。呈現層要兩套都做。
詳見 `docs/playtest/01-dosbox-probe.md`。

開場的 256 色圖不可能是 `.TPC`（`.TPC` 是 4-plane 16 色，§3.3 已驗算），
存在別處；`GRT.GLB` 的高熵是候選解釋。

**這件事對專案是好消息**：BGI 的 API 語意是公開的（`getimage`／`putimage`／
`setpalette`／`setfillstyle`），反組譯時可以直接對照函式語意，不必從零推繪圖管線。

### 3.3 `.TPC` = BGI `getimage` 緩衝區（已驗證）

檔頭是兩個 word：`width-1`、`height-1`，其後是 4 個 bit-plane 的 planar 像素。
用 `size == 4 + ceil(w/8) * h * 4` 逐檔驗算：

| 檔案 | 大小 | 解出的 w×h | 公式差額 |
|---|---|---|---|
| `CHOOSE2.TPC` | 47,404 | 395×237 | **0** |
| `CHOOSE31.TPC` | 52,996 | 571×184 | **0** |
| `HEAD1.TPC` | 59,844 | 640×187 | **0** |
| `HEAD2.TPC` | 52,804 | 640×165 | **0** |
| `BOOK1.TPC` | 7,064 | 637×22 | +20 |
| `FRAME1.TPC` | 664 | 10×80 | +20 |
| `AC.TPC` | 94,248 | 178×60 | +88,728 |

三種形狀：差額 0 的是單張純圖；差額固定 +20 的那批多帶 20 byte（尾端調色盤或 padding，未解）；
`AC.TPC` 差額巨大，**是多張圖串接的圖集**，第一張只是開頭。`w=640` 直接印證 BGI 640 寬。

### 3.4 `.15` = 16×15 中文點陣字模，每字 30 byte（已驗證）

`TN15.1`–`TN15.39`、`1.15`–`4.15`、`FAN(1)(2)(3).15`、`JAPAN1.15`／`JAPAN2.15`、
`MAN115`／`MAN215`／`MAN315`——**51 個檔案的大小全部整除 30**，且照 16×15
每列 2 byte 直接渲染就是可辨識的中文字形。單字數從 35 到 822 不等。

> 注意 `TN15.9` 的副檔名是 `9` 不是 `15`，`ls *.15` 這種篩法會漏掉大半。

這是倚天 `STDFONT.15` 那一族的格式（`/home/anr2/cht/etan_font/` 有現成素材），
但**這些檔案是每個場景各自帶一份子集**，不是完整字庫。
全部 51 檔已還原完畢，見 `docs/formats/01-glyph-text.md`。

### 3.5 文字不是 Big5，是定長槽位的字模序列（已解）

五個執行檔、`GRT.GLB`、所有 `.DAT` 用 Big5 解碼，**中文命中數是零**。
原因不是「存了索引」，而是**原始碼裡本來就沒有碼點**：

> 字模序列本身就是文本。每 `w` 個字模為一個詞條（`w` 依檔案固定），
> 不足的格子填全零字模。程式從 `k × w × 30` 直接取 bytes 送 BGI `putimage`。

51 檔 6,174 個字模已全量反查：倚天命中 4,799、空白填充 1,374、
例外 1（`1.15` 的逗號，是倚天「，」左移 3 px 下移 2 列的版本）。
**字形 100% 出自倚天字庫，沒有自製字。** 詳見 `docs/formats/01-glyph-text.md`。

還沒解的是**敘事文本**（事件、新聞、劇情）——這 51 檔只有地名、人名、番號、UI 詞彙。
候選位置是 `NEWSDATA.DAT`（29,784 B，不整除 30）與 `GRT2.GLB`（138,660 B，整除 30）。

### 3.6 其他已知

- `.RGB`（8 個）是**純文字**檔：`5 5 5\r\n` 這種十進位三元組，VGA DAC 0–63 的調色盤。
- `.MUS`／`.TIM` 成對，共 **8 組**（原本這裡只列了 5 組，漏掉 `MAINTHEM`、
  `STRATEGY`、`WALL`）：`MAINTHEM`(31,227 B，最大，主題曲)、`BATTLE1`、`BATTLE2`、
  `SCENE`、`FINAL`、`BT02`、`WALL`、`STRATEGY`。檔名對得上場景：政略、戰鬥、過場、結局。
  `BT02.TIM` 裡有 `bass2` 這種樂器名字串——**已 confirmed**：`.TIM` 就是
  AdLib Timbre Bank（OPL 雙運算元 FM 參數），`.MUS` 是 AdLib MUS 事件流。
  詳見 `docs/formats/06-mus-tim-audio.md`。
- `GRT.GLB` 239 KB，熵 **7.96 bit/byte** → 壓縮或加密過的封裝，不是裸資料。
- `PLACD.SAV` 1.2 MB，是所有檔案裡最大的一個，用途未知。
- `SAVE(1)`／`SAVE(2)` 各有 `.DT1` 與 `.DT2` 兩份，可拿來做存檔 diff。
- `WAR.EXE` 裡有 `Please remove Disk MARK-A from A:` → **有磁片／防拷檢查**。
  這在大富翁2 專案是擋住 DOSBox oracle 的路障，這裡要提早處理。
- `CHKLIST.CPS` **不是遊戲檔**，是當年 Central Point 防毒軟體的校驗檔殘留。不要花時間解它。

### 3.7 未知（誠實標記，不要靜悄悄變成前提）

`.GLB` 封裝格式、`PLACD.SAV` 用途、`.DAT` 十二個資料表的語意、
`.GTB`（2 個，200–380 B）、`SR.EXE` 的來歷、`.TPC` 那 20 byte 的差額。

✅ **`.MUS`／`.TIM` 已全解**（`docs/formats/06`，READY）：音源是
**AdLib／OPL2（YM3812），埠 0x388–0x389**——三條獨立證據（`GRT.EXE`
`sub_144C3` 是教科書等級的 AdLib 偵測程序、`.TIM` 是公開文件化的
AdLib Timbre Bank、聲部 0–5 旋律 + 6–10 打擊吻合 OPL2 rhythm mode）。
兩者都**不是自製格式**，是公開的 AdLib MUS + Timbre Bank，全部欄位對上。
旋律可完整取出（`tools/mus.py midi`）。播放走 TSR 掛 **INT 66h**。

五個執行檔的角色（2026-08-01 反組譯，見 `docs/re/02`、`docs/re/03`）：

**`WAR.EXE` 是遊戲本體**——政略、戰鬥、存檔、資料表全在裡面，引用 80 個以上的資料檔
（含 `save(1..10).dt2`）。另外三支各自只引用兩三個檔，是播放器：
`GRT` = 開場圖庫、`SR` = 劇情過場、`GRTE` = 結局。`SDFA` 未解包（環境偵測）。

原本的假說「`GRT` 政略主程式、`WAR` 戰鬥模組」**是反的**，已推翻。`play.bat` 把它們串成一條鏈，代表模組間靠**檔案**交接狀態
（`Config.dat`、`PLACD.SAV` 是候選）。這條交接鏈解開之前，不要假設任何模組可以獨立執行。

---

## 4. Oracle 優先序

1. **DOSBox 跑原版實測**（固定存檔、固定操作序列、截圖）。
2. **執行檔反組譯**（PKLITE 先解包）。
3. **官方說明書／當年雜誌攻略**。
4. 社群 wiki、巴哈攻略百科、實況影片、他人的修改版與《民國無雙》——最低，
   只能當「往哪裡找」的提示，不能當結論。**修改版尤其危險**：這款遊戲三十年來
   有大量愛好者修改版流傳，社群寫的數值不一定出自 1992 原版。

衝突時記錄在 `CONTEXT.md` 的「已被推翻的斷言」區，寫清楚誰推翻誰、憑什麼。

### 4.1 反組譯工具：`[HARD]` IDA Pro 優先

**反組譯一律先用 IDA Pro 9.4**，Ghidra 只當交叉驗證。

```sh
mkdir -p workplace/ida
cp --reflink=auto workplace/orig/game/WAR.EXE workplace/ida/WAR.EXE
sha256sum workplace/ida/WAR.EXE

docker run --rm -v "$PWD/workplace/ida:/work" -w /work \
  ida-pro-9.4-ver2 idat -A -B WAR.EXE                    # headless 產 .i64 + .asm

# IDC 稽核腳本
docker run --rm -v "$PWD/workplace/ida:/work" -v "$PWD/tools:/work/tools" -w /work \
  ida-pro-9.4-ver2 idat -A -S/work/tools/<script>.idc WAR.EXE.i64
```

image 來源 **`/home/anr2/ida_94_official/dist`**，image 名稱 **`ida-pro-9.4-ver2`**，
16-bit DOS loader。**完整環境說明與踩過的坑見
`~/.claude/knowledge-base/retro/ida-pro-9.4.md`**（已掛進 `rules/00-rules-index.md`
的觸發表，「反組譯」這個情境會自動命中）。

### ⭐ 不要 grep `.asm`，要查 `.i64`

`.asm` 是攤平的文字，**沒有交叉參考圖**。想知道「某個全域變數是什麼」，
grep `.asm` 只能從呼叫端的參數順序反推——那是間接證據，會推錯
（`word_64944` 就是這樣被推成「第二方代表單位」，後來出現反證退回未解）。

`tools/ida_xref.idc` 直接查 IDA 的 xref 圖：

```sh
tools/ida.sh raw idat -A "-S/work/tools/ida_xref.idc word_64944" WAR.EXE.i64
cat workplace/ida/xref-word_64944.txt
```

三條硬知識（實測 2026-08-01）：

1. **IDAPython 在這個 image 跑不起來，要寫 IDC。** 兩種寫法都試過，無輸出。
2. **headless 的 `print`／`Message()` 不進 stdout**，腳本一律把結果 `fopen` 寫檔。
   不寫檔就等於沒跑，而且 exit code 還是 0。
3. **讀寫判定用 `XrefType()`**（`dr_W=2`／`dr_R=3`／`dr_O=1`），
   不要比對助憶碼字串（多空格會漏），也不要看 `print_operand(x,0)`
   （`push` 的第 0 個運算元是來源不是目的，會把 30 個 push 全判成寫）。
4. **xref 只涵蓋直接參考**。`ptr = &x` → `es:[di] = v` 這種間接寫入抓不到，
   症狀是「讀 74 處、寫 1 處」——看到寫入數異常少，先看「取位址」那幾筆。

三條配套規則：

1. **PKLITE 打包的三個要先解包**（`GRT`／`GRTE`／`SDFA`），否則只會看到解壓 stub。
   `WAR.EXE` 沒打包，**從它開始最省事，而且它是最大的那個**。
2. **每份筆記都要標「輸入檔 + SHA-256 + IDA 位址」**。位址一律寫 IDA database 的
   linear address，同時引用 Ghidra 位址時要明講是哪一套。
3. `.i64`、`.asm`、解包後的 binary 全部 gitignore。

Turbo Pascal 編譯的程式有一個額外優勢：Graph unit 的呼叫（`PUTIMAGE`／`SETRGBPALETTE`）
會被 IDA 直接認出名字，不必自己辨識。但**呼叫慣例是 Pascal**（參數由左至右壓棧、
被呼叫者清棧），字串是長度前綴——用 C 的直覺讀參數順序會全部反過來。

---

## 5. 文本還原：這個專案的核心難題

> **2026-08-01 更新**：詞表層（51 個 `.15` 檔）已還原完畢，而且比原本假設的簡單——
> 沒有索引那一層，字模序列直接就是文本（§3.5）。下面這條含索引的還原鏈**只剩敘事文本適用**，
> 因為敘事文本至今沒找到，仍可能是索引流。步驟 1、3、4 對兩者都適用。

原文並不以文字形式存在於任何檔案裡，而是散在字模與索引之間。還原鏈是這樣：

```
場景字模檔 (TN15.N) ── 逐字比對 ──▶ 倚天 STDFONT.15 全字庫 ──▶ Big5 碼點
                                                                  │
資料檔／執行檔裡的索引序列 ─────────────────────────────────────┘
                                          ▼
                                  可讀可改的繁中原文
```

具體做法：

1. **字模反查**。`TN15.N` 的每個 30 byte 字模，拿去跟倚天 16×15 全字庫
   （`/home/anr2/cht/etan_font/`）逐 byte 比對。完全相同 → 直接得到 Big5 碼。
   不同 → 可能是遊戲自製字或改過的字形，列成例外清單人工判讀，**不要跳過**。
2. **索引來源**。找出「畫面上第幾個字用第幾個字模」的那串索引存在哪裡
   （`.DAT`？`.GLB`？執行檔內？），從 BGI 繪字的呼叫端往回追。
3. **round-trip 驗證**。還原出來的原文重新編成索引 + 字模，要能畫出與原版截圖
   逐像素相同的畫面。**這是唯一可信的驗收標準**，肉眼看起來對不算數。
4. **例外要記帳**。哪些字反查不到、哪些場景字模數對不上索引範圍，全部寫進
   `docs/formats/`，不要默默略過——那些通常就是特殊符號或自製字。

先做一個場景把整條鏈打通，再擴到全部 46 個字模檔。鏈沒通之前不要碰 §6。

---

## 6. 多語系：繁中母本 + 英文 + 日文

- **繁中是母本，且是 §5 還原出來的原文，不是重寫的。**
- **原版的字模索引機制到 remake 就結束了。** 重寫版用真正的字串表 + 完整字型，
  不再沿用「每個場景帶一份字模子集」——那是 1990 年代磁片容量的產物，不是設計意圖。
  原版機制仍要完整記錄在 `docs/formats/`（那是保存的一部分），但不進 remake 的執行路徑。
- **所有玩家看得到的文字都在語系資料檔，不進 Go 原始碼。**
- 英文版不是把中文換掉就好：640×350 的版面是照全形字排的，換成比例字後
  字寬、行高、對話框全都要重算。**排版層必須先抽離**，否則英文一定溢出。
- 日文版可沿用點陣字路線（`JAPAN1.15` 已經存在，先確認它到底是日文字模還是別的東西）。
- 譯名表 `translations/glossary.md` 是唯一真相，品質閘要檢查譯名漂移。

---

## 6.5 遊戲機制文件化 `[HARD]`

**每解出一條遊戲機制，就要按屬性歸檔到 `docs/mechanics/`。**
這不是「有空再補」的附加工作，是與程式碼同等的交付物——
remake 完成之後的優化（尤其是**改進電腦 AI**）全靠這批文件。

### 為什麼

規格檔（`docs/spec/`）寫的是「資料長什麼樣」，反組譯筆記（`docs/re/`）
寫的是「程式碼在哪」。**兩者都不回答「這個遊戲是怎麼運作的」**。
機制文件補的就是這一塊，而且要按屬性分類，讓人能照主題查。

### 分類

| 檔案 | 屬性 |
|---|---|
| `00-index.md` | 索引與狀態總表 |
| `10-political.md` | 政略：15 個指令各自做什麼、命令數上限 |
| `20-military.md` | 軍事：出兵流程、部隊編成、兵種 |
| `30-combat.md` | 戰鬥：六角格移動、機動力、地形修正、戰損公式 |
| `40-economy.md` | 經濟：六種資源的生產與消耗、人口、開發 |
| `50-diplomacy.md` | 外交：結盟、談判停火、秘密行動 |
| `60-personnel.md` | 人事：將領忠誠度、任免、能力值怎麼用 |
| **`70-ai.md`** | **電腦 AI 的判斷邏輯**——優先度最高，見下 |
| `80-victory.md` | 勝負判定、事件、時期推進 |

### `[HARD]` AI 決策是重點，看到就記

**電腦勢力怎麼想，是這批文件裡最重要也最難補的一塊。**
未來要調整難度、改進電腦對手、或做 remake 專屬的強化 AI，都需要先知道
原版是怎麼判斷的。

反組譯時只要碰到下列東西，**當場記進 `70-ai.md`**，不要想著之後再回頭補：

- 電腦勢力選指令的優先序（為什麼這回合徵兵而不是攻打）
- 出兵目標怎麼選（挑最弱的鄰省？挑資源多的？挑通往首都的路？）
- 兵力配置、派將的挑選規則
- 撤退／求和的門檻
- 任何帶 `Random` 的判斷——亂數在哪裡介入、範圍多大
- 難度相關的參數（如果有）

### 每條機制的格式

一律標推論等級（`CLAUDE.md` §9）：**confirmed / 強證據 / 假說 / 未知**，
並寫明**是在哪個執行檔的哪個位址驗的**，或是哪一次實機測試看到的。

**不准為了讓文件看起來完整而編機制。** 未知就寫未知，附上「下一步該從哪裡挖」。

---

## 7. 從前兩個 remake 專案帶過來的教訓

冬之魔與大富翁2 反覆踩過的同一組錯誤，這裡先寫死：

1. **動手挖之前先查手上已有的。** 第一動作是 grep `docs/`、程式碼、攻略、譯名表，
   不是讀組合語言。但 grep 落空 ≠ 不存在。

   **反組譯有兩份索引，動手前兩份都要查**：

   | 手上拿到的是 | 查 | 產生方式 |
   |---|---|---|
   | `sub_XXXXX` | `docs/re/00-function-index.md` | `./tools/py.sh tools/gen_func_index.py` |
   | **位址／偏移**（`[di-6221h]`、`7A8Bh`）| **`./tools/py.sh tools/addr.py -6221h`** | 手維護三張表 |

   ### ⭐ 為什麼位址索引比函式索引更重要

   `word_64944` 被推錯**兩次**，而答案**四份文件裡都有**：

   ```
   docs/re/10 L202   mov [di-6221h], cx  ; 省份[目標].+20 = 我方勢力
   docs/re/20 L22    省份記錄基址 -6235h、將領記錄基址 +7A7Dh
   docs/spec/03 L59  +20 u16 = 司令
   internal/game/combat.go   +14 = 效忠的勢力領袖
   ```

   **答案是完整的，缺的是反向索引。** 專案早就有函式索引，但讀組語時
   手上拿到的是**位址**——最需要的那份索引剛好缺席，於是「已經有答案」
   變成「以為沒有答案」，只好從呼叫端的參數順序硬推，推錯。

   > **不要用 `awk`／`sed` 直接 dump 組語，用 `tools/dump_func.py`。**
   > 它會自動把每一行的位址翻成語意，附出處與推論等級：
   >
   > ```
   > mov     ax, [di-6221h]
   >                     ; ← 省份記錄 +20：司令（將領 ID，1-based；0 = 無主）
   > mov     word_64944, ax
   >                     ; ← 全域：當前交戰省的司令
   > ```

   ### ⛔ 為什麼**不**在 IDA 裡把變數改名

   事後檢討 `word_64944` 時，第一個想到的解法是「把已解出的全域變數在 `.i64`
   裡改名，讓 `.asm` 自帶語意」。**那個方案被否決了**，兩個理由：

   1. **蓋不到出錯的那一類。** 當初看不懂的不是 `word_64944`（它有符號），
      是 `mov ax, [di-6221h]`——**`di` 相對偏移沒有符號，IDA 無從命名**。
   2. **命名是破壞性的，而且名字不帶推論等級。** 改名之後原始位址被覆蓋，
      名字讀起來像事實。`word_64944` 推錯過兩次——要是第一次就命名成
      `g_side2_unit`，那個錯誤會被烙進之後每一次 dump，而且名字本身
      會讓人不去質疑它。這是 §9「推論標籤要誠實」的反面。

   > **註記要非破壞性：原始位址留著，語意是附加的，而且一定要帶推論等級。**
   > `dump_func.py` 對非 confirmed 的欄位會印 `⚠️[強證據]`／`⚠️[假說]`。

   索引解決「查得到」，但沒解決「**想到要查**」——`word_64944` 那次的失敗
   模式正是「我以為已經知道答案了，根本沒想到要查」。而紀律靠不住：
   §7.1 白紙黑字寫著先查手上已有的，這個 session 還是違反了五次。
   **所以把查詢併進 dump 流程，讓它不需要紀律。**

   ### 解出新東西就回填三張表

   `tools/addr.py` 的 `BASES`／`FIELDS`／`GLOBALS` 是**手維護**的，故意的——
   從 markdown 自動抽會把「假說」跟「confirmed」混在一起，而這張表要能
   直接拿來下判斷。**每一列都標推論等級**，解出新欄位就加一列。

   ### ⏳ 這條方法論還在驗證期（使用者指示 2026-08-01）

   有效才升級，沒驗證過的方法論不進全域規則。**驗收條件寫死如下**：

   > 中文化完成時，回頭數 `CONTEXT.md` 的「已被推翻的斷言」清單——
   > **`tools/dump_func.py` 導入（2026-08-01）之後新增的項目裡，
   > 不得再出現「已經有答案卻推錯」這一類。**

   那份清單本來就在維護，直接當計數器用，不必另外記帳。
   目前這一類的既有案例：`word_64942`／`word_64944`（推錯兩次）、
   `sub_54DAC` 等五次重讀已解函式。

   通過就整理進 `~/.claude/knowledge-base/retro/` 與
   `~/my_skill/reverse-engineer-retro-game-remake/`；沒通過就檢討為什麼
   工具沒擋住，而不是把它當成已經有效的東西搬走。
2. **掃常數，不要掃結果。** 老軟體的數字是公式算出來的，搜不到觀察值是正常的。
2.5 **不要用 `grep -v` 過濾組合語言。** 為了讓輸出短而濾掉
   `mov di, ax`／`add di, ax`／`shl di, 1` 這類「看起來是樣板」的行，
   濾掉的正是**索引計算**，會把 `旗標[f(x)] = 1` 讀成 `旗標[0] = 1`。
   要短就用 `sed -n` 取行段。（2026-08-01 兩次差點誤讀，見 `docs/re/31` §28。）
3. **寫下一個值之前先 grep 它。** 新結論與既有常數衝突，是最便宜的錯誤偵測。
3.5 **省編號、將領編號這類對照表一律從資料取，不要憑印象編。**
   2026-08-01 驗證兵工廠分佈時，第一版用了憑記憶編的省編號表
   （以為 #1 是遼寧、#26 是湖北），結論完全錯——實際 #1 是嫩江省、
   #11 才是河北。省名在 `3.15` 詞表
   （`translations/zh-Hant/glyphtext.json` 的 `files["3.15"].entries`），
   將領名在 `MAN(N).DAT`。**編出來的對照表會讓整條推論安靜地錯掉。**
4. **一條規則只留一份實作。** 重抄的那份一定會漂，症狀是「看起來完全合理的錯」。
5. **訂正比新結論需要更硬的證據。** 推翻舊結論前，先找出當初支持它的證據，
   解釋它為什麼不成立。
6. **從零建立的路徑會藏 bug。** 從存檔載入時每個欄位都有效，缺口不顯現。
   開新遊戲是一等驗收路徑，而且要看狀態不能只看畫面。
6.5 **原版的哨兵值不等於 Go 的零值。** 原版用 `0xFF` 表示「沒有」的欄位
   （`+5` 戰場格、`+12` 下一跳），Go 的零值是 `0`——**而 `0` 是合法的格編號**。
   漏設就變成「所有單位都要走到格 0」，而且不會報錯，只會安靜地行為錯誤。
   在**唯一入口**正規化（`NewBattleSim`），不要指望每個呼叫端都記得。
   （2026-08-01 被 `TestExecDeployFillsRing` 抓到。）
7. **畫面 bug 測試看不到。** 排版溢出與輸入順序只有實跑＋軌跡抓得到。編譯過 ≠ 視覺驗證。
8. **先量再猜。** 大富翁2 那邊「有些段落被壓縮」的假說，被一次熵掃描證偽——
   全族 3.07–6.59 bit/byte，根本沒壓縮。這裡也一樣：`GRT.GLB` 的 7.96 是量出來的，
   不是猜的。**任何「這東西被壓縮／加密了」的說法，先量熵再說。**
9. **不要跨檔案外推。** 大富翁2 把 `SS.EXE` 驗到的 C runtime 結論外推到 `RUN.EXE`，
   結果後者是 BASIC 編譯的，一整串推論作廢。**這裡有五個執行檔，更容易犯。**
   每個結論標清楚是在哪個 binary 上驗的。
10. **別再猜編碼，從程式碼反追。** 大富翁2 的 `.PXX` 猜了三種 RLE 變體全錯，
    最後只能從繪製分派器的呼叫端反追。**猜三次沒中就停手，去讀反組譯。**
11. **防拷／磁片檢查會擋住 oracle。** 大富翁2 卡在密碼畫面，DOSBox 實測整條線停擺。
    這裡的 `Please remove Disk MARK-A from A:` 是同類東西，**提早排除，不要等到 M4**。

---

## 8. 里程碑

先出 spec，才寫程式。規格檔放 `docs/spec/`，標 `DRAFT` / `READY`。

| | 里程碑 | 內容 |
|---|---|---|
| M0 | 資產解密 | `.TPC`（含 +20 差額與圖集）、`.15` 字模、`.RGB`、`.GLB`、`.DAT`、`.DT1/.DT2`、`.MUS/.TIM`。Go 解碼器 + round-trip 測試 |
| M1 | 文本還原鏈 | §5 整條鏈打通：字模反查 Big5 → 索引來源 → 逐像素 round-trip |
| M2 | 執行檔反組譯 | `WAR.EXE` 起手，PKLITE 三支解包；主迴圈、模組間交接、亂數、存檔讀寫 |
| M3 | 規則規格 | 五個模組各自的玩法規則、資料表語意、勝負判定。**同步產出 `docs/mechanics/`（§6.5）**，含電腦 AI 的判斷邏輯 |
| M4 | Go 引擎（規則層）| 純邏輯，不認識畫面 |
| M5 | 呈現層 | Ebiten、BGI 16 色 planar 還原、音樂音效 |
| M6 | 多語系 | 繁中母本 → 英文／日文，含版面適配 |
| M7 | 打包發行 | 跨平台建置、資產 deny-list 掃描、實機驗收 |

M0／M1 沒完成之前不要碰 M4 以後。

---

## 9. 硬規則 `[HARD]`

- **建置一律走 docker**（Go、IDA、DOSBox、Python 全部）。不裝到系統環境。
- **Python 一律 docker + uv venv**，不污染系統。
- **原版資產唯讀。** 測試存檔一律寫到 `/tmp` 或明確的測試輸出目錄，
  不覆蓋原版 `SAVE(1).DT1`／`.DT2`／`PLACD.SAV`／`Config.dat`。
- **存檔寫回策略是「改寫」不是「重建」**：從原始 bytes 出發，只蓋已解欄位，
  未解區域一個 byte 都不動。驗收標準是 byte-for-byte round-trip。
- **不得 commit 或打包任何原版 `.EXE/.TPC/.15/.RGB/.MUS/.TIM/.DAT/.GLB/.DT1/.DT2/.SAV/.BGI`，
  也不得散布倚天字型。** 發行前跑 deny-list 掃描。
  **唯一例外（使用者明示 2026-08-01）：`docs/images/` 底下的 README 展示截圖。**
  那是渲染結果不是資產本身，還原不出任何原版檔案；沒有畫面就無從判斷進度。
  例外只到這裡——`deny_scan.sh` 會擋掉出現在其他位置的 PNG，所以這是白名單不是漏洞。
- **推論標籤要誠實**：confirmed / 強證據 / 假說 / 未知。不准為了讓 worklist 看起來完整而編規則。
- **每個結論要標明是在哪個執行檔上驗的**（見 §7 第 9 條）。
- **不碰共用的 docker 資源。** 只能清理自己這次建立的 container。**禁止**
  `docker image prune`、`docker system prune`、`docker volume prune`、
  `docker builder prune`、`docker rmi`、`docker container prune`。
- **截圖驗收要帶固定亂數種子。**
- **ESC 只取消／退回上一層，F10 才離開**，離開前跳 Y／N 確認並自動存檔；存檔失敗就不離開。

---

## 10. 每一輪的工作紀律

1. 動手前讀 `CONTEXT.md` worklist（狀態的單一真相來源）與相關的 `docs/re/`、`docs/spec/`。
2. 看 `git status --short`。既有改動屬於使用者或當前任務，**不要 reset 或丟棄**。
3. 做完一項：更新 markdown → **清掉被推翻的斷言**（不是加註解，是刪掉並記進推翻清單）
   → 跑測試 → 留視覺／實跑證據 → commit + push → 更新 `CONTEXT.md` 現況。
4. 宣稱「完成」之前，對照 reference 實測。**測試綠不是完成**，測試綠只是沒退步。
5. 簡易、可平行的工作派 subagent；派工時把邊界寫進 prompt。

### 派 subagent 的邊界（主迴圈自己要寫進 prompt）

- 只能清理自己建立的 container，禁止任何 docker image／volume／system prune 或 rmi。
- 明列不准改的目錄（其他 repo、`~/.claude/`、`workplace/orig/`）。
- 明列不准做的收尾動作（commit、push、重編、清理）。**沒寫的等於允許**。
- 收到「我順便做了 X」時，X 一律當事故處理：先查影響範圍再談結果。

### ⛔ 有 subagent 在跑時，主迴圈**不要 `git add -A`**

2026-08-01 的實例：派工 prompt 明文寫了「agent 不准 commit／push／add」，
但**主迴圈自己**在 agent 還在寫檔時跑 `git add -A && commit`，
把音訊 agent 的**未完成版** `docs/formats/06-mus-tim-audio.md` 與
`tools/mus.py` 掃進了 commit `4ccff9d`。

> **「禁止 agent commit」只管住了 agent，沒管住主迴圈。**
> 邊界要雙向：派工時限制 agent，同時也要限制自己。

作法：subagent 執行期間，主迴圈 commit 一律**列出明確路徑**，
不用 `-A` 也不用 `.`：

```sh
git add docs/re/31-battle-ai-chain.md CONTEXT.md internal/game/foo.go
```

真的要全加就先 `git status --short` 看一遍，確認沒有 agent 正在寫的檔案。
（這次的後果不嚴重——agent 完成後最終版又被下一個 commit 帶進去了——
但那是運氣，不是設計。）

---

## 11. 目錄結構

```
docs/re/          反組譯筆記（編號流水，一個發現一份）
docs/formats/     檔案格式規格
docs/spec/        實作規格（標 DRAFT / READY，只有 READY 能動手）
docs/mechanics/   遊戲機制，按屬性分類（§6.5）。70-ai.md 是電腦 AI 的判斷邏輯
docs/playtest/    實跑驗收紀錄與截圖
docs/reference/   外部中文資料整理（社群攻略、雜誌、wiki），標來源與抓取日期
translations/     glossary.md 譯名表、翻譯流程說明
internal/assets/  純解碼層，不認識 Ebiten
internal/game/    規則層，不認識畫面
internal/ui/      Ebiten 呈現層
cmd/              可執行程式（遊戲本體、抽字工具、解碼驗證工具）
tools/            docker 包裝腳本（go.sh、ida.sh、dosbox.sh）與 IDC 稽核腳本
workplace/orig/   原版素材（gitignore，唯讀）
workplace/ida/    解包後的 binary 與 IDA database（gitignore）
```

分層理由：排版邏輯（`internal/ui/textlayout`）**不能依賴 Ebiten**，
否則無頭環境連測都測不了——Ebiten 在 init 期就要求顯示器。

`CONTEXT.md` 的骨架：現況一覽表／文件索引／術語表／**已被推翻的斷言**／
Worklist（含「一句話現況」與「下一步」）／已建好的工具清單。

---

## 原始素材

- `game/`：解開的原版檔案（約 130 檔）
- `大時代的故事-jsdos.zip`：js-dos 打包版，同一份內容
- 兩者都要搬進 gitignore 的 `workplace/orig/` 並視為唯讀
